<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://ccl.cse.nd.edu/feed.xml" rel="self" type="application/atom+xml"/><link href="https://ccl.cse.nd.edu/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-15T19:11:40+00:00</updated><id>https://ccl.cse.nd.edu/feed.xml</id><title type="html">The Cooperative Computing Lab</title><subtitle>Cooperative Computing Lab website: news, projects, publications, software, and community updates. </subtitle><entry><title type="html">TaskVine Insights: Shipping Worker Builds to Remote Nodes and Debugging There</title><link href="https://ccl.cse.nd.edu/blog/2026/taskvine-insights-shipping-worker-builds-to-remote-nodes/" rel="alternate" type="text/html" title="TaskVine Insights: Shipping Worker Builds to Remote Nodes and Debugging There"/><published>2026-05-05T17:00:00+00:00</published><updated>2026-05-05T17:00:00+00:00</updated><id>https://ccl.cse.nd.edu/blog/2026/taskvine-insights-shipping-worker-builds-to-remote-nodes</id><content type="html" xml:base="https://ccl.cse.nd.edu/blog/2026/taskvine-insights-shipping-worker-builds-to-remote-nodes/"><![CDATA[<div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/taskvine-insights-shipping-worker-builds-to-remote-nodes/TaskVine-Insights-480.webp 480w,/assets/blog/2026/taskvine-insights-shipping-worker-builds-to-remote-nodes/TaskVine-Insights-800.webp 800w,/assets/blog/2026/taskvine-insights-shipping-worker-builds-to-remote-nodes/TaskVine-Insights-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/taskvine-insights-shipping-worker-builds-to-remote-nodes/TaskVine-Insights.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Once you are hacking <a href="https://github.com/cooperative-computing-lab/cctools/tree/master/taskvine/src/worker"><code class="language-plaintext highlighter-rouge">vine_worker</code></a> in C and feeding workers through HTCondor, two boring problems show up over and over: the pool still runs yesterday’s binary, and your printfs never appear next to the Python manager. This note is about staging the binary you meant to ship and getting worker-side trace somewhere useful. For the repo layout start with <a href="https://ccl.cse.nd.edu/blog/2026/taskvine-insights-a-beginners-map-to-the-cctools-codebase/">A Beginner’s Map to the CCTools Codebase</a>; for the usual <code class="language-plaintext highlighter-rouge">vine_submit_workers</code> / <code class="language-plaintext highlighter-rouge">vine_factory</code> workflow see <a href="https://ccl.cse.nd.edu/blog/2026/taskvine-insights-submitting-workers-to-a-cluster/">Submitting Workers to a Cluster</a>.</p> <p>None of that changes the basic fact that a live worker runs whatever was staged when the job went in, not necessarily whatever <code class="language-plaintext highlighter-rouge">make install</code> just dropped into <code class="language-plaintext highlighter-rouge">$PATH</code>. To pick up a new build you still have to drain the old worker jobs, stop a factory that is holding a stale scratch copy if one is running, install, and submit again.</p> <h2 id="the-install-that-actually-reaches-htcondor">The install that actually reaches HTCondor</h2> <p>After <code class="language-plaintext highlighter-rouge">./configure</code>, we still do the blunt rebuild from <a href="https://github.com/cooperative-computing-lab/cctools/tree/master/taskvine/src"><code class="language-plaintext highlighter-rouge">taskvine/src</code></a> when we want zero ambiguity:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>make clean <span class="o">&amp;&amp;</span> make <span class="o">&amp;&amp;</span> make <span class="nb">install</span>
</code></pre></div></div> <p>On your own machine you probably have a <code class="language-plaintext highlighter-rouge">vine_worker</code> running already. Stop it before <code class="language-plaintext highlighter-rouge">make clean &amp;&amp; make &amp;&amp; make install</code>. Overwriting the on-disk binary while the process still has it mapped is a good way to take a nose dive, sometimes straight into a segfault, and whatever tasks it was holding will go with it. Bring the worker back up against the manager after the install.</p> <p>On the cluster there is another wrinkle. <code class="language-plaintext highlighter-rouge">vine_submit_workers</code> drops a copy of <code class="language-plaintext highlighter-rouge">vine_worker</code> into its submit directory when you run it; <code class="language-plaintext highlighter-rouge">vine_factory</code> copies whatever <code class="language-plaintext highlighter-rouge">vine_worker</code> resolves to (unless you pointed it at <code class="language-plaintext highlighter-rouge">--worker-binary</code>) into <code class="language-plaintext highlighter-rouge">-S,--scratch-dir</code> once at startup, and every batch job runs <code class="language-plaintext highlighter-rouge">./vine_worker</code> out of that directory. Tweaking <code class="language-plaintext highlighter-rouge">$PATH</code> later while the factory is still running does nothing for jobs already in flight. When you need a clean slate, <code class="language-plaintext highlighter-rouge">condor_rm</code> tears down old worker jobs (plenty of sites use <code class="language-plaintext highlighter-rouge">condor_rm -all</code> to wipe your queue); kill the factory on the submit side if it is still recycling the old scratch tree; install; submit again so what landed in the staging directory matches what you think you built.</p> <h2 id="turning-on-logs-people-can-open">Turning on logs people can open</h2> <p>Extra <code class="language-plaintext highlighter-rouge">fprintf</code> lines in <code class="language-plaintext highlighter-rouge">vine_worker</code> are not going to show up in your Python manager transcript; you still need worker-side debug. <code class="language-plaintext highlighter-rouge">-d all</code> is the heavy-handed default that turns the noise up across subsystems. Add <code class="language-plaintext highlighter-rouge">-o</code> with a path when stderr is useless because you will never SSH to that execute node (<a href="https://github.com/cooperative-computing-lab/cctools/blob/master/taskvine/src/worker/vine_worker_options.c">CLI reference</a>).</p> <p>Sanity check on the laptop first:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>vine_worker <span class="nt">-d</span> all localhost 9123
</code></pre></div></div> <p>You want a wall of debug text right away; same flags later ride along inside Condor.</p> <p><code class="language-plaintext highlighter-rouge">vine_factory --debug-workers</code> appends <code class="language-plaintext highlighter-rouge">-d all -o worker.&lt;n&gt;.log</code> for each submission (the number is a factory-side counter, not Condor’s proc id). Those files hop back in the job sandbox when the run finishes because Condor uses <code class="language-plaintext highlighter-rouge">when_to_transfer_output = on_exit</code>, which is fine for an autopsy and useless if you wanted <code class="language-plaintext highlighter-rouge">tail -f</code> while the slot was still busy.</p> <p>For that you need a directory the execute node and your login node both mount. Hand the worker <code class="language-plaintext highlighter-rouge">-d all</code> and <code class="language-plaintext highlighter-rouge">-o /absolute/path/...</code> through <code class="language-plaintext highlighter-rouge">vine_factory</code> <code class="language-plaintext highlighter-rouge">-E,--extra-options</code>. Under the hood the Condor batch module writes a tiny <code class="language-plaintext highlighter-rouge">condor.sh</code> wrapper that does <code class="language-plaintext highlighter-rouge">eval "$@"</code> with the full command line as arguments (<a href="https://github.com/cooperative-computing-lab/cctools/blob/master/batch_job/src/batch_queue_condor.c"><code class="language-plaintext highlighter-rouge">batch_queue_condor.c</code></a>).</p> <p>Put something unique to each job in the filename. HTCondor’s <code class="language-plaintext highlighter-rouge">$(Cluster)</code> and <code class="language-plaintext highlighter-rouge">$(Process)</code> (or the <code class="language-plaintext highlighter-rouge">$(ClusterId)</code> / <code class="language-plaintext highlighter-rouge">$(ProcId)</code> spellings your site prefers) expand per proc; write them as <code class="language-plaintext highlighter-rouge">\$(Cluster)</code> and <code class="language-plaintext highlighter-rouge">\$(Process)</code> when an earlier shell would swallow the <code class="language-plaintext highlighter-rouge">$</code> before Condor sees them.</p> <p>Choose a parent dir <code class="language-plaintext highlighter-rouge">d</code> on the shared filesystem, maybe stamp <code class="language-plaintext highlighter-rouge">ts</code> when you kick off a factory, maybe add something like <code class="language-plaintext highlighter-rouge">ipid</code> if your wrapper exports it before <code class="language-plaintext highlighter-rouge">vine_worker</code> starts. The filename pattern is your own business as long as two workers never write the same path by accident.</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">d</span><span class="o">=</span>/cephfs/group/myproject/vine-logs
<span class="nv">ts</span><span class="o">=</span><span class="si">$(</span><span class="nb">date</span> +%Y%m%d%H%M%S<span class="si">)</span>
vine_factory <span class="nt">-T</span> condor <span class="nt">-M</span> myproject <span class="nt">-S</span> /scratch/<span class="nv">$USER</span>/vf <span class="se">\</span>
  <span class="nt">-E</span> <span class="s2">"-d all -o </span><span class="k">${</span><span class="nv">d</span><span class="k">}</span><span class="s2">/worker-</span><span class="k">${</span><span class="nv">ts</span><span class="k">}</span><span class="s2">-</span><span class="se">\$</span><span class="s2">(Cluster).</span><span class="se">\$</span><span class="s2">(Process).log"</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">ts</code> in that example is just a label for one factory session; <code class="language-plaintext highlighter-rouge">\$(Cluster).\$(Process)</code> keeps different Condor procs from stepping on each other. If you already have something like <code class="language-plaintext highlighter-rouge">ipid</code> in the environment and you trust it to be unique per writer, <code class="language-plaintext highlighter-rouge">${d}/worker-${ts}-${ipid}-\$(Cluster).\$(Process).log</code> is the same idea with more belt and suspenders.</p> <p>Do not turn on <code class="language-plaintext highlighter-rouge">--debug-workers</code> and a fat custom <code class="language-plaintext highlighter-rouge">-E</code> at the same time unless you like reading everything twice.</p> <p>From Python the same string can ride through <a href="https://github.com/cooperative-computing-lab/cctools/blob/master/taskvine/src/bindings/python3/ndcctools/taskvine/manager.py"><code class="language-plaintext highlighter-rouge">vine.Factory.extra_options</code></a>, which maps to <code class="language-plaintext highlighter-rouge">vine_factory --extra-options</code>. There is still no <code class="language-plaintext highlighter-rouge">debug_workers</code> knob in the binding, so either stay explicit in <code class="language-plaintext highlighter-rouge">extra_options</code> or shell out to the CLI if you need the factory-only switch:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">ndcctools.taskvine</span> <span class="k">as</span> <span class="n">vine</span>

<span class="n">manager</span> <span class="o">=</span> <span class="n">vine</span><span class="p">.</span><span class="nc">Manager</span><span class="p">(</span><span class="mi">9123</span><span class="p">)</span>
<span class="n">factory</span> <span class="o">=</span> <span class="n">vine</span><span class="p">.</span><span class="nc">Factory</span><span class="p">(</span><span class="n">batch_type</span><span class="o">=</span><span class="sh">"</span><span class="s">condor</span><span class="sh">"</span><span class="p">,</span> <span class="n">manager</span><span class="o">=</span><span class="n">manager</span><span class="p">,</span> <span class="n">log_file</span><span class="o">=</span><span class="sh">"</span><span class="s">factory.log</span><span class="sh">"</span><span class="p">)</span>
<span class="n">factory</span><span class="p">.</span><span class="n">extra_options</span> <span class="o">=</span> <span class="sa">r</span><span class="sh">"</span><span class="s">-d all -o /cephfs/group/myproject/vine-logs/worker-\$(Cluster).\$(Process).log</span><span class="sh">"</span>
<span class="k">with</span> <span class="n">factory</span><span class="p">:</span>
    <span class="n">job</span> <span class="o">=</span> <span class="n">vine</span><span class="p">.</span><span class="nc">Task</span><span class="p">(</span><span class="sh">"</span><span class="s">echo heartbeat</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">manager</span><span class="p">.</span><span class="nf">submit</span><span class="p">(</span><span class="n">job</span><span class="p">)</span>
    <span class="n">manager</span><span class="p">.</span><span class="nf">wait</span><span class="p">(</span><span class="n">timeout</span><span class="o">=</span><span class="mi">120</span><span class="p">)</span>
</code></pre></div></div> <p>When you pack up, skim the usual checklist: did the slot actually run the binary you staged, is <code class="language-plaintext highlighter-rouge">-d all</code> going somewhere you can read, and did you give each job a distinct path (Condor macros plus any label you added on purpose) so the logs never clobber each other.</p>]]></content><author><name>Cooperative Computing Lab</name></author><category term="technical-articles"/><category term="taskvine"/><category term="htcondor"/><category term="debugging"/><summary type="html"><![CDATA[On HTCondor, workers keep running the vine_worker binary that got staged at submit time, not the one you just installed into $PATH. Here is how to get a fresh build onto the nodes and how to point -d all output somewhere you can actually read.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://ccl.cse.nd.edu/assets/blog/2026/taskvine-insights-shipping-worker-builds-to-remote-nodes/TaskVine-Insights.png"/><media:content medium="image" url="https://ccl.cse.nd.edu/assets/blog/2026/taskvine-insights-shipping-worker-builds-to-remote-nodes/TaskVine-Insights.png" xmlns:media="http://search.yahoo.com/mrss/"/></entry><entry><title type="html">TaskVine Insights: Submitting Workers to a Cluster</title><link href="https://ccl.cse.nd.edu/blog/2026/taskvine-insights-submitting-workers-to-a-cluster/" rel="alternate" type="text/html" title="TaskVine Insights: Submitting Workers to a Cluster"/><published>2026-04-28T17:00:00+00:00</published><updated>2026-04-28T17:00:00+00:00</updated><id>https://ccl.cse.nd.edu/blog/2026/taskvine-insights-submitting-workers-to-a-cluster</id><content type="html" xml:base="https://ccl.cse.nd.edu/blog/2026/taskvine-insights-submitting-workers-to-a-cluster/"><![CDATA[<div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/taskvine-insights-submitting-workers-to-a-cluster/TaskVine-Insights-480.webp 480w,/assets/blog/2026/taskvine-insights-submitting-workers-to-a-cluster/TaskVine-Insights-800.webp 800w,/assets/blog/2026/taskvine-insights-submitting-workers-to-a-cluster/TaskVine-Insights-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/taskvine-insights-submitting-workers-to-a-cluster/TaskVine-Insights.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>A TaskVine application becomes useful on a cluster only after workers are placed where the compute capacity is. On a laptop, we may start a worker by hand:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>vine_worker HOST PORT
</code></pre></div></div> <p>That is fine for a local test. It is not how we want to run a real workflow on a shared system. A cluster already has a resource manager, and TaskVine should use it. CCTools supports worker submission through several batch systems, including HTCondor, Slurm, and UGE. At the Notre Dame CRC compute cluster, the common path for our TaskVine runs is HTCondor, so this post uses Condor as the concrete example.</p> <p>There are two ways to think about cluster workers. <code class="language-plaintext highlighter-rouge">vine_submit_workers</code> is the direct path. It submits a fixed number of workers and then gets out of the way. <code class="language-plaintext highlighter-rouge">vine_factory</code> is the managed path. It keeps watching the manager, estimates how many workers are needed, submits new workers when old ones disappear, and writes logs that are much easier to use during a postmortem.</p> <p>The <a href="https://cctools.readthedocs.io/en/latest/taskvine/">TaskVine User’s Manual</a> covers both tools. Here we focus on the HTCondor path and the Conda environment details that tend to matter in real runs.</p> <h2 id="the-runtime-shape">The runtime shape</h2> <p>A TaskVine manager listens on a host and port, or advertises itself under a project name through the catalog. Workers connect back to that manager. The batch system does not run your manager. It runs worker jobs that eventually execute <code class="language-plaintext highlighter-rouge">vine_worker</code> with the right connection arguments.</p> <p>That distinction explains the first command most users see in the manual:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>vine_submit_workers <span class="nt">-T</span> condor MACHINENAME 9123 10
</code></pre></div></div> <p>This asks HTCondor to start ten workers. When those jobs begin to run, each worker calls back to <code class="language-plaintext highlighter-rouge">MACHINENAME:9123</code>. If your manager uses a project name, the command becomes:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>vine_submit_workers <span class="nt">-T</span> condor <span class="nt">-M</span> myproject 10
</code></pre></div></div> <p>The project name form is usually easier once runs become repeatable, because workers can discover the manager from the catalog instead of hard coding a hostname and port.</p> <h2 id="direct-submission-with-vine_submit_workers">Direct submission with <code class="language-plaintext highlighter-rouge">vine_submit_workers</code></h2> <p><code class="language-plaintext highlighter-rouge">vine_submit_workers</code> is a shell script in CCTools. It supports Condor, Slurm, and UGE through <code class="language-plaintext highlighter-rouge">-T</code> or <code class="language-plaintext highlighter-rouge">--batch-type</code>. The Condor path is useful to understand because it shows what the tool is actually doing for us.</p> <p>For Condor, the script builds a small submission directory, usually <code class="language-plaintext highlighter-rouge">/tmp/$USER-workers</code>. It copies <code class="language-plaintext highlighter-rouge">vine_worker</code> into that directory. It also copies helper inputs such as <code class="language-plaintext highlighter-rouge">cctools_gpu_autodetect</code>. If <code class="language-plaintext highlighter-rouge">--poncho-env</code> is used, it also copies <code class="language-plaintext highlighter-rouge">poncho_package_run</code> and the packaged environment. Then it writes a <code class="language-plaintext highlighter-rouge">worker.sh</code> wrapper and a <code class="language-plaintext highlighter-rouge">condor_submit_file</code>.</p> <p>The generated Condor submit file uses <code class="language-plaintext highlighter-rouge">worker.sh</code> as the executable. It transfers the worker binary and all declared input files. It writes output to files named like <code class="language-plaintext highlighter-rouge">worker.$(CLUSTER).$(PROCESS).output</code>, error output to <code class="language-plaintext highlighter-rouge">worker.$(CLUSTER).$(PROCESS).error</code>, and Condor job events to <code class="language-plaintext highlighter-rouge">workers.log</code>. It also sets <code class="language-plaintext highlighter-rouge">getenv = true</code>, so the submitted job inherits the submit side environment unless you change the generated file or Condor policy rejects it.</p> <p>That automation is the main convenience. A practical HTCondor command looks like this:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>vine_submit_workers <span class="se">\</span>
  <span class="nt">-T</span> condor <span class="se">\</span>
  <span class="nt">--cores</span> 4 <span class="se">\</span>
  <span class="nt">--memory</span> 8192 <span class="se">\</span>
  <span class="nt">--disk</span> 20000 <span class="se">\</span>
  <span class="nt">--poncho-env</span> dv5-env.tar.gz <span class="se">\</span>
  <span class="nt">-M</span> myproject <span class="se">\</span>
  20
</code></pre></div></div> <p>This submits twenty workers. Each worker advertises four cores, 8192 MB of memory, and 20000 MB of disk to TaskVine, and the Condor submit file requests matching resources from Condor.</p> <p>One common gotcha is argument order. The script parses options until it reaches the final positional arguments. Put the manager address, or the worker count for a project name, at the end. Also put <code class="language-plaintext highlighter-rouge">-T condor</code> before Condor specific options such as <code class="language-plaintext highlighter-rouge">--requirements</code>, because the script only knows how to parse those options after the batch system has been selected. The two accepted shapes are:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>vine_submit_workers <span class="o">[</span>options] HOST PORT NUM_WORKERS
vine_submit_workers <span class="o">[</span>options] <span class="nt">-M</span> PROJECT NUM_WORKERS
</code></pre></div></div> <p>Another easy mistake is <code class="language-plaintext highlighter-rouge">localhost</code>. The script explicitly rejects <code class="language-plaintext highlighter-rouge">localhost</code> for Condor workers. A worker running on a remote execute node would interpret <code class="language-plaintext highlighter-rouge">localhost</code> as the execute node itself, not the manager. Use a routable hostname or a project name.</p> <p>Finally, <code class="language-plaintext highlighter-rouge">vine_submit_workers</code> submits a fixed number of jobs and exits. In the Condor path, the script writes <code class="language-plaintext highlighter-rouge">queue NUM_WORKERS</code>, runs <code class="language-plaintext highlighter-rouge">condor_submit</code>, and does not leave behind a monitor. If a worker exits because of a worker side bug, a segmentation fault, preemption, a node problem, or a random distributed systems failure, the submit command is no longer around to replace it. The manager can retry interrupted tasks when another worker is available, but the worker pool itself may silently shrink. You need to notice that through <code class="language-plaintext highlighter-rouge">condor_q</code>, TaskVine status, manager logs, or slow progress, and then resubmit workers yourself.</p> <h2 id="common-vine_submit_workers-options">Common <code class="language-plaintext highlighter-rouge">vine_submit_workers</code> options</h2> <p>Do not treat <code class="language-plaintext highlighter-rouge">vine_submit_workers --help</code> as a checklist. Most runs only need a small part of it.</p> <p>Where to submit:</p> <ul> <li><code class="language-plaintext highlighter-rouge">-T, --batch-type condor|slurm|uge</code>: choose the batch system. On Notre Dame CRC, we usually use <code class="language-plaintext highlighter-rouge">condor</code>.</li> <li><code class="language-plaintext highlighter-rouge">HOST PORT NUM_WORKERS</code>: connect workers directly to a manager address.</li> <li><code class="language-plaintext highlighter-rouge">-M, --manager-name PROJECT NUM_WORKERS</code>: connect workers to a manager advertised through the catalog.</li> </ul> <p>How many resources each worker should have:</p> <ul> <li><code class="language-plaintext highlighter-rouge">--cores N</code>: request this many cores and report the same number to TaskVine.</li> <li><code class="language-plaintext highlighter-rouge">--memory MB</code>: request this much memory and report it to TaskVine.</li> <li><code class="language-plaintext highlighter-rouge">--disk MB</code>: request this much disk and report it to TaskVine.</li> </ul> <p>How workers run:</p> <ul> <li><code class="language-plaintext highlighter-rouge">-t, --timeout SECONDS</code>: let idle workers exit after this time. The default is 900 seconds.</li> <li><code class="language-plaintext highlighter-rouge">--scratch-dir PATH</code>: choose where the local submit directory is created.</li> <li><code class="language-plaintext highlighter-rouge">--poncho-env FILE.tar.gz</code>: run the worker inside a packaged Poncho environment.</li> <li><code class="language-plaintext highlighter-rouge">--dry-run</code>: print the generated worker script and Condor submit file instead of submitting. Use this before submitting if the command has changed.</li> </ul> <p>Condor placement:</p> <ul> <li><code class="language-plaintext highlighter-rouge">-r, --requirements EXPR</code>: add a Condor requirements expression.</li> </ul> <p>The remaining flags are for less common site policies and advanced worker behavior. Leave them out of the first version of a run. When one of those cases comes up, check the <a href="https://cctools.readthedocs.io/en/latest/taskvine/">TaskVine manual</a> and the <a href="https://github.com/cooperative-computing-lab/cctools/blob/master/taskvine/src/tools/vine_submit_workers"><code class="language-plaintext highlighter-rouge">vine_submit_workers</code> source</a>.</p> <h2 id="why-a-factory-is-usually-better">Why a factory is usually better</h2> <p>Direct submission is convenient when the run is small, short, or interactive. For a serious workflow, the better default is <code class="language-plaintext highlighter-rouge">vine_factory</code>.</p> <p>The factory is a long running process. It periodically asks the manager, or the catalog, what work exists and how many workers are connected. It then compares the desired worker count with the workers it has already submitted. If it needs more workers, it submits them through the selected batch system. If workers exit, it observes those exits, removes them from its internal job table, and can submit replacements on the next cycle.</p> <p>That behavior matters on shared clusters. HTCondor can preempt jobs. Nodes can fail. Worker processes can crash. A manager can keep retrying tasks, but it cannot create new batch jobs by itself. The factory fills that gap.</p> <p>A normal Condor factory command looks like this:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>vine_factory <span class="se">\</span>
  <span class="nt">-T</span> condor <span class="se">\</span>
  <span class="nt">--min-workers</span> 2 <span class="se">\</span>
  <span class="nt">--max-workers</span> 50 <span class="se">\</span>
  <span class="nt">--workers-per-cycle</span> 10 <span class="se">\</span>
  <span class="nt">--cores</span> 4 <span class="se">\</span>
  <span class="nt">--memory</span> 8192 <span class="se">\</span>
  <span class="nt">--disk</span> 20000 <span class="se">\</span>
  <span class="nt">--poncho-env</span> dv5-env.tar.gz <span class="se">\</span>
  <span class="nt">--manager-name</span> myproject
</code></pre></div></div> <p>This keeps at least two workers and at most fifty workers available for managers named <code class="language-plaintext highlighter-rouge">myproject</code>. The factory checks the manager periodically, estimates worker demand from waiting and running tasks, caps the target at <code class="language-plaintext highlighter-rouge">--max-workers</code>, raises it to <code class="language-plaintext highlighter-rouge">--min-workers</code> if needed, and submits at most <code class="language-plaintext highlighter-rouge">--workers-per-cycle</code> new jobs in a single cycle.</p> <p>The implementation keeps an internal table of submitted batch job IDs. Each submitted worker runs a generated <code class="language-plaintext highlighter-rouge">vine_worker</code> command with the manager target, timeout, and requested resource shape. The factory then calls the batch queue layer to submit that job. Later, it waits for completed jobs with a short timeout. When a known worker job exits, the factory decrements its submitted count. That is what lets it replace lost workers without you watching <code class="language-plaintext highlighter-rouge">condor_q</code> all afternoon.</p> <p>The logs are also useful. <code class="language-plaintext highlighter-rouge">vine_factory -d vine -o factory.log</code> gives you a persistent record of what the factory thought the manager needed, how many jobs it submitted, and which jobs exited. <code class="language-plaintext highlighter-rouge">--debug-workers</code> adds per worker debug logs in the factory scratch directory. When a run fails at scale, this is much easier to debug than a one time submit command whose only durable state is in Condor.</p> <h2 id="common-vine_factory-options">Common <code class="language-plaintext highlighter-rouge">vine_factory</code> options</h2> <p>The factory has many knobs because it is a controller, not only a submit command. For normal Condor use, start with these.</p> <p>Where to submit and which manager to serve:</p> <ul> <li><code class="language-plaintext highlighter-rouge">-T, --batch-type TYPE</code>: required batch system type. Use <code class="language-plaintext highlighter-rouge">condor</code> for HTCondor.</li> <li><code class="language-plaintext highlighter-rouge">-M, -N, --manager-name PROJECT</code>: serve managers matching a project name or regular expression.</li> <li><code class="language-plaintext highlighter-rouge">HOST PORT</code>: connect directly to one manager instead of using the catalog.</li> <li><code class="language-plaintext highlighter-rouge">-C, --config-file FILE</code>: read options from a JSON config file. The factory re-reads this file periodically.</li> </ul> <p>How many workers to keep around:</p> <ul> <li><code class="language-plaintext highlighter-rouge">-w, --min-workers N</code>: keep at least this many workers.</li> <li><code class="language-plaintext highlighter-rouge">-W, --max-workers N</code>: never ask for more than this many workers.</li> <li><code class="language-plaintext highlighter-rouge">--workers-per-cycle N</code>: limit how many new workers the factory submits in one cycle.</li> <li><code class="language-plaintext highlighter-rouge">-t, --timeout SECONDS</code>: idle timeout passed to each worker.</li> <li><code class="language-plaintext highlighter-rouge">--factory-period SECONDS</code>: how often the factory re-evaluates demand.</li> </ul> <p>Worker resources:</p> <ul> <li><code class="language-plaintext highlighter-rouge">--cores N</code>: request and advertise cores per worker.</li> <li><code class="language-plaintext highlighter-rouge">--memory MB</code>: request and advertise memory per worker.</li> <li><code class="language-plaintext highlighter-rouge">--disk MB</code>: request and advertise disk per worker.</li> <li><code class="language-plaintext highlighter-rouge">--gpus N</code>: request and advertise GPUs per worker.</li> </ul> <p>Worker environment and logs:</p> <ul> <li><code class="language-plaintext highlighter-rouge">-S, --scratch-dir PATH</code>: choose the factory scratch directory.</li> <li><code class="language-plaintext highlighter-rouge">-d, --debug SUBSYSTEM</code>: enable factory debugging.</li> <li><code class="language-plaintext highlighter-rouge">-o, --debug-file FILE</code>: write factory debug output to a file.</li> <li><code class="language-plaintext highlighter-rouge">--debug-workers</code>: create worker side debug logs.</li> <li><code class="language-plaintext highlighter-rouge">--poncho-env FILE.tar.gz</code>: run each worker in a Poncho package.</li> </ul> <p>Condor placement:</p> <ul> <li><code class="language-plaintext highlighter-rouge">--condor-requirements EXPR</code>: add Condor requirements. Multiple uses are combined.</li> </ul> <p>The remaining factory options are for less common site policies and advanced worker setups. Leave them out of the first version of a run unless your cluster setup requires them. For the complete set, use the <a href="https://cctools.readthedocs.io/en/latest/taskvine/">TaskVine manual</a> and the <a href="https://github.com/cooperative-computing-lab/cctools/blob/master/batch_job/src/vine_factory.c"><code class="language-plaintext highlighter-rouge">vine_factory</code> source</a>.</p> <p>A config file is often cleaner than a long command:</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"manager-name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"myproject"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"min-workers"</span><span class="p">:</span><span class="w"> </span><span class="mi">2</span><span class="p">,</span><span class="w">
  </span><span class="nl">"max-workers"</span><span class="p">:</span><span class="w"> </span><span class="mi">50</span><span class="p">,</span><span class="w">
  </span><span class="nl">"workers-per-cycle"</span><span class="p">:</span><span class="w"> </span><span class="mi">10</span><span class="p">,</span><span class="w">
  </span><span class="nl">"factory-period"</span><span class="p">:</span><span class="w"> </span><span class="mi">30</span><span class="p">,</span><span class="w">
  </span><span class="nl">"cores"</span><span class="p">:</span><span class="w"> </span><span class="mi">4</span><span class="p">,</span><span class="w">
  </span><span class="nl">"memory"</span><span class="p">:</span><span class="w"> </span><span class="mi">8192</span><span class="p">,</span><span class="w">
  </span><span class="nl">"disk"</span><span class="p">:</span><span class="w"> </span><span class="mi">20000</span><span class="p">,</span><span class="w">
  </span><span class="nl">"condor-requirements"</span><span class="p">:</span><span class="w"> </span><span class="s2">"TARGET.OpSysAndVer == </span><span class="se">\"</span><span class="s2">AlmaLinux9</span><span class="se">\"</span><span class="s2">"</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p>Then start the factory with:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>vine_factory <span class="nt">-T</span> condor <span class="nt">-C</span> factory.json <span class="nt">--poncho-env</span> dv5-env.tar.gz
</code></pre></div></div> <p>The config file is re-read while the factory runs, so you can raise <code class="language-plaintext highlighter-rouge">max-workers</code>, lower <code class="language-plaintext highlighter-rouge">min-workers</code>, or adjust resource settings without restarting the controller.</p> <h2 id="a-small-wrapper-for-regular-crc-runs">A small wrapper for regular CRC runs</h2> <p>After a command becomes part of a weekly workflow, it is worth wrapping the repeated parts. In our day to day runs, we usually know the manager project name, the desired number of workers, the per worker resource shape, the scratch location, and the Poncho package. A thin shell wrapper makes that intent easier to read than a long <code class="language-plaintext highlighter-rouge">vine_factory</code> command.</p> <p>Here is a cleaned up version of the pattern:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/usr/bin/env bash</span>

<span class="nv">MANAGER_NAME</span><span class="o">=</span>dagvine-manager
<span class="nv">N_WORKERS</span><span class="o">=</span>40
<span class="nv">CORES</span><span class="o">=</span>16
<span class="nv">MEMORY_GB</span><span class="o">=</span>12
<span class="nv">DISK_GB</span><span class="o">=</span>100
<span class="nv">PONCHO_ENV</span><span class="o">=</span>dagvine-env.tar.gz
<span class="nv">SCRATCH_DIR</span><span class="o">=</span>/scratch365/<span class="nv">$USER</span>/factory_dv5
<span class="nv">CONDOR_REQUIREMENTS</span><span class="o">=</span><span class="s1">'((has_vast))'</span>

<span class="k">while</span> <span class="o">[</span> <span class="nv">$# </span><span class="nt">-gt</span> 0 <span class="o">]</span>
<span class="k">do
    if</span> <span class="o">[</span> <span class="s2">"</span><span class="nv">$1</span><span class="s2">"</span> <span class="o">=</span> <span class="s2">"-M"</span> <span class="o">]</span> <span class="o">||</span> <span class="o">[</span> <span class="s2">"</span><span class="nv">$1</span><span class="s2">"</span> <span class="o">=</span> <span class="s2">"--manager-name"</span> <span class="o">]</span>
    <span class="k">then
        </span><span class="nv">MANAGER_NAME</span><span class="o">=</span><span class="s2">"</span><span class="nv">$2</span><span class="s2">"</span>
        <span class="nb">shift </span>2
    <span class="k">elif</span> <span class="o">[</span> <span class="s2">"</span><span class="nv">$1</span><span class="s2">"</span> <span class="o">=</span> <span class="s2">"--workers"</span> <span class="o">]</span>
    <span class="k">then
        </span><span class="nv">N_WORKERS</span><span class="o">=</span><span class="s2">"</span><span class="nv">$2</span><span class="s2">"</span>
        <span class="nb">shift </span>2
    <span class="k">elif</span> <span class="o">[</span> <span class="s2">"</span><span class="nv">$1</span><span class="s2">"</span> <span class="o">=</span> <span class="s2">"--cores"</span> <span class="o">]</span>
    <span class="k">then
        </span><span class="nv">CORES</span><span class="o">=</span><span class="s2">"</span><span class="nv">$2</span><span class="s2">"</span>
        <span class="nb">shift </span>2
    <span class="k">elif</span> <span class="o">[</span> <span class="s2">"</span><span class="nv">$1</span><span class="s2">"</span> <span class="o">=</span> <span class="s2">"--memory"</span> <span class="o">]</span>
    <span class="k">then
        </span><span class="nv">MEMORY_GB</span><span class="o">=</span><span class="s2">"</span><span class="nv">$2</span><span class="s2">"</span>
        <span class="nb">shift </span>2
    <span class="k">elif</span> <span class="o">[</span> <span class="s2">"</span><span class="nv">$1</span><span class="s2">"</span> <span class="o">=</span> <span class="s2">"--disk"</span> <span class="o">]</span>
    <span class="k">then
        </span><span class="nv">DISK_GB</span><span class="o">=</span><span class="s2">"</span><span class="nv">$2</span><span class="s2">"</span>
        <span class="nb">shift </span>2
    <span class="k">elif</span> <span class="o">[</span> <span class="s2">"</span><span class="nv">$1</span><span class="s2">"</span> <span class="o">=</span> <span class="s2">"--poncho-env"</span> <span class="o">]</span>
    <span class="k">then
        </span><span class="nv">PONCHO_ENV</span><span class="o">=</span><span class="s2">"</span><span class="nv">$2</span><span class="s2">"</span>
        <span class="nb">shift </span>2
    <span class="k">else
        </span><span class="nb">echo</span> <span class="s2">"unknown option: </span><span class="nv">$1</span><span class="s2">"</span> <span class="o">&gt;</span>&amp;2
        <span class="nb">exit </span>2
    <span class="k">fi
done

</span><span class="nv">MEMORY_MB</span><span class="o">=</span><span class="k">$((</span>MEMORY_GB <span class="o">*</span> <span class="m">1024</span><span class="k">))</span>
<span class="nv">DISK_MB</span><span class="o">=</span><span class="k">$((</span>DISK_GB <span class="o">*</span> <span class="m">1024</span><span class="k">))</span>

vine_factory <span class="se">\</span>
    <span class="nt">-T</span> condor <span class="se">\</span>
    <span class="nt">--scratch-dir</span> <span class="s2">"</span><span class="nv">$SCRATCH_DIR</span><span class="s2">"</span> <span class="se">\</span>
    <span class="nt">--poncho-env</span> <span class="s2">"</span><span class="nv">$PONCHO_ENV</span><span class="s2">"</span> <span class="se">\</span>
    <span class="nt">--condor-requirements</span> <span class="s2">"</span><span class="nv">$CONDOR_REQUIREMENTS</span><span class="s2">"</span> <span class="se">\</span>
    <span class="nt">--manager-name</span> <span class="s2">"</span><span class="nv">$MANAGER_NAME</span><span class="s2">"</span> <span class="se">\</span>
    <span class="nt">--min-workers</span> <span class="s2">"</span><span class="nv">$N_WORKERS</span><span class="s2">"</span> <span class="se">\</span>
    <span class="nt">--max-workers</span> <span class="s2">"</span><span class="nv">$N_WORKERS</span><span class="s2">"</span> <span class="se">\</span>
    <span class="nt">--workers-per-cycle</span> <span class="s2">"</span><span class="nv">$N_WORKERS</span><span class="s2">"</span> <span class="se">\</span>
    <span class="nt">--cores</span> <span class="s2">"</span><span class="nv">$CORES</span><span class="s2">"</span> <span class="se">\</span>
    <span class="nt">--memory</span> <span class="s2">"</span><span class="nv">$MEMORY_MB</span><span class="s2">"</span> <span class="se">\</span>
    <span class="nt">--disk</span> <span class="s2">"</span><span class="nv">$DISK_MB</span><span class="s2">"</span> <span class="se">\</span>
    <span class="nt">--timeout</span> 36000
</code></pre></div></div> <p>The wrapper intentionally sets <code class="language-plaintext highlighter-rouge">min-workers</code>, <code class="language-plaintext highlighter-rouge">max-workers</code>, and <code class="language-plaintext highlighter-rouge">workers-per-cycle</code> to the same value. That makes the command behave like a fixed size pool, but with the factory still watching for worker exits and replacing lost jobs. The <code class="language-plaintext highlighter-rouge">--timeout 36000</code> setting keeps workers around for long runs. The <code class="language-plaintext highlighter-rouge">--condor-requirements '((has_vast))'</code> line is site specific. Keep it if your workflow needs that Condor attribute, and replace it with the requirement expression that matches your cluster policy otherwise.</p> <p>The script also accepts resource values in GB for memory and disk, then converts them to MB because <code class="language-plaintext highlighter-rouge">vine_factory --memory</code> and <code class="language-plaintext highlighter-rouge">--disk</code> expect megabytes. That small conversion removes a common source of mistakes when we are switching between Condor habits, shell scripts, and TaskVine options.</p> <h2 id="conda-environments-and-poncho">Conda environments and Poncho</h2> <p>Mismatched Conda environments create failures that look unrelated to worker submission. The worker binary, Python packages, shared libraries, and task imports should agree with the manager side assumptions. Start the manager, <code class="language-plaintext highlighter-rouge">vine_submit_workers</code>, and <code class="language-plaintext highlighter-rouge">vine_factory</code> from the same intended Conda environment whenever possible.</p> <p>For Python heavy workflows, package that environment and attach it to every worker. The workflow is small:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conda activate dv5-env
poncho_package_create <span class="s2">"</span><span class="k">${</span><span class="nv">CONDA_PREFIX</span><span class="k">}</span><span class="s2">"</span> dv5-env.tar.gz
</code></pre></div></div> <p>Then pass the tarball to the submitter:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>vine_submit_workers <span class="nt">-T</span> condor <span class="nt">--poncho-env</span> dv5-env.tar.gz <span class="nt">-M</span> myproject 20
</code></pre></div></div> <p>or to the factory:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>vine_factory <span class="nt">-T</span> condor <span class="nt">--poncho-env</span> dv5-env.tar.gz <span class="nt">--min-workers</span> 2 <span class="nt">--max-workers</span> 50 <span class="nt">-M</span> myproject
</code></pre></div></div> <p>The details are handled for you. <code class="language-plaintext highlighter-rouge">vine_submit_workers</code> ships <code class="language-plaintext highlighter-rouge">poncho_package_run</code> and the tarball as Condor input files, then runs <code class="language-plaintext highlighter-rouge">./poncho_package_run -e ENV.tar.gz -- ./vine_worker ...</code>. <code class="language-plaintext highlighter-rouge">vine_factory</code> implements <code class="language-plaintext highlighter-rouge">--poncho-env</code> as a wrapper around the worker command and adds both the wrapper tool and package as worker inputs. In both cases, the worker starts inside the packaged environment before it connects to the manager.</p> <h2 id="htcondor-operations-we-actually-use">HTCondor operations we actually use</h2> <p>Once workers are submitted, Condor is still the place to inspect and clean up batch jobs.</p> <p>Check your queued and running worker jobs:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>condor_q
</code></pre></div></div> <p>Remove workers when you want to stop cleanly:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>condor_rm <span class="nt">-all</span>
</code></pre></div></div> <p>On a shared system, be careful with <code class="language-plaintext highlighter-rouge">-all</code>. It removes your Condor jobs, and that is normally what we want when a TaskVine experiment needs a clean restart. After removal, also check for leftover local processes on the submit host:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ps aux | <span class="nb">grep </span>vine_worker
ps aux | <span class="nb">grep </span>vine_factory
</code></pre></div></div> <p>This matters because a stale factory can quietly submit new workers after you think the old run is gone. A stale worker or wrapper process can also leave the environment in a state that does not match the next manager run. Clean up before changing Conda environments, project names, worker resources, or package tarballs.</p> <p>Condor also lets you enter a worker sandbox:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>condor_ssh_to_job JOBID
</code></pre></div></div> <p>This is useful when a worker behaves differently on the execute node than it does on the submit node. You can inspect transferred files, the unpacked Poncho environment, logs, temporary outputs, and the actual command environment. For a worker crash, combine <code class="language-plaintext highlighter-rouge">condor_ssh_to_job</code>, <code class="language-plaintext highlighter-rouge">worker.$CLUSTER.$PROCESS.error</code>, factory logs, and manager logs before changing code.</p> <h2 id="which-tool-should-you-use">Which tool should you use</h2> <p>Use <code class="language-plaintext highlighter-rouge">vine_submit_workers</code> at the beginning. It is a good way to run a very small workflow, or a simple bag of tasks, and check that the basics are correct: the manager is reachable, workers can start on Condor, the Poncho package works, and the requested resources make sense.</p> <p>After that first check, switch to <code class="language-plaintext highlighter-rouge">vine_factory</code> and make it the habit. Normal cluster work should not depend on manually noticing that workers disappeared. The factory has a control loop. It notices exited worker jobs, keeps the pool between <code class="language-plaintext highlighter-rouge">min-workers</code> and <code class="language-plaintext highlighter-rouge">max-workers</code>, throttles new submissions with <code class="language-plaintext highlighter-rouge">workers-per-cycle</code>, adapts to manager demand, and leaves behind logs that explain its decisions.</p> <p>The manager still owns task scheduling and retries. Condor still owns placement and preemption. The factory sits between them and keeps the worker supply healthy. A good workflow is to validate with <code class="language-plaintext highlighter-rouge">vine_submit_workers</code>, then run real workloads with <code class="language-plaintext highlighter-rouge">vine_factory</code>.</p>]]></content><author><name>Cooperative Computing Lab</name></author><category term="technical-articles"/><category term="taskvine"/><category term="htcondor"/><summary type="html"><![CDATA[TaskVine workers can be submitted directly to batch systems or managed dynamically with vine_factory, with HTCondor details for worker resources, environment packaging, cleanup, and debugging.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://ccl.cse.nd.edu/assets/blog/2026/taskvine-insights-submitting-workers-to-a-cluster/TaskVine-Insights.png"/><media:content medium="image" url="https://ccl.cse.nd.edu/assets/blog/2026/taskvine-insights-submitting-workers-to-a-cluster/TaskVine-Insights.png" xmlns:media="http://search.yahoo.com/mrss/"/></entry><entry><title type="html">Congratulations to Colin Thomas on Passing His Candidacy Exam</title><link href="https://ccl.cse.nd.edu/blog/2026/congratulations-to-colin-on-passing-his-candidacy-exam/" rel="alternate" type="text/html" title="Congratulations to Colin Thomas on Passing His Candidacy Exam"/><published>2026-04-21T17:00:00+00:00</published><updated>2026-04-21T17:00:00+00:00</updated><id>https://ccl.cse.nd.edu/blog/2026/congratulations-to-colin-on-passing-his-candidacy-exam</id><content type="html" xml:base="https://ccl.cse.nd.edu/blog/2026/congratulations-to-colin-on-passing-his-candidacy-exam/"><![CDATA[<p>Colin Thomas passed his Ph.D. candidacy exam this April. We are thrilled for him, and congratulations, Colin. A lot of reading, writing, and committee prep went into getting here, and it paid off.</p> <p>Colin is a Ph.D. student in Computer Science and Engineering at Notre Dame and works in our group with Prof. Douglas Thain. His research interests are distributed systems and scientific workflows on clusters. Much of his work so far has centered on Parsl and TaskVine, on how scheduling and data placement behave when workflows mix a parallel filesystem with node-local disks, and on keeping related tasks and their intermediate files together instead of shuffling everything through shared storage. With candidacy behind him, we are looking forward to the next stretch of the Ph.D. and what he will build and publish from here.</p> <p>Nice work on candidacy, Colin.</p>]]></content><author><name>Cooperative Computing Lab</name></author><category term="news"/><category term="news"/><summary type="html"><![CDATA[Congratulations to Colin Thomas on his Ph.D. candidacy in April 2026.]]></summary></entry><entry><title type="html">Congratulations to Thanh Son Phung on Passing His Doctoral Defense</title><link href="https://ccl.cse.nd.edu/blog/2026/congratulations-to-thanh-on-passing-his-doctoral-defense/" rel="alternate" type="text/html" title="Congratulations to Thanh Son Phung on Passing His Doctoral Defense"/><published>2026-04-16T17:00:00+00:00</published><updated>2026-04-16T17:00:00+00:00</updated><id>https://ccl.cse.nd.edu/blog/2026/congratulations-to-thanh-on-passing-his-doctoral-defense</id><content type="html" xml:base="https://ccl.cse.nd.edu/blog/2026/congratulations-to-thanh-on-passing-his-doctoral-defense/"><![CDATA[<p>This month we are delighted to congratulate <strong>Thanh Son Phung</strong> on successfully defending his Ph.D. dissertation. Thanh has been part of our group since 2020.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/congratulations-to-thanh-on-passing-his-doctoral-defense/thanh-photo-480.webp 480w,/assets/blog/2026/congratulations-to-thanh-on-passing-his-doctoral-defense/thanh-photo-800.webp 800w,/assets/blog/2026/congratulations-to-thanh-on-passing-his-doctoral-defense/thanh-photo-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/congratulations-to-thanh-on-passing-his-doctoral-defense/thanh-photo.jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Thanh Son Phung" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Thanh’s research sits at the intersection of <strong>distributed systems</strong> and <strong>large-scale scientific computing</strong>. He focused on software that helps researchers decompose massive computational problems into many smaller tasks and run them in parallel efficiently—turning runs that might otherwise take weeks or days into hours or minutes. Along the way he collaborated with scientists across several areas, including molecular chemistry, particle physics, and artificial intelligence, to make their computations faster and more practical on modern clusters.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/congratulations-to-thanh-on-passing-his-doctoral-defense/couple-photo-480.webp 480w,/assets/blog/2026/congratulations-to-thanh-on-passing-his-doctoral-defense/couple-photo-800.webp 800w,/assets/blog/2026/congratulations-to-thanh-on-passing-his-doctoral-defense/couple-photo-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/congratulations-to-thanh-on-passing-his-doctoral-defense/couple-photo.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Thanh with his Ph.D. advisor" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>We are proud of this milestone and grateful for Thanh’s contributions to the lab. Well done, Thanh—and best wishes for what comes next.</p>]]></content><author><name>Cooperative Computing Lab</name></author><category term="news"/><category term="news"/><category term="graduation"/><summary type="html"><![CDATA[Thanh Son Phung successfully defended his Ph.D. dissertation in April 2026. He joined the lab in 2020 and worked on distributed systems for large-scale scientific computing.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://ccl.cse.nd.edu/assets/blog/2026/congratulations-to-thanh-on-passing-his-doctoral-defense/thanh-photo.jpg"/><media:content medium="image" url="https://ccl.cse.nd.edu/assets/blog/2026/congratulations-to-thanh-on-passing-his-doctoral-defense/thanh-photo.jpg" xmlns:media="http://search.yahoo.com/mrss/"/></entry><entry><title type="html">Introducing X-Bucket</title><link href="https://ccl.cse.nd.edu/blog/2026/introducing-x-bucket/" rel="alternate" type="text/html" title="Introducing X-Bucket"/><published>2026-04-09T17:00:00+00:00</published><updated>2026-04-09T17:00:00+00:00</updated><id>https://ccl.cse.nd.edu/blog/2026/introducing-x-bucket</id><content type="html" xml:base="https://ccl.cse.nd.edu/blog/2026/introducing-x-bucket/"><![CDATA[<p>Dynamic workflow systems enable local applications to scale out by decomposing their computational needs into task graphs on-the-fly and executing them on large clusters. However, this dynamic task generation raises a challenging resource allocation problem: upon scheduling, a task needs to be allocated some amount of resources, yet its resource consumption is only known after its execution.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/introducing-x-bucket/fig1-system-overview-480.webp 480w,/assets/blog/2026/introducing-x-bucket/fig1-system-overview-800.webp 800w,/assets/blog/2026/introducing-x-bucket/fig1-system-overview-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/introducing-x-bucket/fig1-system-overview.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>The figure above demonstrates the dynamicity of task generation in these workflow systems. On the manager node, the application first defines a large number of computations as functions and passes them via invocations to the workflow manager. The workflow manager takes care of packaging them into tasks by marshalling their arguments, detecting software dependencies, wrapping error handling code, etc., and resolves the inter-task dependencies into a dynamic task graph. Ready tasks are sent to the task allocator for resource allocation (e.g., cores, memory, disk, GPUs) before they arrive at the scheduler. The scheduler then maps tasks with a certain resource allocation into available workers for remote execution, where each task is run in a file system and resource sandbox. Upon task completion, workers send back the measured resource consumption of tasks to the task allocator for data collection purposes, and tasks’ results to the application.</p> <p>In addition to this inherent uncertainty, the resource allocation problem is further complicated by stochastic behaviors of workflows, which can be divided into two categories: Internal Stochasticity and External Stochasticity. Internal Stochasticity refers to random elements that are uncontrollable within the execution of a workflow, including (1) arbitrary ordering of task execution, where tasks are defined and submitted sequentially but executed in an arbitrary order due to a variety of factors (inter-task dependencies, data locality, etc.), (2) task specialization, in which tasks of different natures can be core-, memory-, or I/O-intensive, and (3) arbitrary structure of workflows, in which tasks are grouped into phases for coordination and computational purposes, resulting in a moving resource consumption distribution over time.</p> <p>External Stochasticity then refers to random elements that are uncontrollable between executions of the same workflow, including (1) the current system state, where workflow execution performance is subject to arbitrary usage from workloads of co-located users (e.g., shared storage, underlying physical network), (2) evolution of workflows, as workflows over time are run with different input datasets, require software updates, or users can add or remove a certain number of tasks in the workflow task generation logic, and (3) inherent stochasticity of tasks, as some tasks can be stochastic (e.g., Monte Carlo simulations, SGD-based training).</p> <p>To tackle the uncertainty of the resource allocation problem and address the stochasticity of workflows’ execution, we introduce X-Bucket, a family of four resource allocation algorithms that meet four following criteria: (1) general-purpose, as they do not rely on task- or workflow-specific information, (2) prior-free, where only information of the current run is used, (3) online, in which algorithms predict resource allocations as workflows run, and (4) robust, as we expect good resource efficiency across a diverse set of workflow behaviors.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/introducing-x-bucket/fig2-xbucket-integration-480.webp 480w,/assets/blog/2026/introducing-x-bucket/fig2-xbucket-integration-800.webp 800w,/assets/blog/2026/introducing-x-bucket/fig2-xbucket-integration-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/introducing-x-bucket/fig2-xbucket-integration.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>In the figure above, the diagram on the left shows how algorithms in the X-Bucket family fit in the components of a dynamic workflow system. When the workflow manager sends a ready task to the task allocator, it asks Bucket Composer, an object encapsulating the X-Bucket algorithms, for a resource allocation prediction. Once a task with that allocation finishes its execution, the task allocator forwards the resource record of the task to both Bucket Composer and the workflow manager so they can update their internal states. The diagram on the right shows the general bucketing approach, in which collected resource records are broken into several buckets, each with a probability value, which are then used in the prediction of the next task’s resource allocation.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/introducing-x-bucket/fig3-bucketing-algorithms-480.webp 480w,/assets/blog/2026/introducing-x-bucket/fig3-bucketing-algorithms-800.webp 800w,/assets/blog/2026/introducing-x-bucket/fig3-bucketing-algorithms-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/introducing-x-bucket/fig3-bucketing-algorithms.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>The figure above shows examples of how Greedy Bucketing and Exhaustive Bucketing, two sub-families in the X-Bucket family, work. While Greedy Bucketing algorithms recursively find an optimal break point and split a current region into two, Exhaustive Bucketing algorithms generate all possible combinations of bucket configurations, compute the associated expected resource wastes, and choose the one yielding the minimum value.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/introducing-x-bucket/fig4-evaluation-results-480.webp 480w,/assets/blog/2026/introducing-x-bucket/fig4-evaluation-results-800.webp 800w,/assets/blog/2026/introducing-x-bucket/fig4-evaluation-results-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/introducing-x-bucket/fig4-evaluation-results.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>The figure above shows a portion of the evaluation of the X-Bucket algorithms. In this figure, the rows are the resource types (cores, memory, and disk), and the columns are the evaluated workflows, including five synthetic workflows (Normal, Uniform, Bimodal, Trimodal, and Exponential) and three production workflows (ColmenaXTB, TopEFT, and LNNI). In each small figure, the first five bars are alternative algorithms: (1) Whole Machine, where each task is allocated a whole machine, (2) Max Seen, where each task is allocated the maximum resource consumption seen so far, (3) Min Waste and (4) Max Throughput, following their descriptions in Tovar et al. [2017], and (5) Quantized Bucketing, following their descriptions in Phung et al. [2021]. The last four bars are the X-Bucket algorithms, including PG-Bucket (Probabilistic Greedy Bucketing), PD-Bucket (Probabilistic Exhaustive Bucketing), DG-Bucket (Deterministic Greedy Bucketing), and DE-Bucket (Deterministic Exhaustive Bucketing). Results show that algorithms in the X-Bucket family consistently outperform alternative algorithms on all evaluated workflows, which we attribute to their designs in finding bucket configurations with minimum expected resource waste and to the four principles they follow: general-purpose, prior-free, online, and robust.</p>]]></content><author><name>Cooperative Computing Lab</name></author><category term="technical-articles"/><category term="resource-allocation"/><category term="workflows"/><summary type="html"><![CDATA[In cluster-backed dynamic workflows, the allocator must reserve cores, memory, and related resources before each task runs, but its real footprint only shows up afterward. Dynamic workflows make that puzzle even messier. X-Bucket turns streaming measurements into bucketed predictions that stay general, free of priors, and online. Read on for how it stacks up.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://ccl.cse.nd.edu/assets/blog/2026/introducing-x-bucket/fig1-system-overview.png"/><media:content medium="image" url="https://ccl.cse.nd.edu/assets/blog/2026/introducing-x-bucket/fig1-system-overview.png" xmlns:media="http://search.yahoo.com/mrss/"/></entry><entry><title type="html">TaskVine Insights: How to Propose a Pull Request to the CCL Team</title><link href="https://ccl.cse.nd.edu/blog/2026/taskvine-insights-how-to-propose-a-pull-request-to-the-ccl-team/" rel="alternate" type="text/html" title="TaskVine Insights: How to Propose a Pull Request to the CCL Team"/><published>2026-03-31T17:00:00+00:00</published><updated>2026-03-31T17:00:00+00:00</updated><id>https://ccl.cse.nd.edu/blog/2026/taskvine-insights-how-to-propose-a-pull-request-to-the-ccl-team</id><content type="html" xml:base="https://ccl.cse.nd.edu/blog/2026/taskvine-insights-how-to-propose-a-pull-request-to-the-ccl-team/"><![CDATA[<div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/taskvine-insights-how-to-propose-a-pull-request-to-the-ccl-team/TaskVine-Insights-480.webp 480w,/assets/blog/2026/taskvine-insights-how-to-propose-a-pull-request-to-the-ccl-team/TaskVine-Insights-800.webp 800w,/assets/blog/2026/taskvine-insights-how-to-propose-a-pull-request-to-the-ccl-team/TaskVine-Insights-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/taskvine-insights-how-to-propose-a-pull-request-to-the-ccl-team/TaskVine-Insights.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>TaskVine and CCTools improve when people share ideas, report bugs, and ship code. The Cooperative Computing Lab welcomes your participation. We want <strong>GitHub discussions</strong>, <strong>issues</strong>, <strong>pull requests</strong>, and <strong>patches</strong> to the codebase. You do not need permission to start a conversation or to propose a change.</p> <p>If you run TaskVine workflows, extend the runtime, or simply have a fix in mind for the <a href="https://github.com/cooperative-computing-lab/cctools">CCTools repository</a>, the sections below walk through a path we like: talk in public first, then build and test, then open a pull request that ties back to that conversation. The goal is to keep review fair and merges safe, not to add hoops for their own sake.</p> <h2 id="start-with-an-issue-or-a-discussion-not-with-a-branch">Start with an issue or a discussion, not with a branch</h2> <p>Before you fork the repo or name a branch, open a topic on the upstream project so others can see the problem and the proposed direction. For a concrete bug, the <a href="https://github.com/cooperative-computing-lab/cctools/issues">Issues</a> page is the usual place. Broader design questions sometimes fit better in <a href="https://github.com/cooperative-computing-lab/cctools/discussions">Discussions</a>. Either way, the goal is the same: you explain what fails or what you want, you share enough context that a reviewer can reproduce or reason about it, and you agree on what should change before you invest in a large patch.</p> <p>Logs and traces often matter. If a run log is large, upload it to a shared location such as Google Drive and link it in the issue or discussion so maintainers can inspect the same evidence you see. A pull request should <strong>link to that issue</strong> (for example <code class="language-plaintext highlighter-rouge">Fixes #1234</code> or <code class="language-plaintext highlighter-rouge">Refs #1234</code> in the description). We use the issue to confirm scope before the PR lands, and we close the issue when the change is merged and the problem is addressed.</p> <p>If you skip this step, you risk wasted effort. You might implement something the team would solve differently, or duplicate work someone else already started. The standardized flow is discuss first, then implement, then open the PR.</p> <h2 id="fork-the-official-repository">Fork the official repository</h2> <p>The canonical upstream remote is:</p> <p><code class="language-plaintext highlighter-rouge">https://github.com/cooperative-computing-lab/cctools.git</code></p> <p>Sign in to GitHub, open that repository, and use <strong>Fork</strong> to create a copy under your account. All of your <code class="language-plaintext highlighter-rouge">git push</code> work for a contribution should go to that fork. Pull requests run from your fork into <code class="language-plaintext highlighter-rouge">cooperative-computing-lab/cctools</code>, typically targeting the <code class="language-plaintext highlighter-rouge">master</code> branch unless maintainers ask otherwise.</p> <h2 id="clone-your-fork-and-enter-the-tree">Clone your fork and enter the tree</h2> <p>From your machine, clone <strong>your</strong> fork with SSH so you can push without typing a password each time. The clone URL looks like:</p> <p><code class="language-plaintext highlighter-rouge">git@github.com:&lt;your-username&gt;/cctools.git</code></p> <p>Example session:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone git@github.com:&lt;your-username&gt;/cctools.git cctools-test
<span class="nb">cd </span>cctools-test
</code></pre></div></div> <p>You can name the working directory anything you like. The important part is that the <code class="language-plaintext highlighter-rouge">origin</code> remote points at your fork.</p> <h2 id="build-with-conda-from-a-clean-environment">Build with Conda from a clean environment</h2> <p>The README and manual describe building CCTools from source. For day-to-day development, we isolate compilers and Python with Conda so <code class="language-plaintext highlighter-rouge">./configure</code> can discover everything from a single prefix. A typical Linux setup looks like this:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Create an isolated development environment with a pinned Python version.</span>
conda create <span class="nt">-y</span> <span class="nt">-n</span> cctools-dev <span class="nt">-c</span> conda-forge <span class="nt">--strict-channel-priority</span> <span class="nv">python</span><span class="o">=</span>3.11.12 conda-pack
<span class="c"># Activate the environment so the compiler, Python packages, and install prefix all point to the same place.</span>
conda activate cctools-dev
<span class="c"># Install the build toolchain and development utilities used by CCTools.</span>
conda <span class="nb">install</span> <span class="nt">-y</span> <span class="nt">-c</span> conda-forge <span class="nt">--strict-channel-priority</span> gcc_linux-64 gxx_linux-64 gdb m4 perl swig make zlib libopenssl-static openssl conda-pack packaging cloudpickle flake8 clang-format threadpoolctl
<span class="c"># Go to the directory you just cloned.</span>
<span class="nb">cd </span>cctools-test
<span class="c"># Discover dependencies from the active Conda environment and install back into that same environment.</span>
./configure <span class="nt">--with-base-dir</span> <span class="s2">"</span><span class="k">${</span><span class="nv">CONDA_PREFIX</span><span class="k">}</span><span class="s2">"</span> <span class="nt">--prefix</span> <span class="s2">"</span><span class="k">${</span><span class="nv">CONDA_PREFIX</span><span class="k">}</span><span class="s2">"</span>
<span class="c"># Remove stale build artifacts before compiling.</span>
make clean
<span class="c"># Build the repository in parallel, then install it.</span>
make <span class="nt">-j8</span>
make <span class="nb">install</span>
<span class="c"># Sanity check that the install path is active and the worker binary is usable.</span>
vine_worker <span class="nt">--version</span>
</code></pre></div></div> <p>If <code class="language-plaintext highlighter-rouge">vine_worker --version</code> prints a sensible version string, your toolchain and install prefix are aligned. The <a href="https://cctools.readthedocs.io/en/latest/taskvine/">TaskVine User’s Manual</a> and the top-level <a href="https://github.com/cooperative-computing-lab/cctools/blob/master/README.md">repository README</a> remain the references for environment quirks on other platforms.</p> <h2 id="create-a-branch-for-one-coherent-change">Create a branch for one coherent change</h2> <p>Work on a dedicated branch, not on <code class="language-plaintext highlighter-rouge">master</code> directly. Branch names should hint at the change. Suppose your patch adjusts the default manager keepalive timeout. In <code class="language-plaintext highlighter-rouge">taskvine/src/manager/vine_manager.c</code>, the default keepalive timeout is defined near the other manager defaults:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cm">/* Default value for keepalive timeout in seconds. */</span>
<span class="cp">#define VINE_DEFAULT_KEEPALIVE_TIMEOUT 900
</span></code></pre></div></div> <p>A branch name such as <code class="language-plaintext highlighter-rouge">change-default-keepalive-timeout</code> makes the intent obvious:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git switch <span class="nt">-c</span> change-default-keepalive-timeout
</code></pre></div></div> <p>Then edit the file and set the value you and the maintainers agreed on in the issue, for example <code class="language-plaintext highlighter-rouge">3600</code> instead of <code class="language-plaintext highlighter-rouge">900</code>. Keep the change minimal. One pull request should represent one logical fix or feature unless reviewers explicitly bundle work.</p> <h2 id="format-and-lint-before-you-push">Format and lint before you push</h2> <p>The top-level <code class="language-plaintext highlighter-rouge">Makefile</code> exposes <code class="language-plaintext highlighter-rouge">format</code> and <code class="language-plaintext highlighter-rouge">lint</code> targets. <strong>Run <code class="language-plaintext highlighter-rouge">make format</code> first</strong>, then <code class="language-plaintext highlighter-rouge">make lint</code>. Formatting applies <code class="language-plaintext highlighter-rouge">clang-format</code> (and package-specific rules) so C and related sources follow project style. Linting runs static checks such as <code class="language-plaintext highlighter-rouge">flake8</code> where configured. If you lint before format, you may still need a second lint pass after the formatter rewrites lines.</p> <p>From the repository root after <code class="language-plaintext highlighter-rouge">configure</code>:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>make format
make lint
</code></pre></div></div> <p>Fix anything these targets report. For larger changes, also read <code class="language-plaintext highlighter-rouge">STYLE.md</code> in the repository root if your edit touches areas with extra conventions.</p> <h2 id="run-the-test-suite">Run the test suite</h2> <p>Your toy one line constant change might not add new failures, but the standard we expect for any merge candidate is a clean <strong><code class="language-plaintext highlighter-rouge">make test</code></strong> from the top level. That driver invokes <code class="language-plaintext highlighter-rouge">run_all_tests.sh</code>, which exercises packages including TaskVine tests under <code class="language-plaintext highlighter-rouge">taskvine/test/</code>. If you change scheduling, bindings, or worker behavior, assume you must run the full suite and address regressions before opening or updating the PR.</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>make <span class="nb">test</span>
</code></pre></div></div> <h2 id="commit-push-to-your-fork-and-open-the-pull-request">Commit, push to your fork, and open the pull request</h2> <p>Stage and commit your work, then push the branch to <strong>your</strong> GitHub fork:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add <span class="nt">-u</span>
git commit <span class="nt">-m</span> <span class="s2">"Describe the change in one line"</span>
git push <span class="nt">-u</span> origin change-default-keepalive-timeout
</code></pre></div></div> <p>On GitHub, open your fork, switch to the branch you pushed, and use <strong>Compare and pull request</strong> (or the equivalent control) to open a PR against <code class="language-plaintext highlighter-rouge">cooperative-computing-lab/cctools</code>. Fill in a clear title and a description that states motivation, what you changed, and how you tested it. Link the issue you opened earlier. That link is what ties review conversation to the original problem statement.</p> <h2 id="ci-review-and-merge">CI, review, and merge</h2> <p>GitHub Actions and related checks run on the pull request. Expect on the order of <strong>ten minutes</strong> for a full run, depending on queue load. A green CI run is not optional for merge. It signals that the same tests you ran locally also pass in the project automation.</p> <p>At least <strong>one approval</strong> from a maintainer is required before merge. For visibility, you may tag maintainers such as <strong>dthain</strong> or <strong>btovar</strong> when you need a decision or a review slot, especially for behavior that affects scheduling or protocol-level defaults. Reviewers may request changes. Address feedback with additional commits on the same branch and push again. The PR updates automatically.</p> <p>When the maintainers merge your branch into <code class="language-plaintext highlighter-rouge">master</code>, return to the <strong>issue</strong> you linked. Close it if the fix or feature is complete, or note follow-up work if something remains out of scope.</p>]]></content><author><name>Cooperative Computing Lab</name></author><category term="technical-articles"/><category term="taskvine-insights"/><category term="taskvine"/><category term="cctools"/><summary type="html"><![CDATA[Contributions help TaskVine and CCTools move forward. This post welcomes discussion, pull requests, and patches, then describes the workflow from issue to merge and how to build, test, and submit against the official repository.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://ccl.cse.nd.edu/assets/blog/2026/taskvine-insights-how-to-propose-a-pull-request-to-the-ccl-team/TaskVine-Insights.png"/><media:content medium="image" url="https://ccl.cse.nd.edu/assets/blog/2026/taskvine-insights-how-to-propose-a-pull-request-to-the-ccl-team/TaskVine-Insights.png" xmlns:media="http://search.yahoo.com/mrss/"/></entry><entry><title type="html">Cursor Rules in Research Computing</title><link href="https://ccl.cse.nd.edu/blog/2026/cursor-rules-in-research-computing/" rel="alternate" type="text/html" title="Cursor Rules in Research Computing"/><published>2026-03-24T17:00:00+00:00</published><updated>2026-03-24T17:00:00+00:00</updated><id>https://ccl.cse.nd.edu/blog/2026/cursor-rules-in-research-computing</id><content type="html" xml:base="https://ccl.cse.nd.edu/blog/2026/cursor-rules-in-research-computing/"><![CDATA[<div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/cursor-rules-in-research-computing/readme-vs-agents-md-480.webp 480w,/assets/blog/2026/cursor-rules-in-research-computing/readme-vs-agents-md-800.webp 800w,/assets/blog/2026/cursor-rules-in-research-computing/readme-vs-agents-md-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/cursor-rules-in-research-computing/readme-vs-agents-md.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Over the last two years, AI coding assistants (Cursor, GitHub Copilot, OpenAI Codex, and others) have moved fast, from autocomplete to agents that plan and execute multi-step work. Used well, they not only speed up routine edits; they lower the cost of trying ideas, building proofs of concept, and prototyping systems. That matters a lot in research computing, where exploration and iteration are the default.</p> <p>I have used Cursor for more than a year. The feature that has paid off most consistently is <strong>rules</strong>: short, versioned instructions that give the model stable context about how we work—style, stack, project layout, and “how we run things.” They improve consistency across a codebase, cut repetition (“always use pytest,” “line length 100”), and act as lightweight team documentation that travels with the repo.</p> <p>Humans and AI assistants both do better with explicit guidance: a README orients people on install, usage, and contributing; an agent-oriented file does the same kind of job for the assistant—style, architecture, and how to run tests.</p> <p>Rules can be global, project-wide, or scoped to paths or file types. They are usually written in Markdown. In practice, I have used global and especially project-wide rules, set up in the following manner:</p> <ul> <li><strong>Project root:</strong> <code class="language-plaintext highlighter-rouge">AGENTS.md</code> (replacing the legacy single <code class="language-plaintext highlighter-rouge">.cursorrules</code> file). Keep it focused, on the order of hundreds of lines, and commit it like any other source file so it can evolve with the project.</li> <li><strong>Larger projects:</strong> split guidance into smaller files under <code class="language-plaintext highlighter-rouge">.cursor/rules/</code> so each file stays readable and maintainable.</li> </ul> <p>Structure beats length. I get better results when the file has clear sections, for example: agent role and domain (e.g. HPC, batch jobs, numerical code), repository layout, how to run tests and linters, and any non-negotiables for this codebase.</p> <p>In HPC and HTC, rules are a good place to spell out schedulers, managers, typical job patterns, and where experiments or outputs live.</p> <p>Finally, remember that Cursor treats these as strong guidance, not a rigid spec; the model can still drift. Treat rules as living docs: refine them when you see the same mistake twice.</p> <p>Community templates for stacks and project types: <a href="https://cursor.directory/">cursor.directory</a>.</p> <p>For example, a project-level file might include sections like the following:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>## Agent Role &amp; Expertise
You are an expert in:
- Software Architecture &amp; Engineering: Design patterns, modularity, scalability
- High Performance Computing (HPC): Parallel processing, resource optimization
- High Throughput Computing (HTC): Batch processing, job scheduling

## Project Structure
├── src/           # Python source code
├── tests/         # Unit tests (pytest)

## Development Commands
# Code quality
ruff check          # Type-check and lint
ruff format         # Auto-fix formatting
</code></pre></div></div>]]></content><author><name>Cooperative Computing Lab</name></author><category term="technical-articles"/><category term="ai-tools"/><summary type="html"><![CDATA[One of our lab researchers shares practical experience with Cursor rules and AGENTS.md-style project context: how they help AI assistants stay aligned with style, layout, and how we run tests in research computing workflows.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://ccl.cse.nd.edu/assets/blog/2026/cursor-rules-in-research-computing/readme-vs-agents-md.png"/><media:content medium="image" url="https://ccl.cse.nd.edu/assets/blog/2026/cursor-rules-in-research-computing/readme-vs-agents-md.png" xmlns:media="http://search.yahoo.com/mrss/"/></entry><entry><title type="html">TaskVine Insights: A Beginner’s Map to the CCTools Codebase and a First Task Dispatch Patch</title><link href="https://ccl.cse.nd.edu/blog/2026/taskvine-insights-a-beginners-map-to-the-cctools-codebase/" rel="alternate" type="text/html" title="TaskVine Insights: A Beginner’s Map to the CCTools Codebase and a First Task Dispatch Patch"/><published>2026-03-17T17:00:00+00:00</published><updated>2026-03-17T17:00:00+00:00</updated><id>https://ccl.cse.nd.edu/blog/2026/taskvine-insights-a-beginners-map-to-the-cctools-codebase</id><content type="html" xml:base="https://ccl.cse.nd.edu/blog/2026/taskvine-insights-a-beginners-map-to-the-cctools-codebase/"><![CDATA[<div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/taskvine-insights-a-beginners-map-to-the-cctools-codebase/TaskVine-Insights-480.webp 480w,/assets/blog/2026/taskvine-insights-a-beginners-map-to-the-cctools-codebase/TaskVine-Insights-800.webp 800w,/assets/blog/2026/taskvine-insights-a-beginners-map-to-the-cctools-codebase/TaskVine-Insights-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/taskvine-insights-a-beginners-map-to-the-cctools-codebase/TaskVine-Insights.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>The easiest way to approach the CCTools repository is to start from the surface we actually use. With TaskVine, that surface is usually Python: <code class="language-plaintext highlighter-rouge">import ndcctools.taskvine as vine</code>, create a manager, submit work, and wait for results. Once that flow is clear, the repository layout and the C runtime become much easier to navigate.</p> <p>That is the path we follow here: Python surface, repository map, then a small but real patch in <code class="language-plaintext highlighter-rouge">taskvine/src/manager/vine_manager.c</code> that prints which task was committed to which worker.</p> <h2 id="start-from-the-python-user-view">Start from the Python user view</h2> <p>The recent TaskVine Insights posts on Python task surfaces and DaskVine already show the first layer most of us meet: <a href="https://ccl.cse.nd.edu/blog/2026/taskvine-insights-picking-the-right-task-surface-in-python/">TaskVine Insights: Picking the Right Task Surface in Python</a> and <a href="https://ccl.cse.nd.edu/blog/2026/taskvine-insights-daskvine-executor/">TaskVine Insights: DaskVine Executor for Practical Scientific Graphs</a>. The key point is simple: the Python interface is intentionally small.</p> <p>At the simplest level, the entry point looks like this:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">ndcctools.taskvine</span> <span class="k">as</span> <span class="n">vine</span>

<span class="n">m</span> <span class="o">=</span> <span class="n">vine</span><span class="p">.</span><span class="nc">Manager</span><span class="p">([</span><span class="mi">9123</span><span class="p">,</span> <span class="mi">9129</span><span class="p">])</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Listening on port </span><span class="si">{</span><span class="n">m</span><span class="p">.</span><span class="n">port</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <p>From there, we usually choose one of four task surfaces:</p> <ul> <li><code class="language-plaintext highlighter-rouge">Task</code> for explicit command line tasks with explicit file edges.</li> <li><code class="language-plaintext highlighter-rouge">PythonTask</code> for sending a Python callable and arguments.</li> <li><code class="language-plaintext highlighter-rouge">FunctionCall</code> for many small calls that benefit from a long lived worker side library.</li> <li><code class="language-plaintext highlighter-rouge">DaskVine</code> for driving TaskVine from a Dask graph while still using the same underlying scheduler.</li> </ul> <p>That is the first useful mental model: the Python API is not separate from the runtime. It is the front door into the same TaskVine system.</p> <h2 id="where-taskvine-lives-inside-cctools">Where TaskVine lives inside CCTools</h2> <p>Once we know the Python entry point, the next question is where that code lives. The answer is direct: start in <code class="language-plaintext highlighter-rouge">cctools/taskvine</code>. The whole repository is here: <a href="https://github.com/cooperative-computing-lab/cctools">cooperative-computing-lab/cctools</a>.</p> <p>Here is the top level repository view on GitHub:</p> <div class="row justify-content-sm-center"> <div class="col-sm-12"> <img src="/assets/blog/2026/taskvine-insights-a-beginners-map-to-the-cctools-codebase/cctools-screenshot.png" alt="CCTools repository screenshot" class="img-fluid rounded z-depth-1"/> </div> </div> <p>The repository is broad because CCTools is a toolkit repository, not a single application. For a new TaskVine reader, not every top level directory matters equally. A practical first pass is:</p> <ul> <li><code class="language-plaintext highlighter-rouge">taskvine/</code> is the main TaskVine implementation.</li> <li><code class="language-plaintext highlighter-rouge">doc/</code> contains the manuals, including the official TaskVine manual at <a href="https://cctools.readthedocs.io/en/latest/taskvine/">TaskVine User’s Manual</a>.</li> <li><code class="language-plaintext highlighter-rouge">dttools/</code> provides shared low level infrastructure used by TaskVine and other tools.</li> <li><code class="language-plaintext highlighter-rouge">resource_monitor/</code> provides monitoring and resource measurement support that TaskVine uses directly.</li> <li><code class="language-plaintext highlighter-rouge">poncho/</code> is relevant because TaskVine uses execution environments and packaged Python environments in real workflows.</li> <li><code class="language-plaintext highlighter-rouge">batch_job/</code> matters because it is part of the broader execution and backend support layer in CCTools.</li> </ul> <p><code class="language-plaintext highlighter-rouge">work_queue/</code> is worth placing correctly too. It is historically relevant as an older predecessor in the same manager-worker family, but it is not the best starting point if the goal is to understand current TaskVine behavior. For that, begin with <code class="language-plaintext highlighter-rouge">taskvine/</code>.</p> <h2 id="the-python-layer-inside-cctoolstaskvine">The Python layer inside <code class="language-plaintext highlighter-rouge">cctools/taskvine</code></h2> <p>Inside <code class="language-plaintext highlighter-rouge">taskvine/</code>, the Python layer lives primarily in:</p> <ul> <li><code class="language-plaintext highlighter-rouge">taskvine/src/bindings/python3/</code></li> <li><code class="language-plaintext highlighter-rouge">taskvine/src/bindings/python3/ndcctools/taskvine/</code></li> </ul> <p>This is where we find the Python side of:</p> <ul> <li><code class="language-plaintext highlighter-rouge">Manager</code></li> <li><code class="language-plaintext highlighter-rouge">Task</code></li> <li><code class="language-plaintext highlighter-rouge">PythonTask</code></li> <li><code class="language-plaintext highlighter-rouge">FunctionCall</code></li> <li><code class="language-plaintext highlighter-rouge">DaskVine</code></li> </ul> <p>For a first read, a good path is:</p> <ol> <li>Read the TaskVine manual and a small Python example.</li> <li>Read the Python API layer in <code class="language-plaintext highlighter-rouge">taskvine/src/bindings/python3/ndcctools/taskvine/</code>.</li> <li>Only then step down into the C manager and worker runtime.</li> </ol> <p>That order is much friendlier than jumping straight into a large C file and trying to reverse engineer the programming model from internal state transitions.</p> <h2 id="a-practical-map-of-taskvine">A practical map of <code class="language-plaintext highlighter-rouge">taskvine/</code></h2> <p>For day to day development, two directories matter most:</p> <ul> <li><code class="language-plaintext highlighter-rouge">taskvine/src/</code> contains the implementation.</li> <li><code class="language-plaintext highlighter-rouge">taskvine/test/</code> contains focused regression and usage tests.</li> </ul> <p>Inside <code class="language-plaintext highlighter-rouge">taskvine/src/</code>, the three most useful entries for a first pass are:</p> <ul> <li><code class="language-plaintext highlighter-rouge">bindings/</code></li> <li><code class="language-plaintext highlighter-rouge">manager/</code></li> <li><code class="language-plaintext highlighter-rouge">worker/</code></li> </ul> <h3 id="taskvinesrcbindings"><code class="language-plaintext highlighter-rouge">taskvine/src/bindings/</code></h3> <p>This is where the user facing interfaces are assembled. In practice, the Python path under <code class="language-plaintext highlighter-rouge">taskvine/src/bindings/python3/ndcctools/taskvine/</code> is the one we usually read first. A few files are especially useful:</p> <ul> <li><code class="language-plaintext highlighter-rouge">manager.py</code> is a natural place to look when we want to understand the Python <code class="language-plaintext highlighter-rouge">Manager</code> surface.</li> <li><code class="language-plaintext highlighter-rouge">task.py</code> shows how Python task objects are represented and exposed.</li> <li><code class="language-plaintext highlighter-rouge">file.py</code> helps us understand the Python side of TaskVine file objects.</li> <li><code class="language-plaintext highlighter-rouge">futures.py</code> is useful when we want to see how the futures style interface is layered on top of the runtime.</li> <li><code class="language-plaintext highlighter-rouge">dask_executor.py</code> shows the modern DaskVine execution path.</li> <li><code class="language-plaintext highlighter-rouge">compat/dask_executor.py</code> shows the compatibility path for older Dask behavior.</li> <li><code class="language-plaintext highlighter-rouge">__init__.py</code> is a good quick check for what the package exports at the top level.</li> </ul> <h3 id="taskvinesrcmanager"><code class="language-plaintext highlighter-rouge">taskvine/src/manager/</code></h3> <p>This is the core manager side runtime and the most important C directory for understanding scheduling and dispatch. A few files are especially worth naming:</p> <ul> <li><code class="language-plaintext highlighter-rouge">vine_manager.c</code> is the large control flow file where we can see dispatch, completion handling, worker interactions, and runtime state updates.</li> <li><code class="language-plaintext highlighter-rouge">vine_schedule.c</code> is the right place to look when we want to understand how tasks are matched to workers.</li> <li><code class="language-plaintext highlighter-rouge">vine_task.c</code> helps explain how task state and task side bookkeeping are implemented in C.</li> <li><code class="language-plaintext highlighter-rouge">vine_worker_info.c</code> shows how the manager tracks workers and their metadata.</li> <li><code class="language-plaintext highlighter-rouge">vine_file.c</code> matters when we want to understand declared files, cached files, and the manager side file abstractions.</li> <li><code class="language-plaintext highlighter-rouge">vine_txn_log.c</code> and <code class="language-plaintext highlighter-rouge">vine_perf_log.c</code> are useful when we want to connect runtime behavior to logging and postmortem analysis.</li> </ul> <h3 id="taskvinesrcworker"><code class="language-plaintext highlighter-rouge">taskvine/src/worker/</code></h3> <p>This is the worker side runtime. It matters less than <code class="language-plaintext highlighter-rouge">manager/</code> for the specific patch in this post, but it becomes important as soon as we want to understand sandbox execution and what happens after a task arrives at a worker. Good entry points include:</p> <ul> <li><code class="language-plaintext highlighter-rouge">vine_worker.c</code> is the main worker program.</li> <li><code class="language-plaintext highlighter-rouge">vine_process.c</code> is useful when we want to understand task process startup and execution.</li> <li><code class="language-plaintext highlighter-rouge">vine_sandbox.c</code> helps explain task sandbox setup.</li> <li><code class="language-plaintext highlighter-rouge">vine_transfer.c</code> and <code class="language-plaintext highlighter-rouge">vine_transfer_server.c</code> matter for understanding how data moves between manager and worker.</li> <li><code class="language-plaintext highlighter-rouge">vine_cache.c</code> and <code class="language-plaintext highlighter-rouge">vine_cache_file.c</code> are useful when we want to understand worker side file caching behavior.</li> </ul> <h2 id="what-taskvinetest-is-for">What <code class="language-plaintext highlighter-rouge">taskvine/test/</code> is for</h2> <p><code class="language-plaintext highlighter-rouge">taskvine/test/</code> becomes important as soon as we change behavior. It contains focused tests and small usage programs, many paired with shell drivers. A few examples make the pattern clear:</p> <ul> <li><code class="language-plaintext highlighter-rouge">vine_python.py</code> and <code class="language-plaintext highlighter-rouge">TR_vine_python.sh</code> cover basic Python manager behavior.</li> <li><code class="language-plaintext highlighter-rouge">vine_python_task.py</code> and <code class="language-plaintext highlighter-rouge">TR_vine_python_task.sh</code> focus on Python task execution.</li> <li><code class="language-plaintext highlighter-rouge">vine_python_future_funcall.py</code> and related future tests exercise higher level Python interfaces.</li> <li><code class="language-plaintext highlighter-rouge">vine_python_serverless.py</code> and <code class="language-plaintext highlighter-rouge">vine_python_serverless_failure.py</code> cover the serverless and function call paths.</li> <li><code class="language-plaintext highlighter-rouge">vine_python_tag.py</code> is especially relevant when we think about logging task tags, because it exercises tagged tasks directly.</li> </ul> <p>When we run <code class="language-plaintext highlighter-rouge">make test</code> from the top level <code class="language-plaintext highlighter-rouge">cctools</code> directory, the repository test driver walks into each <code class="language-plaintext highlighter-rouge">package/test</code> directory and executes the executable <code class="language-plaintext highlighter-rouge">TR_*</code> scripts it finds there. In other words, <code class="language-plaintext highlighter-rouge">taskvine/test/</code> is part of the normal repository level validation path.</p> <h2 id="the-runtime-path-under-the-python-layer">The runtime path under the Python layer</h2> <p>One level below the Python entry points, the architecture becomes simple. A TaskVine application needs a manager and one or more workers. The Python code drives the manager, the manager decides what to run and where, and the workers execute tasks and return results.</p> <p>That core runtime lives in:</p> <ul> <li><code class="language-plaintext highlighter-rouge">taskvine/src/manager/</code></li> <li><code class="language-plaintext highlighter-rouge">taskvine/src/worker/</code></li> </ul> <p>For the specific question in this post, the interesting action is on the manager side. The main manager implementation lives in <code class="language-plaintext highlighter-rouge">taskvine/src/manager/</code>, which builds <code class="language-plaintext highlighter-rouge">libtaskvine.a</code> and is also the part linked by the Python binding.</p> <p>Within that directory, <code class="language-plaintext highlighter-rouge">taskvine/src/manager/vine_manager.c</code> is one of the central files because it contains much of the manager control flow that users feel at runtime:</p> <ul> <li>worker connection handling</li> <li>task dispatch and commit</li> <li>task completion handling</li> <li>failure and recovery paths</li> <li>runtime bookkeeping and statistics</li> </ul> <p>That makes <code class="language-plaintext highlighter-rouge">vine_manager.c</code> a sensible first C file once we understand the Python surface and want to see what happens after <code class="language-plaintext highlighter-rouge">m.submit(...)</code>.</p> <h2 id="how-a-user-actually-runs-taskvine">How a user actually runs TaskVine</h2> <p>The codebase is easier to navigate if we keep the runtime loop in mind. A normal user does something like this:</p> <ol> <li>Write a manager program.</li> <li>Run it so that it listens on a port.</li> <li>Start one or more <code class="language-plaintext highlighter-rouge">vine_worker</code> processes.</li> <li>Submit tasks and wait for results.</li> </ol> <p>A tiny example is enough to anchor that flow:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">ndcctools.taskvine</span> <span class="k">as</span> <span class="n">vine</span>

<span class="n">m</span> <span class="o">=</span> <span class="n">vine</span><span class="p">.</span><span class="nc">Manager</span><span class="p">(</span><span class="mi">9123</span><span class="p">)</span>

<span class="n">task</span> <span class="o">=</span> <span class="n">vine</span><span class="p">.</span><span class="nc">Task</span><span class="p">(</span><span class="sh">"</span><span class="s">echo hello from worker</span><span class="sh">"</span><span class="p">)</span>
<span class="n">task</span><span class="p">.</span><span class="nf">set_tag</span><span class="p">(</span><span class="sh">"</span><span class="s">hello-demo</span><span class="sh">"</span><span class="p">)</span>
<span class="n">m</span><span class="p">.</span><span class="nf">submit</span><span class="p">(</span><span class="n">task</span><span class="p">)</span>

<span class="k">while</span> <span class="ow">not</span> <span class="n">m</span><span class="p">.</span><span class="nf">empty</span><span class="p">():</span>
    <span class="n">done</span> <span class="o">=</span> <span class="n">m</span><span class="p">.</span><span class="nf">wait</span><span class="p">(</span><span class="mi">5</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">done</span><span class="p">:</span>
        <span class="nf">print</span><span class="p">(</span><span class="n">done</span><span class="p">.</span><span class="n">output</span><span class="p">)</span>
</code></pre></div></div> <p>Then we start a worker:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>vine_worker localhost 9123
</code></pre></div></div> <p>The official manual covers this workflow in more detail and should be the first reference for new readers: <a href="https://cctools.readthedocs.io/en/latest/taskvine/">TaskVine User’s Manual</a>.</p> <h2 id="how-to-clone-and-build-the-source">How to clone and build the source</h2> <p>If we want to study or modify the code instead of only using packaged binaries, it helps to build from a clean Conda environment. A practical from-scratch path looks like this:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Create an isolated development environment with a pinned Python version.</span>
conda create <span class="nt">-y</span> <span class="nt">-p</span> cctools-dev <span class="nt">-c</span> conda-forge <span class="nt">--strict-channel-priority</span> <span class="nv">python</span><span class="o">=</span>3.11.12 conda-pack
<span class="c"># Activate the environment so the compiler, Python packages, and install prefix all point to the same place.</span>
conda activate cctools-dev
<span class="c"># Install the build toolchain and development utilities used by CCTools.</span>
conda <span class="nb">install</span> <span class="nt">-y</span> <span class="nt">-c</span> conda-forge <span class="nt">--strict-channel-priority</span> gcc_linux-64 gxx_linux-64 gdb m4 perl swig make zlib libopenssl-static openssl conda-pack packaging cloudpickle flake8 clang-format threadpoolctl
<span class="c"># Clone the repository and enter its root directory.</span>
git clone https://github.com/cooperative-computing-lab/cctools.git
<span class="nb">cd </span>cctools
<span class="c"># Discover dependencies from the active Conda environment and install back into that same environment.</span>
./configure <span class="nt">--with-base-dir</span> <span class="s2">"</span><span class="k">${</span><span class="nv">CONDA_PREFIX</span><span class="k">}</span><span class="s2">"</span> <span class="nt">--prefix</span> <span class="s2">"</span><span class="k">${</span><span class="nv">CONDA_PREFIX</span><span class="k">}</span><span class="s2">"</span>
<span class="c"># Remove stale build artifacts before compiling.</span>
make clean
<span class="c"># Build the repository in parallel, then install it.</span>
make <span class="nt">-j8</span>
make <span class="nb">install</span>
<span class="c"># Sanity check that the install path is active and the worker binary is usable.</span>
vine_worker <span class="nt">--version</span>
</code></pre></div></div> <p>This is consistent with the source build direction described in the repository and the official documentation, while being more explicit about the environment and toolchain: <a href="https://github.com/cooperative-computing-lab/cctools">CCTools on GitHub</a> and <a href="https://cctools.readthedocs.io/en/latest/taskvine/">TaskVine User’s Manual</a>.</p> <h2 id="how-to-rebuild-after-modifying-taskvine">How to rebuild after modifying TaskVine</h2> <p>Suppose we edit <code class="language-plaintext highlighter-rouge">taskvine/src/manager/vine_manager.c</code>. The simplest and safest rebuild path is still:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>make clean
make <span class="nt">-j</span>
make <span class="nb">install</span>
</code></pre></div></div> <p>That approach removes guesswork. In practice, our rule is even simpler: after changing any C source in TaskVine, go to <code class="language-plaintext highlighter-rouge">taskvine/src/</code> and run a full clean rebuild:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd </span>taskvine/src
make clean <span class="o">&amp;&amp;</span> make <span class="nt">-j</span> <span class="o">&amp;&amp;</span> make <span class="nb">install</span>
</code></pre></div></div> <p>It is slower than rebuilding only one subdirectory, but it is the least ambiguous path when we want to be sure the change has propagated through the TaskVine sources, libraries, and bindings.</p> <p>One common exception is when we only change Python files in the bindings layer and do not touch any C source. Then we usually only need:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd </span>taskvine/src
make <span class="nb">install</span>
</code></pre></div></div> <h2 id="a-first-real-patch-in-vine_managerc">A first real patch in <code class="language-plaintext highlighter-rouge">vine_manager.c</code></h2> <p>Now we reach the concrete example. The requested feature is simple: when a task is successfully dispatched, print which task was committed to which worker.</p> <p>The target location sits inside <code class="language-plaintext highlighter-rouge">send_one_task_with_cr()</code> in <code class="language-plaintext highlighter-rouge">taskvine/src/manager/vine_manager.c</code>.</p> <p>This is a good place to start because it captures one core manager step in a compact form: selecting a ready task, finding a worker that can take it, and committing that task to the worker.</p> <p>Inside that function, the branch we care about is:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">case</span> <span class="n">VINE_SUCCESS</span><span class="p">:</span> <span class="cm">/* return on successful commit. */</span> <span class="p">{</span>
    <span class="n">committable_cores</span><span class="o">--</span><span class="p">;</span>
    <span class="n">total_inuse_cores</span><span class="o">++</span><span class="p">;</span>
    <span class="n">skip_list_remove_here</span><span class="p">(</span><span class="n">cur</span><span class="p">);</span>
    <span class="k">break</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <p>This location already sits on the successful commit path, and the needed metadata is already available. By the time the commit succeeds, the runtime can identify:</p> <ul> <li>the task through <code class="language-plaintext highlighter-rouge">t-&gt;task_id</code></li> <li>the Python level label through <code class="language-plaintext highlighter-rouge">t-&gt;tag</code></li> <li>the worker host through <code class="language-plaintext highlighter-rouge">w-&gt;hostname</code></li> <li>the worker address and port through <code class="language-plaintext highlighter-rouge">w-&gt;addrport</code></li> </ul> <p>So the patch is straightforward:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">case</span> <span class="n">VINE_SUCCESS</span><span class="p">:</span> <span class="cm">/* return on successful commit. */</span> <span class="p">{</span>
    <span class="n">committable_cores</span><span class="o">--</span><span class="p">;</span>
    <span class="n">total_inuse_cores</span><span class="o">++</span><span class="p">;</span>
    <span class="n">notice</span><span class="p">(</span><span class="n">D_VINE</span><span class="p">,</span>
            <span class="s">"Committed task %d (%s) to worker %s (%s)."</span><span class="p">,</span>
            <span class="n">t</span><span class="o">-&gt;</span><span class="n">task_id</span><span class="p">,</span>
            <span class="n">t</span><span class="o">-&gt;</span><span class="n">tag</span> <span class="o">?</span> <span class="n">t</span><span class="o">-&gt;</span><span class="n">tag</span> <span class="o">:</span> <span class="s">"no-tag"</span><span class="p">,</span>
            <span class="n">w</span><span class="o">-&gt;</span><span class="n">hostname</span> <span class="o">?</span> <span class="n">w</span><span class="o">-&gt;</span><span class="n">hostname</span> <span class="o">:</span> <span class="s">"unknown-host"</span><span class="p">,</span>
            <span class="n">w</span><span class="o">-&gt;</span><span class="n">addrport</span> <span class="o">?</span> <span class="n">w</span><span class="o">-&gt;</span><span class="n">addrport</span> <span class="o">:</span> <span class="s">"unknown-addrport"</span><span class="p">);</span>
    <span class="n">skip_list_remove_here</span><span class="p">(</span><span class="n">cur</span><span class="p">);</span>
    <span class="k">break</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <p>This is a good beginner patch because it stays within the existing manager logging style, prints both machine precise and human readable identifiers, and shows how much easier the runtime becomes to navigate once we follow the Python-to-manager path.</p> <h2 id="how-to-verify-the-patch">How to verify the patch</h2> <p>After applying the change, we rebuild the source:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd </span>taskvine/src
make clean <span class="o">&amp;&amp;</span> make <span class="nt">-j</span> <span class="o">&amp;&amp;</span> make <span class="nb">install</span>
</code></pre></div></div> <p>Then we run a tiny manager:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">ndcctools.taskvine</span> <span class="k">as</span> <span class="n">vine</span>

<span class="n">m</span> <span class="o">=</span> <span class="n">vine</span><span class="p">.</span><span class="nc">Manager</span><span class="p">(</span><span class="mi">9123</span><span class="p">)</span>

<span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">3</span><span class="p">):</span>
    <span class="n">t</span> <span class="o">=</span> <span class="n">vine</span><span class="p">.</span><span class="nc">Task</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">echo task-</span><span class="si">{</span><span class="n">i</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">t</span><span class="p">.</span><span class="nf">set_tag</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">demo-</span><span class="si">{</span><span class="n">i</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">m</span><span class="p">.</span><span class="nf">submit</span><span class="p">(</span><span class="n">t</span><span class="p">)</span>

<span class="k">while</span> <span class="ow">not</span> <span class="n">m</span><span class="p">.</span><span class="nf">empty</span><span class="p">():</span>
    <span class="n">done</span> <span class="o">=</span> <span class="n">m</span><span class="p">.</span><span class="nf">wait</span><span class="p">(</span><span class="mi">5</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">done</span><span class="p">:</span>
        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">done: </span><span class="si">{</span><span class="n">done</span><span class="p">.</span><span class="nb">id</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <p>In another terminal, we start a worker:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>vine_worker localhost 9123
</code></pre></div></div> <p>If everything is wired up correctly, the manager side output should now include lines like:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>2026/03/18 16:42:26.94 vine_manager[867641]notice: Committed task 1 (demo-0) to worker condorfe.crc.nd.edu (127.0.0.1:35434).
2026/03/18 16:42:26.95 vine_manager[867641]notice: Committed task 2 (demo-1) to worker condorfe.crc.nd.edu (127.0.0.1:35434).
2026/03/18 16:42:26.95 vine_manager[867641]notice: Committed task 3 (demo-2) to worker condorfe.crc.nd.edu (127.0.0.1:35434).
</code></pre></div></div> <h2 id="the-right-mental-model-to-keep">The right mental model to keep</h2> <p>For a beginner, the most useful mental model is still the simplest one: start from the Python surface, not from the C internals. The codebase entry is <code class="language-plaintext highlighter-rouge">cctools/taskvine</code>. The manual explains the programming model, the bindings show how that model appears in Python, and the manager and worker runtime in C show how it is implemented.</p> <p>Once we follow that order, even a first patch in <code class="language-plaintext highlighter-rouge">vine_manager.c</code> stops feeling mysterious. It becomes a small change inside a system whose layers already make sense.</p>]]></content><author><name>Cooperative Computing Lab</name></author><category term="technical-articles"/><category term="taskvine-insights"/><category term="taskvine"/><category term="cctools"/><summary type="html"><![CDATA[CCTools can look intimidating at first glance. This post gives beginners a practical map of the repository, explains how TaskVine fits into the larger system, and walks through a small but real source change that prints which task was committed to which worker.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://ccl.cse.nd.edu/assets/blog/2026/taskvine-insights-a-beginners-map-to-the-cctools-codebase/TaskVine-Insights.png"/><media:content medium="image" url="https://ccl.cse.nd.edu/assets/blog/2026/taskvine-insights-a-beginners-map-to-the-cctools-codebase/TaskVine-Insights.png" xmlns:media="http://search.yahoo.com/mrss/"/></entry><entry><title type="html">CCL Participated in the 18th ND CSE Annual Poster Session</title><link href="https://ccl.cse.nd.edu/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/" rel="alternate" type="text/html" title="CCL Participated in the 18th ND CSE Annual Poster Session"/><published>2026-03-10T17:00:00+00:00</published><updated>2026-03-10T17:00:00+00:00</updated><id>https://ccl.cse.nd.edu/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session</id><content type="html" xml:base="https://ccl.cse.nd.edu/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/"><![CDATA[<p>On February 26, 2026, we participated in the 18th ND CSE Annual Poster Session. It was a good chance for the lab to share what we’ve been working on. Here’s a quick rundown of what everyone presented.</p> <p><strong>Alan Malta</strong> presented his work on large-scale HEP workflows. CMS and other LHC experiments run workflows across the Worldwide LHC Computing Grid, and the way you group and compose tasks can make a big difference in throughput and resource use. Alan built DAGFlowSim to explore the trade-offs between different workflow constructions (TaskChain vs StepChain and everything in between) and figure out which compositions work best for event throughput, CPU utilization, and network efficiency.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Alan-480.webp 480w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Alan-800.webp 800w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Alan-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Alan.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Alan Malta Poster" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p><strong>Barry Sly-Delgado</strong> talked about restructuring workflows with algebraic labels. The idea is that if you can characterize operations by properties like commutativity and associativity, you can restructure the DAG for a better topology, with more parallelism, less overhead, and better fault tolerance. He integrated this with Dask and TaskVine and showed improvements on real HEP apps like RsTriPhoton, DVS, and tBar.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Barry-480.webp 480w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Barry-800.webp 800w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Barry-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Barry.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Barry Sly-Delgado Poster" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p><strong>Colin Thomas</strong> presented Contour, a tool that traces the I/O of a running application and turns it into an “execution contract.” Scientific apps often have implicit file dependencies that are hard to see from the script alone. Contour captures what gets read and written, then uses that to generate workflows for Makeflow so you can reproduce, distribute, and scale the app more reliably.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Colin-480.webp 480w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Colin-800.webp 800w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Colin-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Colin.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Colin Thomas Poster" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p><strong>Jin Zhou</strong> presented DAGVine, which tackles a bottleneck we’ve been hitting: when you have lots of small tasks, serialization and dispatch overhead can dominate. DAGVine pre-compiles the whole dependency graph and installs it on workers, so at runtime you just send lightweight task references instead of shipping Python objects every time. In benchmarks it gets up to 82× higher throughput than DaskVine.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Jin-480.webp 480w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Jin-800.webp 800w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Jin-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Jin.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Jin Zhou Poster" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p><strong>Laxminarayana Vadnala</strong> (Lax) presented SADE-SIM, a simulation platform for validating city-scale multi-drone operations. It combines physics simulation with photorealistic visualization so you can test autonomous UAV missions in realistic urban environments. His case study ran 32 heterogeneous drones over downtown Chicago with dynamic airspace policies. Only public-safety drones could enter a restricted zone, while others were deflected at the boundary.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Lax-480.webp 480w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Lax-800.webp 800w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Lax-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Lax.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Laxminarayana Vadnala Poster" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p><strong>Ryan Hartung</strong> presented xGFabric, which connects sensors in the field with HPC facilities over 5G/6G networks for real-time digital agriculture. Sensors at UNL collect weather data, which flows over a private 5G cell to a base station, then to UCSB, and finally triggers an OpenFOAM simulation on an HPC cluster. The whole pipeline runs in near real time so you can steer the simulation based on what’s happening in the field.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Ryan-480.webp 480w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Ryan-800.webp 800w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Ryan-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Ryan.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Ryan Hartung Poster" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p><strong>Md Saiful Islam</strong> presented his work on portable notebook workflows. Data-intensive notebooks are hard to move between HPC sites because of different storage, paths, and staging logic. Saiful’s Backpack abstraction uses declarative data specs and fingerprinting to capture what’s needed and run consistently across ND CRC, Anvil, and Stampede3. He demonstrated it on HEP analysis and image convolution workloads.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Saiful-480.webp 480w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Saiful-800.webp 800w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Saiful-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Saiful.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Md Saiful Islam Poster" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p><strong>Thanh Son Phung</strong> presented StickyInvoc, which rethinks how we run LLM inference at scale. The problem is that loading a model on a GPU is expensive, and on opportunistic clusters you might get preempted and have to reload over and over. StickyInvoc lets workers keep model state across tasks so subsequent inferences reuse it. In a claim verification workflow it hit 3.6× speedup, and scaled to 186 opportunistic GPU tasks in about 13 minutes.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Thanh-480.webp 480w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Thanh-800.webp 800w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Thanh-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Thanh.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Thanh Son Phung Poster" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>It was a great day to catch up with colleagues and share what we’ve been building. Thanks to everyone who stopped by our posters, and we look forward to the next one.</p>]]></content><author><name>Cooperative Computing Lab</name></author><category term="news"/><category term="news"/><category term="poster-session"/><summary type="html"><![CDATA[CCL team members presented eight research posters at the 18th ND CSE Annual Poster Session on February 26, 2026, spanning HEP workflows, DAG execution, reproducibility, digital agriculture, and LLM inference.]]></summary></entry><entry><title type="html">HEP Development at the CCL</title><link href="https://ccl.cse.nd.edu/blog/2026/hep-development-at-the-ccl/" rel="alternate" type="text/html" title="HEP Development at the CCL"/><published>2026-03-03T17:00:00+00:00</published><updated>2026-03-03T17:00:00+00:00</updated><id>https://ccl.cse.nd.edu/blog/2026/hep-development-at-the-ccl</id><content type="html" xml:base="https://ccl.cse.nd.edu/blog/2026/hep-development-at-the-ccl/"><![CDATA[<div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/hep-development-at-the-ccl/application-stack-480.webp 480w,/assets/blog/2026/hep-development-at-the-ccl/application-stack-800.webp 800w,/assets/blog/2026/hep-development-at-the-ccl/application-stack-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/hep-development-at-the-ccl/application-stack.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>The Cooperative Computing Lab has been longtime collaborators with the NDCMS project here at Notre Dame. Over the years, we have seen many developments in the space of high energy physics analyses and now support a variety of methods of scaling these applications to a large number of compute nodes at a variety of compute sites. HEP analyses have utilized Coffea, a Python toolkit which allows users to easily manipulate columnar data and scale from local execution to large clusters.</p> <p>Early versions of Coffea (0.7) incorporated dedicated “executors” which utilized a variety of workflow management systems. Initial work into the Coffea Work Queue executor allowed users to utilize Work Queue to distribute their applications onto a variety of execution environments. Later improvements and restructuring to workflow scheduling led to the development of TaskVine. Like Work Queue, a TaskVine executor was developed for analyses that utilized Coffea (0.7).</p> <p>With the release of Coffea (2024), Dask was utilized as a layer underneath to create a task graph and distribute work to compute resources. To facilitate compatibility, DaskVine was created, which is a compatible distributed executor that utilizes TaskVine and serves as an alternative to Dask’s native dask.distributed scheduler. Here, Dask would generate the task graph but DaskVine would manage the synthesis and distribution of individual tasks.</p> <p>Even further development with Coffea (2025) returned to the individual executor model seen in Coffea (0.7) while maintaining support with Dask as an option for distributing work. With this, an updated version of the TaskVine executor was created. Limitations in executing very large task graphs led to the development of VineReduce, which aimed to solve this issue by taking advantage of the map-reduce-like nature of these applications and some commonality in analysis applications.</p> <p>At this moment, a variety of applications currently use these application stacks to distribute their work on to compute clusters. This includes, RsTriPhoton, DV5, ttbarEFT, and TopEFT.</p>]]></content><author><name>Cooperative Computing Lab</name></author><category term="technical-articles"/><category term="hep"/><category term="taskvine"/><category term="coffea"/><category term="daskvine"/><category term="vine-reduce"/><summary type="html"><![CDATA[CCL has long collaborated with NDCMS on high-energy physics workflows. A look at how we evolved from Coffea 0.7 executors through DaskVine and VineReduce to power production apps like RsTriPhoton and TopEFT.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://ccl.cse.nd.edu/assets/blog/2026/hep-development-at-the-ccl/application-stack.png"/><media:content medium="image" url="https://ccl.cse.nd.edu/assets/blog/2026/hep-development-at-the-ccl/application-stack.png" xmlns:media="http://search.yahoo.com/mrss/"/></entry></feed>