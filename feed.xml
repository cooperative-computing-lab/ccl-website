<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://ccl.cse.nd.edu/feed.xml" rel="self" type="application/atom+xml"/><link href="https://ccl.cse.nd.edu/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-03-25T13:21:16+00:00</updated><id>https://ccl.cse.nd.edu/feed.xml</id><title type="html">The Cooperative Computing Lab</title><subtitle>Cooperative Computing Lab website: news, projects, publications, software, and community updates. </subtitle><entry><title type="html">Cursor Rules in Research Computing</title><link href="https://ccl.cse.nd.edu/blog/2026/cursor-rules-in-research-computing/" rel="alternate" type="text/html" title="Cursor Rules in Research Computing"/><published>2026-03-24T17:00:00+00:00</published><updated>2026-03-24T17:00:00+00:00</updated><id>https://ccl.cse.nd.edu/blog/2026/cursor-rules-in-research-computing</id><content type="html" xml:base="https://ccl.cse.nd.edu/blog/2026/cursor-rules-in-research-computing/"><![CDATA[<div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/cursor-rules-in-research-computing/readme-vs-agents-md-480.webp 480w,/assets/blog/2026/cursor-rules-in-research-computing/readme-vs-agents-md-800.webp 800w,/assets/blog/2026/cursor-rules-in-research-computing/readme-vs-agents-md-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/cursor-rules-in-research-computing/readme-vs-agents-md.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Over the last two years, AI coding assistants (Cursor, GitHub Copilot, OpenAI Codex, and others) have moved fast, from autocomplete to agents that plan and execute multi-step work. Used well, they not only speed up routine edits; they lower the cost of trying ideas, building proofs of concept, and prototyping systems. That matters a lot in research computing, where exploration and iteration are the default.</p> <p>I have used Cursor for more than a year. The feature that has paid off most consistently is <strong>rules</strong>: short, versioned instructions that give the model stable context about how we work—style, stack, project layout, and “how we run things.” They improve consistency across a codebase, cut repetition (“always use pytest,” “line length 100”), and act as lightweight team documentation that travels with the repo.</p> <p>Humans and AI assistants both do better with explicit guidance: a README orients people on install, usage, and contributing; an agent-oriented file does the same kind of job for the assistant—style, architecture, and how to run tests.</p> <p>Rules can be global, project-wide, or scoped to paths or file types. They are usually written in Markdown. In practice, I have used global and especially project-wide rules, set up in the following manner:</p> <ul> <li><strong>Project root:</strong> <code class="language-plaintext highlighter-rouge">AGENTS.md</code> (replacing the legacy single <code class="language-plaintext highlighter-rouge">.cursorrules</code> file). Keep it focused, on the order of hundreds of lines, and commit it like any other source file so it can evolve with the project.</li> <li><strong>Larger projects:</strong> split guidance into smaller files under <code class="language-plaintext highlighter-rouge">.cursor/rules/</code> so each file stays readable and maintainable.</li> </ul> <p>Structure beats length. I get better results when the file has clear sections, for example: agent role and domain (e.g. HPC, batch jobs, numerical code), repository layout, how to run tests and linters, and any non-negotiables for this codebase.</p> <p>In HPC and HTC, rules are a good place to spell out schedulers, managers, typical job patterns, and where experiments or outputs live.</p> <p>Finally, remember that Cursor treats these as strong guidance, not a rigid spec; the model can still drift. Treat rules as living docs: refine them when you see the same mistake twice.</p> <p>Community templates for stacks and project types: <a href="https://cursor.directory/">cursor.directory</a>.</p> <p>For example, a project-level file might include sections like the following:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>## Agent Role &amp; Expertise
You are an expert in:
- Software Architecture &amp; Engineering: Design patterns, modularity, scalability
- High Performance Computing (HPC): Parallel processing, resource optimization
- High Throughput Computing (HTC): Batch processing, job scheduling

## Project Structure
├── src/           # Python source code
├── tests/         # Unit tests (pytest)

## Development Commands
# Code quality
ruff check          # Type-check and lint
ruff format         # Auto-fix formatting
</code></pre></div></div>]]></content><author><name>Cooperative Computing Lab</name></author><category term="technical-articles"/><category term="ai-tools"/><summary type="html"><![CDATA[One of our lab researchers shares practical experience with Cursor rules and AGENTS.md-style project context: how they help AI assistants stay aligned with style, layout, and how we run tests in research computing workflows.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://ccl.cse.nd.edu/assets/blog/2026/cursor-rules-in-research-computing/readme-vs-agents-md.png"/><media:content medium="image" url="https://ccl.cse.nd.edu/assets/blog/2026/cursor-rules-in-research-computing/readme-vs-agents-md.png" xmlns:media="http://search.yahoo.com/mrss/"/></entry><entry><title type="html">TaskVine Insights: A Beginner’s Map to the CCTools Codebase and a First Task Dispatch Patch</title><link href="https://ccl.cse.nd.edu/blog/2026/taskvine-insights-a-beginners-map-to-the-cctools-codebase/" rel="alternate" type="text/html" title="TaskVine Insights: A Beginner’s Map to the CCTools Codebase and a First Task Dispatch Patch"/><published>2026-03-17T17:00:00+00:00</published><updated>2026-03-17T17:00:00+00:00</updated><id>https://ccl.cse.nd.edu/blog/2026/taskvine-insights-a-beginners-map-to-the-cctools-codebase</id><content type="html" xml:base="https://ccl.cse.nd.edu/blog/2026/taskvine-insights-a-beginners-map-to-the-cctools-codebase/"><![CDATA[<div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/taskvine-insights-a-beginners-map-to-the-cctools-codebase/TaskVine-Insights-480.webp 480w,/assets/blog/2026/taskvine-insights-a-beginners-map-to-the-cctools-codebase/TaskVine-Insights-800.webp 800w,/assets/blog/2026/taskvine-insights-a-beginners-map-to-the-cctools-codebase/TaskVine-Insights-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/taskvine-insights-a-beginners-map-to-the-cctools-codebase/TaskVine-Insights.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>The easiest way to approach the CCTools repository is to start from the surface we actually use. With TaskVine, that surface is usually Python: <code class="language-plaintext highlighter-rouge">import ndcctools.taskvine as vine</code>, create a manager, submit work, and wait for results. Once that flow is clear, the repository layout and the C runtime become much easier to navigate.</p> <p>That is the path we follow here: Python surface, repository map, then a small but real patch in <code class="language-plaintext highlighter-rouge">taskvine/src/manager/vine_manager.c</code> that prints which task was committed to which worker.</p> <h2 id="start-from-the-python-user-view">Start from the Python user view</h2> <p>The recent TaskVine Insights posts on Python task surfaces and DaskVine already show the first layer most of us meet: <a href="https://ccl.cse.nd.edu/blog/2026/taskvine-insights-picking-the-right-task-surface-in-python/">TaskVine Insights: Picking the Right Task Surface in Python</a> and <a href="https://ccl.cse.nd.edu/blog/2026/taskvine-insights-daskvine-executor/">TaskVine Insights: DaskVine Executor for Practical Scientific Graphs</a>. The key point is simple: the Python interface is intentionally small.</p> <p>At the simplest level, the entry point looks like this:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">ndcctools.taskvine</span> <span class="k">as</span> <span class="n">vine</span>

<span class="n">m</span> <span class="o">=</span> <span class="n">vine</span><span class="p">.</span><span class="nc">Manager</span><span class="p">([</span><span class="mi">9123</span><span class="p">,</span> <span class="mi">9129</span><span class="p">])</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Listening on port </span><span class="si">{</span><span class="n">m</span><span class="p">.</span><span class="n">port</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <p>From there, we usually choose one of four task surfaces:</p> <ul> <li><code class="language-plaintext highlighter-rouge">Task</code> for explicit command line tasks with explicit file edges.</li> <li><code class="language-plaintext highlighter-rouge">PythonTask</code> for sending a Python callable and arguments.</li> <li><code class="language-plaintext highlighter-rouge">FunctionCall</code> for many small calls that benefit from a long lived worker side library.</li> <li><code class="language-plaintext highlighter-rouge">DaskVine</code> for driving TaskVine from a Dask graph while still using the same underlying scheduler.</li> </ul> <p>That is the first useful mental model: the Python API is not separate from the runtime. It is the front door into the same TaskVine system.</p> <h2 id="where-taskvine-lives-inside-cctools">Where TaskVine lives inside CCTools</h2> <p>Once we know the Python entry point, the next question is where that code lives. The answer is direct: start in <code class="language-plaintext highlighter-rouge">cctools/taskvine</code>. The whole repository is here: <a href="https://github.com/cooperative-computing-lab/cctools">cooperative-computing-lab/cctools</a>.</p> <p>Here is the top level repository view on GitHub:</p> <div class="row justify-content-sm-center"> <div class="col-sm-12"> <img src="/assets/blog/2026/taskvine-insights-a-beginners-map-to-the-cctools-codebase/cctools-screenshot.png" alt="CCTools repository screenshot" class="img-fluid rounded z-depth-1"/> </div> </div> <p>The repository is broad because CCTools is a toolkit repository, not a single application. For a new TaskVine reader, not every top level directory matters equally. A practical first pass is:</p> <ul> <li><code class="language-plaintext highlighter-rouge">taskvine/</code> is the main TaskVine implementation.</li> <li><code class="language-plaintext highlighter-rouge">doc/</code> contains the manuals, including the official TaskVine manual at <a href="https://cctools.readthedocs.io/en/latest/taskvine/">TaskVine User’s Manual</a>.</li> <li><code class="language-plaintext highlighter-rouge">dttools/</code> provides shared low level infrastructure used by TaskVine and other tools.</li> <li><code class="language-plaintext highlighter-rouge">resource_monitor/</code> provides monitoring and resource measurement support that TaskVine uses directly.</li> <li><code class="language-plaintext highlighter-rouge">poncho/</code> is relevant because TaskVine uses execution environments and packaged Python environments in real workflows.</li> <li><code class="language-plaintext highlighter-rouge">batch_job/</code> matters because it is part of the broader execution and backend support layer in CCTools.</li> </ul> <p><code class="language-plaintext highlighter-rouge">work_queue/</code> is worth placing correctly too. It is historically relevant as an older predecessor in the same manager-worker family, but it is not the best starting point if the goal is to understand current TaskVine behavior. For that, begin with <code class="language-plaintext highlighter-rouge">taskvine/</code>.</p> <h2 id="the-python-layer-inside-cctoolstaskvine">The Python layer inside <code class="language-plaintext highlighter-rouge">cctools/taskvine</code></h2> <p>Inside <code class="language-plaintext highlighter-rouge">taskvine/</code>, the Python layer lives primarily in:</p> <ul> <li><code class="language-plaintext highlighter-rouge">taskvine/src/bindings/python3/</code></li> <li><code class="language-plaintext highlighter-rouge">taskvine/src/bindings/python3/ndcctools/taskvine/</code></li> </ul> <p>This is where we find the Python side of:</p> <ul> <li><code class="language-plaintext highlighter-rouge">Manager</code></li> <li><code class="language-plaintext highlighter-rouge">Task</code></li> <li><code class="language-plaintext highlighter-rouge">PythonTask</code></li> <li><code class="language-plaintext highlighter-rouge">FunctionCall</code></li> <li><code class="language-plaintext highlighter-rouge">DaskVine</code></li> </ul> <p>For a first read, a good path is:</p> <ol> <li>Read the TaskVine manual and a small Python example.</li> <li>Read the Python API layer in <code class="language-plaintext highlighter-rouge">taskvine/src/bindings/python3/ndcctools/taskvine/</code>.</li> <li>Only then step down into the C manager and worker runtime.</li> </ol> <p>That order is much friendlier than jumping straight into a large C file and trying to reverse engineer the programming model from internal state transitions.</p> <h2 id="a-practical-map-of-taskvine">A practical map of <code class="language-plaintext highlighter-rouge">taskvine/</code></h2> <p>For day to day development, two directories matter most:</p> <ul> <li><code class="language-plaintext highlighter-rouge">taskvine/src/</code> contains the implementation.</li> <li><code class="language-plaintext highlighter-rouge">taskvine/test/</code> contains focused regression and usage tests.</li> </ul> <p>Inside <code class="language-plaintext highlighter-rouge">taskvine/src/</code>, the three most useful entries for a first pass are:</p> <ul> <li><code class="language-plaintext highlighter-rouge">bindings/</code></li> <li><code class="language-plaintext highlighter-rouge">manager/</code></li> <li><code class="language-plaintext highlighter-rouge">worker/</code></li> </ul> <h3 id="taskvinesrcbindings"><code class="language-plaintext highlighter-rouge">taskvine/src/bindings/</code></h3> <p>This is where the user facing interfaces are assembled. In practice, the Python path under <code class="language-plaintext highlighter-rouge">taskvine/src/bindings/python3/ndcctools/taskvine/</code> is the one we usually read first. A few files are especially useful:</p> <ul> <li><code class="language-plaintext highlighter-rouge">manager.py</code> is a natural place to look when we want to understand the Python <code class="language-plaintext highlighter-rouge">Manager</code> surface.</li> <li><code class="language-plaintext highlighter-rouge">task.py</code> shows how Python task objects are represented and exposed.</li> <li><code class="language-plaintext highlighter-rouge">file.py</code> helps us understand the Python side of TaskVine file objects.</li> <li><code class="language-plaintext highlighter-rouge">futures.py</code> is useful when we want to see how the futures style interface is layered on top of the runtime.</li> <li><code class="language-plaintext highlighter-rouge">dask_executor.py</code> shows the modern DaskVine execution path.</li> <li><code class="language-plaintext highlighter-rouge">compat/dask_executor.py</code> shows the compatibility path for older Dask behavior.</li> <li><code class="language-plaintext highlighter-rouge">__init__.py</code> is a good quick check for what the package exports at the top level.</li> </ul> <h3 id="taskvinesrcmanager"><code class="language-plaintext highlighter-rouge">taskvine/src/manager/</code></h3> <p>This is the core manager side runtime and the most important C directory for understanding scheduling and dispatch. A few files are especially worth naming:</p> <ul> <li><code class="language-plaintext highlighter-rouge">vine_manager.c</code> is the large control flow file where we can see dispatch, completion handling, worker interactions, and runtime state updates.</li> <li><code class="language-plaintext highlighter-rouge">vine_schedule.c</code> is the right place to look when we want to understand how tasks are matched to workers.</li> <li><code class="language-plaintext highlighter-rouge">vine_task.c</code> helps explain how task state and task side bookkeeping are implemented in C.</li> <li><code class="language-plaintext highlighter-rouge">vine_worker_info.c</code> shows how the manager tracks workers and their metadata.</li> <li><code class="language-plaintext highlighter-rouge">vine_file.c</code> matters when we want to understand declared files, cached files, and the manager side file abstractions.</li> <li><code class="language-plaintext highlighter-rouge">vine_txn_log.c</code> and <code class="language-plaintext highlighter-rouge">vine_perf_log.c</code> are useful when we want to connect runtime behavior to logging and postmortem analysis.</li> </ul> <h3 id="taskvinesrcworker"><code class="language-plaintext highlighter-rouge">taskvine/src/worker/</code></h3> <p>This is the worker side runtime. It matters less than <code class="language-plaintext highlighter-rouge">manager/</code> for the specific patch in this post, but it becomes important as soon as we want to understand sandbox execution and what happens after a task arrives at a worker. Good entry points include:</p> <ul> <li><code class="language-plaintext highlighter-rouge">vine_worker.c</code> is the main worker program.</li> <li><code class="language-plaintext highlighter-rouge">vine_process.c</code> is useful when we want to understand task process startup and execution.</li> <li><code class="language-plaintext highlighter-rouge">vine_sandbox.c</code> helps explain task sandbox setup.</li> <li><code class="language-plaintext highlighter-rouge">vine_transfer.c</code> and <code class="language-plaintext highlighter-rouge">vine_transfer_server.c</code> matter for understanding how data moves between manager and worker.</li> <li><code class="language-plaintext highlighter-rouge">vine_cache.c</code> and <code class="language-plaintext highlighter-rouge">vine_cache_file.c</code> are useful when we want to understand worker side file caching behavior.</li> </ul> <h2 id="what-taskvinetest-is-for">What <code class="language-plaintext highlighter-rouge">taskvine/test/</code> is for</h2> <p><code class="language-plaintext highlighter-rouge">taskvine/test/</code> becomes important as soon as we change behavior. It contains focused tests and small usage programs, many paired with shell drivers. A few examples make the pattern clear:</p> <ul> <li><code class="language-plaintext highlighter-rouge">vine_python.py</code> and <code class="language-plaintext highlighter-rouge">TR_vine_python.sh</code> cover basic Python manager behavior.</li> <li><code class="language-plaintext highlighter-rouge">vine_python_task.py</code> and <code class="language-plaintext highlighter-rouge">TR_vine_python_task.sh</code> focus on Python task execution.</li> <li><code class="language-plaintext highlighter-rouge">vine_python_future_funcall.py</code> and related future tests exercise higher level Python interfaces.</li> <li><code class="language-plaintext highlighter-rouge">vine_python_serverless.py</code> and <code class="language-plaintext highlighter-rouge">vine_python_serverless_failure.py</code> cover the serverless and function call paths.</li> <li><code class="language-plaintext highlighter-rouge">vine_python_tag.py</code> is especially relevant when we think about logging task tags, because it exercises tagged tasks directly.</li> </ul> <p>When we run <code class="language-plaintext highlighter-rouge">make test</code> from the top level <code class="language-plaintext highlighter-rouge">cctools</code> directory, the repository test driver walks into each <code class="language-plaintext highlighter-rouge">package/test</code> directory and executes the executable <code class="language-plaintext highlighter-rouge">TR_*</code> scripts it finds there. In other words, <code class="language-plaintext highlighter-rouge">taskvine/test/</code> is part of the normal repository level validation path.</p> <h2 id="the-runtime-path-under-the-python-layer">The runtime path under the Python layer</h2> <p>One level below the Python entry points, the architecture becomes simple. A TaskVine application needs a manager and one or more workers. The Python code drives the manager, the manager decides what to run and where, and the workers execute tasks and return results.</p> <p>That core runtime lives in:</p> <ul> <li><code class="language-plaintext highlighter-rouge">taskvine/src/manager/</code></li> <li><code class="language-plaintext highlighter-rouge">taskvine/src/worker/</code></li> </ul> <p>For the specific question in this post, the interesting action is on the manager side. The main manager implementation lives in <code class="language-plaintext highlighter-rouge">taskvine/src/manager/</code>, which builds <code class="language-plaintext highlighter-rouge">libtaskvine.a</code> and is also the part linked by the Python binding.</p> <p>Within that directory, <code class="language-plaintext highlighter-rouge">taskvine/src/manager/vine_manager.c</code> is one of the central files because it contains much of the manager control flow that users feel at runtime:</p> <ul> <li>worker connection handling</li> <li>task dispatch and commit</li> <li>task completion handling</li> <li>failure and recovery paths</li> <li>runtime bookkeeping and statistics</li> </ul> <p>That makes <code class="language-plaintext highlighter-rouge">vine_manager.c</code> a sensible first C file once we understand the Python surface and want to see what happens after <code class="language-plaintext highlighter-rouge">m.submit(...)</code>.</p> <h2 id="how-a-user-actually-runs-taskvine">How a user actually runs TaskVine</h2> <p>The codebase is easier to navigate if we keep the runtime loop in mind. A normal user does something like this:</p> <ol> <li>Write a manager program.</li> <li>Run it so that it listens on a port.</li> <li>Start one or more <code class="language-plaintext highlighter-rouge">vine_worker</code> processes.</li> <li>Submit tasks and wait for results.</li> </ol> <p>A tiny example is enough to anchor that flow:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">ndcctools.taskvine</span> <span class="k">as</span> <span class="n">vine</span>

<span class="n">m</span> <span class="o">=</span> <span class="n">vine</span><span class="p">.</span><span class="nc">Manager</span><span class="p">(</span><span class="mi">9123</span><span class="p">)</span>

<span class="n">task</span> <span class="o">=</span> <span class="n">vine</span><span class="p">.</span><span class="nc">Task</span><span class="p">(</span><span class="sh">"</span><span class="s">echo hello from worker</span><span class="sh">"</span><span class="p">)</span>
<span class="n">task</span><span class="p">.</span><span class="nf">set_tag</span><span class="p">(</span><span class="sh">"</span><span class="s">hello-demo</span><span class="sh">"</span><span class="p">)</span>
<span class="n">m</span><span class="p">.</span><span class="nf">submit</span><span class="p">(</span><span class="n">task</span><span class="p">)</span>

<span class="k">while</span> <span class="ow">not</span> <span class="n">m</span><span class="p">.</span><span class="nf">empty</span><span class="p">():</span>
    <span class="n">done</span> <span class="o">=</span> <span class="n">m</span><span class="p">.</span><span class="nf">wait</span><span class="p">(</span><span class="mi">5</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">done</span><span class="p">:</span>
        <span class="nf">print</span><span class="p">(</span><span class="n">done</span><span class="p">.</span><span class="n">output</span><span class="p">)</span>
</code></pre></div></div> <p>Then we start a worker:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>vine_worker localhost 9123
</code></pre></div></div> <p>The official manual covers this workflow in more detail and should be the first reference for new readers: <a href="https://cctools.readthedocs.io/en/latest/taskvine/">TaskVine User’s Manual</a>.</p> <h2 id="how-to-clone-and-build-the-source">How to clone and build the source</h2> <p>If we want to study or modify the code instead of only using packaged binaries, it helps to build from a clean Conda environment. A practical from-scratch path looks like this:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Create an isolated development environment with a pinned Python version.</span>
conda create <span class="nt">-y</span> <span class="nt">-p</span> cctools-dev <span class="nt">-c</span> conda-forge <span class="nt">--strict-channel-priority</span> <span class="nv">python</span><span class="o">=</span>3.11.12 conda-pack
<span class="c"># Activate the environment so the compiler, Python packages, and install prefix all point to the same place.</span>
conda activate cctools-dev
<span class="c"># Install the build toolchain and development utilities used by CCTools.</span>
conda <span class="nb">install</span> <span class="nt">-y</span> <span class="nt">-c</span> conda-forge <span class="nt">--strict-channel-priority</span> gcc_linux-64 gxx_linux-64 gdb m4 perl swig make zlib libopenssl-static openssl conda-pack packaging cloudpickle flake8 clang-format threadpoolctl
<span class="c"># Clone the repository and enter its root directory.</span>
git clone https://github.com/cooperative-computing-lab/cctools.git
<span class="nb">cd </span>cctools
<span class="c"># Discover dependencies from the active Conda environment and install back into that same environment.</span>
./configure <span class="nt">--with-base-dir</span> <span class="s2">"</span><span class="k">${</span><span class="nv">CONDA_PREFIX</span><span class="k">}</span><span class="s2">"</span> <span class="nt">--prefix</span> <span class="s2">"</span><span class="k">${</span><span class="nv">CONDA_PREFIX</span><span class="k">}</span><span class="s2">"</span>
<span class="c"># Remove stale build artifacts before compiling.</span>
make clean
<span class="c"># Build the repository in parallel, then install it.</span>
make <span class="nt">-j8</span>
make <span class="nb">install</span>
<span class="c"># Sanity check that the install path is active and the worker binary is usable.</span>
vine_worker <span class="nt">--version</span>
</code></pre></div></div> <p>This is consistent with the source build direction described in the repository and the official documentation, while being more explicit about the environment and toolchain: <a href="https://github.com/cooperative-computing-lab/cctools">CCTools on GitHub</a> and <a href="https://cctools.readthedocs.io/en/latest/taskvine/">TaskVine User’s Manual</a>.</p> <h2 id="how-to-rebuild-after-modifying-taskvine">How to rebuild after modifying TaskVine</h2> <p>Suppose we edit <code class="language-plaintext highlighter-rouge">taskvine/src/manager/vine_manager.c</code>. The simplest and safest rebuild path is still:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>make clean
make <span class="nt">-j</span>
make <span class="nb">install</span>
</code></pre></div></div> <p>That approach removes guesswork. In practice, our rule is even simpler: after changing any C source in TaskVine, go to <code class="language-plaintext highlighter-rouge">taskvine/src/</code> and run a full clean rebuild:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd </span>taskvine/src
make clean <span class="o">&amp;&amp;</span> make <span class="nt">-j</span> <span class="o">&amp;&amp;</span> make <span class="nb">install</span>
</code></pre></div></div> <p>It is slower than rebuilding only one subdirectory, but it is the least ambiguous path when we want to be sure the change has propagated through the TaskVine sources, libraries, and bindings.</p> <p>One common exception is when we only change Python files in the bindings layer and do not touch any C source. Then we usually only need:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd </span>taskvine/src
make <span class="nb">install</span>
</code></pre></div></div> <h2 id="a-first-real-patch-in-vine_managerc">A first real patch in <code class="language-plaintext highlighter-rouge">vine_manager.c</code></h2> <p>Now we reach the concrete example. The requested feature is simple: when a task is successfully dispatched, print which task was committed to which worker.</p> <p>The target location sits inside <code class="language-plaintext highlighter-rouge">send_one_task_with_cr()</code> in <code class="language-plaintext highlighter-rouge">taskvine/src/manager/vine_manager.c</code>.</p> <p>This is a good place to start because it captures one core manager step in a compact form: selecting a ready task, finding a worker that can take it, and committing that task to the worker.</p> <p>Inside that function, the branch we care about is:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">case</span> <span class="n">VINE_SUCCESS</span><span class="p">:</span> <span class="cm">/* return on successful commit. */</span> <span class="p">{</span>
    <span class="n">committable_cores</span><span class="o">--</span><span class="p">;</span>
    <span class="n">total_inuse_cores</span><span class="o">++</span><span class="p">;</span>
    <span class="n">skip_list_remove_here</span><span class="p">(</span><span class="n">cur</span><span class="p">);</span>
    <span class="k">break</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <p>This location already sits on the successful commit path, and the needed metadata is already available. By the time the commit succeeds, the runtime can identify:</p> <ul> <li>the task through <code class="language-plaintext highlighter-rouge">t-&gt;task_id</code></li> <li>the Python level label through <code class="language-plaintext highlighter-rouge">t-&gt;tag</code></li> <li>the worker host through <code class="language-plaintext highlighter-rouge">w-&gt;hostname</code></li> <li>the worker address and port through <code class="language-plaintext highlighter-rouge">w-&gt;addrport</code></li> </ul> <p>So the patch is straightforward:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">case</span> <span class="n">VINE_SUCCESS</span><span class="p">:</span> <span class="cm">/* return on successful commit. */</span> <span class="p">{</span>
    <span class="n">committable_cores</span><span class="o">--</span><span class="p">;</span>
    <span class="n">total_inuse_cores</span><span class="o">++</span><span class="p">;</span>
    <span class="n">notice</span><span class="p">(</span><span class="n">D_VINE</span><span class="p">,</span>
            <span class="s">"Committed task %d (%s) to worker %s (%s)."</span><span class="p">,</span>
            <span class="n">t</span><span class="o">-&gt;</span><span class="n">task_id</span><span class="p">,</span>
            <span class="n">t</span><span class="o">-&gt;</span><span class="n">tag</span> <span class="o">?</span> <span class="n">t</span><span class="o">-&gt;</span><span class="n">tag</span> <span class="o">:</span> <span class="s">"no-tag"</span><span class="p">,</span>
            <span class="n">w</span><span class="o">-&gt;</span><span class="n">hostname</span> <span class="o">?</span> <span class="n">w</span><span class="o">-&gt;</span><span class="n">hostname</span> <span class="o">:</span> <span class="s">"unknown-host"</span><span class="p">,</span>
            <span class="n">w</span><span class="o">-&gt;</span><span class="n">addrport</span> <span class="o">?</span> <span class="n">w</span><span class="o">-&gt;</span><span class="n">addrport</span> <span class="o">:</span> <span class="s">"unknown-addrport"</span><span class="p">);</span>
    <span class="n">skip_list_remove_here</span><span class="p">(</span><span class="n">cur</span><span class="p">);</span>
    <span class="k">break</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <p>This is a good beginner patch because it stays within the existing manager logging style, prints both machine precise and human readable identifiers, and shows how much easier the runtime becomes to navigate once we follow the Python-to-manager path.</p> <h2 id="how-to-verify-the-patch">How to verify the patch</h2> <p>After applying the change, we rebuild the source:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd </span>taskvine/src
make clean <span class="o">&amp;&amp;</span> make <span class="nt">-j</span> <span class="o">&amp;&amp;</span> make <span class="nb">install</span>
</code></pre></div></div> <p>Then we run a tiny manager:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">ndcctools.taskvine</span> <span class="k">as</span> <span class="n">vine</span>

<span class="n">m</span> <span class="o">=</span> <span class="n">vine</span><span class="p">.</span><span class="nc">Manager</span><span class="p">(</span><span class="mi">9123</span><span class="p">)</span>

<span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">3</span><span class="p">):</span>
    <span class="n">t</span> <span class="o">=</span> <span class="n">vine</span><span class="p">.</span><span class="nc">Task</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">echo task-</span><span class="si">{</span><span class="n">i</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">t</span><span class="p">.</span><span class="nf">set_tag</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">demo-</span><span class="si">{</span><span class="n">i</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">m</span><span class="p">.</span><span class="nf">submit</span><span class="p">(</span><span class="n">t</span><span class="p">)</span>

<span class="k">while</span> <span class="ow">not</span> <span class="n">m</span><span class="p">.</span><span class="nf">empty</span><span class="p">():</span>
    <span class="n">done</span> <span class="o">=</span> <span class="n">m</span><span class="p">.</span><span class="nf">wait</span><span class="p">(</span><span class="mi">5</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">done</span><span class="p">:</span>
        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">done: </span><span class="si">{</span><span class="n">done</span><span class="p">.</span><span class="nb">id</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <p>In another terminal, we start a worker:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>vine_worker localhost 9123
</code></pre></div></div> <p>If everything is wired up correctly, the manager side output should now include lines like:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>2026/03/18 16:42:26.94 vine_manager[867641]notice: Committed task 1 (demo-0) to worker condorfe.crc.nd.edu (127.0.0.1:35434).
2026/03/18 16:42:26.95 vine_manager[867641]notice: Committed task 2 (demo-1) to worker condorfe.crc.nd.edu (127.0.0.1:35434).
2026/03/18 16:42:26.95 vine_manager[867641]notice: Committed task 3 (demo-2) to worker condorfe.crc.nd.edu (127.0.0.1:35434).
</code></pre></div></div> <h2 id="the-right-mental-model-to-keep">The right mental model to keep</h2> <p>For a beginner, the most useful mental model is still the simplest one: start from the Python surface, not from the C internals. The codebase entry is <code class="language-plaintext highlighter-rouge">cctools/taskvine</code>. The manual explains the programming model, the bindings show how that model appears in Python, and the manager and worker runtime in C show how it is implemented.</p> <p>Once we follow that order, even a first patch in <code class="language-plaintext highlighter-rouge">vine_manager.c</code> stops feeling mysterious. It becomes a small change inside a system whose layers already make sense.</p>]]></content><author><name>Cooperative Computing Lab</name></author><category term="technical-articles"/><category term="taskvine-insights"/><category term="taskvine"/><category term="cctools"/><summary type="html"><![CDATA[CCTools can look intimidating at first glance. This post gives beginners a practical map of the repository, explains how TaskVine fits into the larger system, and walks through a small but real source change that prints which task was committed to which worker.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://ccl.cse.nd.edu/assets/blog/2026/taskvine-insights-a-beginners-map-to-the-cctools-codebase/TaskVine-Insights.png"/><media:content medium="image" url="https://ccl.cse.nd.edu/assets/blog/2026/taskvine-insights-a-beginners-map-to-the-cctools-codebase/TaskVine-Insights.png" xmlns:media="http://search.yahoo.com/mrss/"/></entry><entry><title type="html">CCL Participated in the 18th ND CSE Annual Poster Session</title><link href="https://ccl.cse.nd.edu/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/" rel="alternate" type="text/html" title="CCL Participated in the 18th ND CSE Annual Poster Session"/><published>2026-03-10T17:00:00+00:00</published><updated>2026-03-10T17:00:00+00:00</updated><id>https://ccl.cse.nd.edu/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session</id><content type="html" xml:base="https://ccl.cse.nd.edu/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/"><![CDATA[<p>On February 26, 2026, we participated in the 18th ND CSE Annual Poster Session. It was a good chance for the lab to share what we’ve been working on. Here’s a quick rundown of what everyone presented.</p> <p><strong>Alan Malta</strong> presented his work on large-scale HEP workflows. CMS and other LHC experiments run workflows across the Worldwide LHC Computing Grid, and the way you group and compose tasks can make a big difference in throughput and resource use. Alan built DAGFlowSim to explore the trade-offs between different workflow constructions (TaskChain vs StepChain and everything in between) and figure out which compositions work best for event throughput, CPU utilization, and network efficiency.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Alan-480.webp 480w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Alan-800.webp 800w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Alan-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Alan.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Alan Malta Poster" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p><strong>Barry Sly-Delgado</strong> talked about restructuring workflows with algebraic labels. The idea is that if you can characterize operations by properties like commutativity and associativity, you can restructure the DAG for a better topology, with more parallelism, less overhead, and better fault tolerance. He integrated this with Dask and TaskVine and showed improvements on real HEP apps like RsTriPhoton, DVS, and tBar.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Barry-480.webp 480w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Barry-800.webp 800w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Barry-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Barry.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Barry Sly-Delgado Poster" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p><strong>Colin Thomas</strong> presented Contour, a tool that traces the I/O of a running application and turns it into an “execution contract.” Scientific apps often have implicit file dependencies that are hard to see from the script alone. Contour captures what gets read and written, then uses that to generate workflows for Makeflow so you can reproduce, distribute, and scale the app more reliably.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Colin-480.webp 480w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Colin-800.webp 800w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Colin-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Colin.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Colin Thomas Poster" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p><strong>Jin Zhou</strong> presented DAGVine, which tackles a bottleneck we’ve been hitting: when you have lots of small tasks, serialization and dispatch overhead can dominate. DAGVine pre-compiles the whole dependency graph and installs it on workers, so at runtime you just send lightweight task references instead of shipping Python objects every time. In benchmarks it gets up to 82× higher throughput than DaskVine.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Jin-480.webp 480w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Jin-800.webp 800w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Jin-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Jin.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Jin Zhou Poster" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p><strong>Laxminarayana Vadnala</strong> (Lax) presented SADE-SIM, a simulation platform for validating city-scale multi-drone operations. It combines physics simulation with photorealistic visualization so you can test autonomous UAV missions in realistic urban environments. His case study ran 32 heterogeneous drones over downtown Chicago with dynamic airspace policies. Only public-safety drones could enter a restricted zone, while others were deflected at the boundary.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Lax-480.webp 480w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Lax-800.webp 800w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Lax-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Lax.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Laxminarayana Vadnala Poster" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p><strong>Ryan Hartung</strong> presented xGFabric, which connects sensors in the field with HPC facilities over 5G/6G networks for real-time digital agriculture. Sensors at UNL collect weather data, which flows over a private 5G cell to a base station, then to UCSB, and finally triggers an OpenFOAM simulation on an HPC cluster. The whole pipeline runs in near real time so you can steer the simulation based on what’s happening in the field.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Ryan-480.webp 480w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Ryan-800.webp 800w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Ryan-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Ryan.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Ryan Hartung Poster" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p><strong>Md Saiful Islam</strong> presented his work on portable notebook workflows. Data-intensive notebooks are hard to move between HPC sites because of different storage, paths, and staging logic. Saiful’s Backpack abstraction uses declarative data specs and fingerprinting to capture what’s needed and run consistently across ND CRC, Anvil, and Stampede3. He demonstrated it on HEP analysis and image convolution workloads.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Saiful-480.webp 480w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Saiful-800.webp 800w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Saiful-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Saiful.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Md Saiful Islam Poster" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p><strong>Thanh Son Phung</strong> presented StickyInvoc, which rethinks how we run LLM inference at scale. The problem is that loading a model on a GPU is expensive, and on opportunistic clusters you might get preempted and have to reload over and over. StickyInvoc lets workers keep model state across tasks so subsequent inferences reuse it. In a claim verification workflow it hit 3.6× speedup, and scaled to 186 opportunistic GPU tasks in about 13 minutes.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Thanh-480.webp 480w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Thanh-800.webp 800w,/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Thanh-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/ccl-participated-in-the-18th-nd-cse-annual-poster-session/Thanh.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Thanh Son Phung Poster" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>It was a great day to catch up with colleagues and share what we’ve been building. Thanks to everyone who stopped by our posters, and we look forward to the next one.</p>]]></content><author><name>Cooperative Computing Lab</name></author><category term="news"/><category term="news"/><category term="poster-session"/><summary type="html"><![CDATA[CCL team members presented eight research posters at the 18th ND CSE Annual Poster Session on February 26, 2026, spanning HEP workflows, DAG execution, reproducibility, digital agriculture, and LLM inference.]]></summary></entry><entry><title type="html">TaskVine Insights: DaskVine Executor for Practical Scientific Graphs</title><link href="https://ccl.cse.nd.edu/blog/2026/taskvine-insights-daskvine-executor/" rel="alternate" type="text/html" title="TaskVine Insights: DaskVine Executor for Practical Scientific Graphs"/><published>2026-02-24T17:00:00+00:00</published><updated>2026-02-24T17:00:00+00:00</updated><id>https://ccl.cse.nd.edu/blog/2026/taskvine-insights-daskvine-executor</id><content type="html" xml:base="https://ccl.cse.nd.edu/blog/2026/taskvine-insights-daskvine-executor/"><![CDATA[<div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/taskvine-insights-daskvine-executor/TaskVine-Insights-480.webp 480w,/assets/blog/2026/taskvine-insights-daskvine-executor/TaskVine-Insights-800.webp 800w,/assets/blog/2026/taskvine-insights-daskvine-executor/TaskVine-Insights-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/taskvine-insights-daskvine-executor/TaskVine-Insights.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>In the TaskVine world, Python users usually build workflows in two ways.</p> <p>The first way is the native TaskVine API surface. It includes three task surfaces: <code class="language-plaintext highlighter-rouge">Task</code>, <code class="language-plaintext highlighter-rouge">PythonTask</code>, and <code class="language-plaintext highlighter-rouge">FunctionCall</code>. You create tasks directly, attach inputs and outputs as <code class="language-plaintext highlighter-rouge">File</code> objects, and control dependencies at fine granularity. This is powerful and explicit. It is also verbose when the workflow is large. The differences among these three surfaces are discussed in detail here: <a href="https://ccl.cse.nd.edu/blog/2026/taskvine-insights-picking-the-right-task-surface-in-python/">TaskVine Insights: Picking the Right Task Surface in Python (Task, PythonTask, FunctionCall)</a>.</p> <p>The second way is a graph level API surface. You describe a whole computation graph and let the runtime map that graph to tasks, dependencies, and scheduling actions.</p> <p>DaskVine sits in this second layer. You use Dask to build the graph you already want, then hand that graph to TaskVine for execution. TaskVine remains the underlying scheduler that places work, tracks files, and moves data. DaskVine gives you that backend without forcing you to write every dependency edge manually.</p> <p>This post focuses on the second way, which is DaskVine. The goal is to help you build a graph with Dask APIs, run it with DaskVine, and tune the parameters that matter for throughput.</p> <h2 id="basic-execution-pattern">Basic execution pattern</h2> <p>The user side pattern is intentionally small. You construct Dask collections in the usual way, create a DaskVine manager, then run <code class="language-plaintext highlighter-rouge">compute</code> with <code class="language-plaintext highlighter-rouge">scheduler=m.get</code>.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">dask</span>
<span class="kn">import</span> <span class="n">dask.array</span> <span class="k">as</span> <span class="n">da</span>
<span class="kn">import</span> <span class="n">ndcctools.taskvine</span> <span class="k">as</span> <span class="n">vine</span>

<span class="n">x</span> <span class="o">=</span> <span class="n">da</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">random</span><span class="p">((</span><span class="mi">10000</span><span class="p">,</span> <span class="mi">10000</span><span class="p">),</span> <span class="n">chunks</span><span class="o">=</span><span class="p">(</span><span class="mi">1000</span><span class="p">,</span> <span class="mi">1000</span><span class="p">))</span>
<span class="n">y</span> <span class="o">=</span> <span class="p">(</span><span class="n">x</span> <span class="o">+</span> <span class="mi">1</span><span class="p">).</span><span class="nf">sum</span><span class="p">()</span>

<span class="n">m</span> <span class="o">=</span> <span class="n">vine</span><span class="p">.</span><span class="nc">DaskVine</span><span class="p">(</span><span class="n">port</span><span class="o">=</span><span class="mi">9123</span><span class="p">)</span>

<span class="k">with</span> <span class="n">dask</span><span class="p">.</span><span class="n">config</span><span class="p">.</span><span class="nf">set</span><span class="p">(</span><span class="n">scheduler</span><span class="o">=</span><span class="n">m</span><span class="p">.</span><span class="n">get</span><span class="p">):</span>
    <span class="n">result</span> <span class="o">=</span> <span class="n">y</span><span class="p">.</span><span class="nf">compute</span><span class="p">(</span>
        <span class="n">task_mode</span><span class="o">=</span><span class="sh">"</span><span class="s">function-calls</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">worker_transfers</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
        <span class="n">task_priority_mode</span><span class="o">=</span><span class="sh">"</span><span class="s">largest-input-first</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">scheduling_mode</span><span class="o">=</span><span class="sh">"</span><span class="s">files</span><span class="sh">"</span><span class="p">,</span>
    <span class="p">)</span>

<span class="nf">print</span><span class="p">(</span><span class="n">result</span><span class="p">)</span>
</code></pre></div></div> <p>You still write Dask code. DaskVine controls how that graph runs on TaskVine workers.</p> <h2 id="new-dask-and-legacy-dask-paths">New Dask and legacy Dask paths</h2> <p>TaskVine ships two DaskVine executor implementations:</p> <ul> <li><code class="language-plaintext highlighter-rouge">ndcctools.taskvine.dask_executor</code> handles the modern Dask task specification path.</li> <li><code class="language-plaintext highlighter-rouge">ndcctools.taskvine.compat.dask_executor</code> preserves legacy behavior for older Dask graph expression styles.</li> </ul> <p>In practice, users should think in terms of compatibility, not internals. Most users should import <code class="language-plaintext highlighter-rouge">vine.DaskVine</code> from <code class="language-plaintext highlighter-rouge">ndcctools.taskvine</code> and let the package choose the backend. Current package logic switches at Dask <code class="language-plaintext highlighter-rouge">2024.12.0</code>: modern versions use the new executor, older versions route to the compat executor. Most users do not need to rewrite graph logic. You keep building the same Dask workflow and pass it to <code class="language-plaintext highlighter-rouge">m.get</code>.</p> <h2 id="task-mode-recommendation-prefer-functioncall">Task mode recommendation: prefer FunctionCall</h2> <p>DaskVine supports two execution surfaces:</p> <ul> <li><code class="language-plaintext highlighter-rouge">task_mode="tasks"</code> uses <code class="language-plaintext highlighter-rouge">PythonTask</code>.</li> <li><code class="language-plaintext highlighter-rouge">task_mode="function-calls"</code> uses <code class="language-plaintext highlighter-rouge">FunctionCall</code>.</li> </ul> <p>The detailed tradeoff is covered in the earlier TaskVine Insights post on Python task surfaces: <a href="https://ccl.cse.nd.edu/blog/2026/taskvine-insights-picking-the-right-task-surface-in-python/">TaskVine Insights: Picking the Right Task Surface in Python</a>.</p> <p>One accuracy note matters here. The API signature still has <code class="language-plaintext highlighter-rouge">task_mode="tasks"</code> as the code default for compatibility. Our operational recommendation is different. For most DaskVine workloads, treat <code class="language-plaintext highlighter-rouge">function-calls</code> as your practical default and set it explicitly. It avoids repeated Python cold starts, reuses a long lived worker side library, and usually gives better throughput for the small and medium task granularity that appears in scientific graphs.</p> <h2 id="parameters-that-matter-most-in-real-runs">Parameters that matter most in real runs</h2> <p>Many options exist, but a few settings dominate behavior.</p> <h3 id="lib_resources"><code class="language-plaintext highlighter-rouge">lib_resources</code></h3> <p><code class="language-plaintext highlighter-rouge">lib_resources</code> defines resources for the long lived library process that serves <code class="language-plaintext highlighter-rouge">FunctionCall</code> tasks.</p> <p>The two most important keys are:</p> <ul> <li><code class="language-plaintext highlighter-rouge">cores</code>: total cores assigned to the library task on one worker.</li> <li><code class="language-plaintext highlighter-rouge">slots</code>: concurrent function call slots inside that library.</li> </ul> <p>A simple mental model:</p> <ul> <li><code class="language-plaintext highlighter-rouge">cores=8</code>, <code class="language-plaintext highlighter-rouge">slots=2</code> means up to 2 calls run at once and each can effectively use about 4 cores.</li> <li><code class="language-plaintext highlighter-rouge">cores=8</code>, <code class="language-plaintext highlighter-rouge">slots=8</code> means up to 8 calls run at once and each maps to about 1 core.</li> </ul> <p>In current practice, setting <code class="language-plaintext highlighter-rouge">cores == slots</code> is the safest recommendation because TaskVine function call concurrency is usually tuned as one core per call.</p> <p>If <code class="language-plaintext highlighter-rouge">memory</code> and <code class="language-plaintext highlighter-rouge">disk</code> are omitted in <code class="language-plaintext highlighter-rouge">lib_resources</code>, the library follows normal TaskVine allocation behavior for unspecified dimensions. In many deployments this effectively gives the library most of the worker budget on those dimensions, especially when few competing tasks share the worker. If you need strict memory and disk guarantees, set them explicitly.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">lib_resources</span> <span class="o">=</span> <span class="p">{</span>
    <span class="sh">"</span><span class="s">cores</span><span class="sh">"</span><span class="p">:</span> <span class="mi">8</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">slots</span><span class="sh">"</span><span class="p">:</span> <span class="mi">8</span><span class="p">,</span>
<span class="p">}</span>
</code></pre></div></div> <h3 id="worker_transfers"><code class="language-plaintext highlighter-rouge">worker_transfers</code></h3> <p><code class="language-plaintext highlighter-rouge">worker_transfers=True</code> means non target intermediate outputs are treated as worker resident temp outputs whenever possible. They stay near the workers instead of returning to the manager after every edge. Final requested targets still come back to the manager. You can also force selected intermediates to return by providing <code class="language-plaintext highlighter-rouge">checkpoint_fn</code>.</p> <p>The result is lower manager network pressure and less sequential transfer overhead on large DAGs. This should remain your default unless worker failure rates are high enough that recomputation cost dominates.</p> <h3 id="lib_modules"><code class="language-plaintext highlighter-rouge">lib_modules</code></h3> <p><code class="language-plaintext highlighter-rouge">lib_modules</code> hoists imports into the function call library preamble. That prevents repeating heavy imports in every individual call.</p> <p>A source level detail matters here. <code class="language-plaintext highlighter-rouge">lib_modules</code> is forwarded as <code class="language-plaintext highlighter-rouge">hoisting_modules</code>, and the current generator handles module objects, functions, and classes. Plain strings are not hoisted. Pass real module objects instead of names.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">numpy</span>
<span class="kn">import</span> <span class="n">pandas</span>
<span class="kn">import</span> <span class="n">scipy</span>

<span class="n">result</span> <span class="o">=</span> <span class="n">graph</span><span class="p">.</span><span class="nf">compute</span><span class="p">(</span>
    <span class="n">scheduler</span><span class="o">=</span><span class="n">m</span><span class="p">.</span><span class="n">get</span><span class="p">,</span>
    <span class="n">task_mode</span><span class="o">=</span><span class="sh">"</span><span class="s">function-calls</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">lib_modules</span><span class="o">=</span><span class="p">[</span><span class="n">numpy</span><span class="p">,</span> <span class="n">pandas</span><span class="p">,</span> <span class="n">scipy</span><span class="p">],</span>
<span class="p">)</span>
</code></pre></div></div> <p>This is especially valuable when import time is noticeable relative to task runtime.</p> <h3 id="extra_files"><code class="language-plaintext highlighter-rouge">extra_files</code></h3> <p>Many workflows need side inputs that are not represented as upstream Dask values. <code class="language-plaintext highlighter-rouge">extra_files</code> is the hook for that case.</p> <p>The expected shape is a dictionary:</p> <ul> <li>key: a TaskVine <code class="language-plaintext highlighter-rouge">File</code> object returned by <code class="language-plaintext highlighter-rouge">m.declare_file</code>, <code class="language-plaintext highlighter-rouge">m.declare_url</code>, or other <code class="language-plaintext highlighter-rouge">declare_*</code> calls</li> <li>value: the remote filename string inside the task sandbox</li> </ul> <p>Implementation wise, DaskVine forwards this dictionary into every generated graph task, then calls <code class="language-plaintext highlighter-rouge">add_input(file, remote_name)</code> for each pair. This happens in both <code class="language-plaintext highlighter-rouge">task_mode="tasks"</code> and <code class="language-plaintext highlighter-rouge">task_mode="function-calls"</code>.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">weights</span> <span class="o">=</span> <span class="n">m</span><span class="p">.</span><span class="nf">declare_file</span><span class="p">(</span><span class="sh">"</span><span class="s">model_weights.bin</span><span class="sh">"</span><span class="p">)</span>
<span class="n">config</span> <span class="o">=</span> <span class="n">m</span><span class="p">.</span><span class="nf">declare_file</span><span class="p">(</span><span class="sh">"</span><span class="s">run_config.json</span><span class="sh">"</span><span class="p">)</span>

<span class="n">result</span> <span class="o">=</span> <span class="n">graph</span><span class="p">.</span><span class="nf">compute</span><span class="p">(</span>
    <span class="n">scheduler</span><span class="o">=</span><span class="n">m</span><span class="p">.</span><span class="n">get</span><span class="p">,</span>
    <span class="n">extra_files</span><span class="o">=</span><span class="p">{</span>
        <span class="n">weights</span><span class="p">:</span> <span class="sh">"</span><span class="s">model_weights.bin</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">config</span><span class="p">:</span> <span class="sh">"</span><span class="s">run_config.json</span><span class="sh">"</span><span class="p">,</span>
    <span class="p">},</span>
    <span class="n">task_mode</span><span class="o">=</span><span class="sh">"</span><span class="s">function-calls</span><span class="sh">"</span><span class="p">,</span>
<span class="p">)</span>
</code></pre></div></div> <p>This pattern is common for model parameters, static lookup tables, and domain specific config bundles.</p> <h3 id="task_priority_mode"><code class="language-plaintext highlighter-rouge">task_priority_mode</code></h3> <p>TaskVine uses a priority queue. <code class="language-plaintext highlighter-rouge">task_priority_mode</code> decides how task priority is computed before dispatch.</p> <p>Common choices:</p> <ul> <li><code class="language-plaintext highlighter-rouge">largest-input-first</code> (default): prioritize tasks with larger ready inputs, often better for storage pressure and pruning.</li> <li><code class="language-plaintext highlighter-rouge">FIFO</code>: older submitted tasks first.</li> <li><code class="language-plaintext highlighter-rouge">LIFO</code>: newer submitted tasks first.</li> <li><code class="language-plaintext highlighter-rouge">depth-first</code>: go deeper in the graph quickly.</li> <li><code class="language-plaintext highlighter-rouge">breadth-first</code>: spread progress across branches.</li> <li><code class="language-plaintext highlighter-rouge">random</code>: randomized ordering.</li> <li><code class="language-plaintext highlighter-rouge">longest-category-first</code> and <code class="language-plaintext highlighter-rouge">shortest-category-first</code>: adaptive policies based on observed category runtime.</li> </ul> <h3 id="scheduling_mode"><code class="language-plaintext highlighter-rouge">scheduling_mode</code></h3> <p>After a task is selected, <code class="language-plaintext highlighter-rouge">scheduling_mode</code> chooses the worker.</p> <ul> <li><code class="language-plaintext highlighter-rouge">files</code> (default): prefer workers that already hold more required input bytes for that task.</li> <li><code class="language-plaintext highlighter-rouge">time</code>: prefer workers that have completed tasks faster.</li> <li><code class="language-plaintext highlighter-rouge">rand</code>: random available worker.</li> <li><code class="language-plaintext highlighter-rouge">worst</code>: prefer worker with the most currently unused resources.</li> <li><code class="language-plaintext highlighter-rouge">disk</code>: prefer worker with the most unused disk.</li> </ul> <p><code class="language-plaintext highlighter-rouge">files</code> is usually the right default because it improves data locality and reduces peer transfer volume. These policy names map to manager level scheduling choices in the TaskVine runtime.</p> <h2 id="a-practical-tuned-template">A practical tuned template</h2> <p>The following template is a good starting point for many scientific Dask workloads:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">with</span> <span class="n">dask</span><span class="p">.</span><span class="n">config</span><span class="p">.</span><span class="nf">set</span><span class="p">(</span><span class="n">scheduler</span><span class="o">=</span><span class="n">m</span><span class="p">.</span><span class="n">get</span><span class="p">):</span>
    <span class="n">result</span> <span class="o">=</span> <span class="n">graph</span><span class="p">.</span><span class="nf">compute</span><span class="p">(</span>
        <span class="n">task_mode</span><span class="o">=</span><span class="sh">"</span><span class="s">function-calls</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">worker_transfers</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
        <span class="n">lib_resources</span><span class="o">=</span><span class="p">{</span><span class="sh">"</span><span class="s">cores</span><span class="sh">"</span><span class="p">:</span> <span class="mi">8</span><span class="p">,</span> <span class="sh">"</span><span class="s">slots</span><span class="sh">"</span><span class="p">:</span> <span class="mi">8</span><span class="p">},</span>
        <span class="n">lib_modules</span><span class="o">=</span><span class="p">[</span><span class="n">numpy</span><span class="p">,</span> <span class="n">pandas</span><span class="p">],</span>
        <span class="n">extra_files</span><span class="o">=</span><span class="p">{</span><span class="n">static_file</span><span class="p">:</span> <span class="sh">"</span><span class="s">static_file.dat</span><span class="sh">"</span><span class="p">},</span>
        <span class="n">task_priority_mode</span><span class="o">=</span><span class="sh">"</span><span class="s">largest-input-first</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">scheduling_mode</span><span class="o">=</span><span class="sh">"</span><span class="s">files</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">retries</span><span class="o">=</span><span class="mi">5</span><span class="p">,</span>
    <span class="p">)</span>
</code></pre></div></div> <p>From there, tune one parameter at a time and inspect throughput and failure behavior.</p> <h2 id="where-to-look-for-the-full-option-set">Where to look for the full option set</h2> <p>DaskVine exposes many additional options such as resource policy, submission backpressure, wrappers, pruning behavior, reconstruction controls, and progress display knobs. The TaskVine manual documents these options and their semantics in detail:</p> <p><a href="https://cctools.readthedocs.io/en/latest/taskvine/">https://cctools.readthedocs.io/en/latest/taskvine/</a></p> <h2 id="what-comes-next-dagvine">What comes next: DAGVine</h2> <p>DaskVine gives a practical path to run Dask graphs on TaskVine today. We are also developing DAGVine as a faster graph execution path. DAGVine targets both Dask built graphs and DAGVine native graphs, with a strong focus on reducing serialization overhead and improving overhead limited throughput.</p> <p>If your workflows are dominated by orchestration cost rather than kernel compute time, this direction is especially relevant.</p>]]></content><author><name>Cooperative Computing Lab</name></author><category term="technical-articles"/><category term="taskvine"/><summary type="html"><![CDATA[In the TaskVine world, workflows can be built task by task or as full graphs. This post focuses on DaskVine and shows how to run Dask graphs on TaskVine with practical guidance on execution modes, data movement, and scheduling options.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://ccl.cse.nd.edu/assets/blog/2026/taskvine-insights-daskvine-executor/TaskVine-Insights.png"/><media:content medium="image" url="https://ccl.cse.nd.edu/assets/blog/2026/taskvine-insights-daskvine-executor/TaskVine-Insights.png" xmlns:media="http://search.yahoo.com/mrss/"/></entry><entry><title type="html">Pledge Development Updates</title><link href="https://ccl.cse.nd.edu/blog/2026/pledge-development-updates/" rel="alternate" type="text/html" title="Pledge Development Updates"/><published>2026-02-17T17:00:00+00:00</published><updated>2026-02-17T17:00:00+00:00</updated><id>https://ccl.cse.nd.edu/blog/2026/pledge-development-updates</id><content type="html" xml:base="https://ccl.cse.nd.edu/blog/2026/pledge-development-updates/"><![CDATA[<div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/pledge-development-updates/pledge-figure-480.webp 480w,/assets/blog/2026/pledge-development-updates/pledge-figure-800.webp 800w,/assets/blog/2026/pledge-development-updates/pledge-figure-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/pledge-development-updates/pledge-figure.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Many HPC applications are composed of shell scripts which invoke one or more executables from a scientific software package. These applications can be distributed by sending the scripts out through a batch submission system, or they may invoke the batch system within the script along with configurations to determine scale and resource allocations.</p> <p>There are a number of reasons one may wish to convert the application to use a higher-level workflow system which constructs a proper DAG. There could be missed concurrency opportunities, data movement bottlenecks, or the desire for portability to run on another system.</p> <p>It can be a significant challenge to port an application to a task-based workflow system since the user must understand the dependencies of each application component. Executables often read and write to implicit locations and files which are not apparent from reading the script itself. The environment can influence the behavior of an application, causing it to function differently if run elsewhere or by another user.</p> <p>In the past months the Pledge project has been under development with focus emphasized on observing or tracing a running application and creating a functional contract. The contract is a legible and comprehensive description of the application’s I/O behavior. The user can read the contract to understand the implicit or unexpected behavior of their software.</p> <p>The contract can also be further processed into a Makeflow application using the JX Expression language. With the application written in this format the user can make minimal edits to scale the application and run a distributed workflow using the TaskVine executor.</p> <p>Check out the latest additions in the Pledge repository: https://github.com/cooperative-computing-lab/pledge</p>]]></content><author><name>Cooperative Computing Lab</name></author><category term="news"/><category term="pledge"/><summary type="html"><![CDATA[Our Pledge project continues to evolve, uncovering hidden dependencies in HPC scripts and making scalable workflows increasingly within reach.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://ccl.cse.nd.edu/assets/blog/2026/pledge-development-updates/pledge-figure.png"/><media:content medium="image" url="https://ccl.cse.nd.edu/assets/blog/2026/pledge-development-updates/pledge-figure.png" xmlns:media="http://search.yahoo.com/mrss/"/></entry><entry><title type="html">TaskVine Insights: Picking the Right Task Surface in Python (Task, PythonTask, FunctionCall)</title><link href="https://ccl.cse.nd.edu/blog/2026/taskvine-insights-picking-the-right-task-surface-in-python/" rel="alternate" type="text/html" title="TaskVine Insights: Picking the Right Task Surface in Python (Task, PythonTask, FunctionCall)"/><published>2026-02-10T17:00:00+00:00</published><updated>2026-02-10T17:00:00+00:00</updated><id>https://ccl.cse.nd.edu/blog/2026/taskvine-insights-picking-the-right-task-surface-in-python</id><content type="html" xml:base="https://ccl.cse.nd.edu/blog/2026/taskvine-insights-picking-the-right-task-surface-in-python/"><![CDATA[<div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/taskvine-insights-picking-the-right-task-surface-in-python/TaskVine-Insights-480.webp 480w,/assets/blog/2026/taskvine-insights-picking-the-right-task-surface-in-python/TaskVine-Insights-800.webp 800w,/assets/blog/2026/taskvine-insights-picking-the-right-task-surface-in-python/TaskVine-Insights-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/taskvine-insights-picking-the-right-task-surface-in-python/TaskVine-Insights.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Two API layers exist in the TaskVine Python interface. The first layer is direct task creation. This layer includes <code class="language-plaintext highlighter-rouge">Task</code>, <code class="language-plaintext highlighter-rouge">PythonTask</code>, and <code class="language-plaintext highlighter-rouge">FunctionCall</code>. The second layer is graph oriented workflow APIs such as DAGVine and DaskVine. Those graph APIs build on top of the same primitives. Another post will cover DAGVine and DaskVine.</p> <p>This post stays in the first layer. The goal is simple. It clarifies how each task surface expresses dependencies with <code class="language-plaintext highlighter-rouge">File</code> objects. It also explains why a long lived library exists, and what it changes for Python workloads.</p> <p>TaskVine uses the same dependency model for all three. The manager schedules tasks, workers execute tasks, and data dependencies live in explicit <code class="language-plaintext highlighter-rouge">File</code> objects. The three surfaces differ in how they create those file edges and what they assume about Python execution.</p> <p><code class="language-plaintext highlighter-rouge">Task</code> is the most explicit surface. You provide a command line and you declare inputs and outputs yourself. That makes dependencies visible in your code. It also makes debugging straightforward because the task boundary matches a real process boundary.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">ndcctools.taskvine</span> <span class="k">as</span> <span class="n">vine</span>

<span class="n">m</span> <span class="o">=</span> <span class="n">vine</span><span class="p">.</span><span class="nc">Manager</span><span class="p">()</span>

<span class="n">inp</span> <span class="o">=</span> <span class="n">m</span><span class="p">.</span><span class="nf">declare_file</span><span class="p">(</span><span class="sh">"</span><span class="s">data/input.txt</span><span class="sh">"</span><span class="p">)</span>
<span class="n">out</span> <span class="o">=</span> <span class="n">m</span><span class="p">.</span><span class="nf">declare_file</span><span class="p">(</span><span class="sh">"</span><span class="s">results/output.txt</span><span class="sh">"</span><span class="p">)</span>

<span class="n">t</span> <span class="o">=</span> <span class="n">vine</span><span class="p">.</span><span class="nc">Task</span><span class="p">(</span><span class="sh">"</span><span class="s">python3 run_pipeline.py input.txt output.txt</span><span class="sh">"</span><span class="p">)</span>
<span class="n">t</span><span class="p">.</span><span class="nf">add_input</span><span class="p">(</span><span class="n">inp</span><span class="p">,</span> <span class="sh">"</span><span class="s">input.txt</span><span class="sh">"</span><span class="p">)</span>
<span class="n">t</span><span class="p">.</span><span class="nf">add_output</span><span class="p">(</span><span class="n">out</span><span class="p">,</span> <span class="sh">"</span><span class="s">output.txt</span><span class="sh">"</span><span class="p">)</span>

<span class="n">m</span><span class="p">.</span><span class="nf">submit</span><span class="p">(</span><span class="n">t</span><span class="p">)</span>

<span class="k">while</span> <span class="ow">not</span> <span class="n">m</span><span class="p">.</span><span class="nf">empty</span><span class="p">():</span>
    <span class="n">done</span> <span class="o">=</span> <span class="n">m</span><span class="p">.</span><span class="nf">wait</span><span class="p">(</span><span class="mi">5</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">done</span> <span class="ow">and</span> <span class="n">done</span><span class="p">.</span><span class="nf">successful</span><span class="p">():</span>
        <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">output at:</span><span class="sh">"</span><span class="p">,</span> <span class="n">out</span><span class="p">.</span><span class="nf">source</span><span class="p">())</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">PythonTask</code> builds on <code class="language-plaintext highlighter-rouge">Task</code>, but it changes how you describe work. You provide a Python callable and arguments. TaskVine turns that into a command that runs Python on the worker. At submission time the manager materializes the pieces needed to run the function. It creates a small wrapper script, it stores the function body in a manager side buffer file, it serializes arguments to a file in the manager staging directory, and it creates an output file to carry the return value. TaskVine then attaches those <code class="language-plaintext highlighter-rouge">File</code> objects as inputs and outputs on the underlying task.</p> <p>That design has two direct consequences for dependency handling. First, the function code and arguments still travel as files and they become part of the task dependency set, even if you never wrote <code class="language-plaintext highlighter-rouge">add_input</code> for them. Second, the return value is still a <code class="language-plaintext highlighter-rouge">File</code>, even when you read it back as a Python object through <code class="language-plaintext highlighter-rouge">.output</code>.</p> <p>This surface is convenient for glue logic. It is also sensitive to Python cold start overhead. Each <code class="language-plaintext highlighter-rouge">PythonTask</code> runs in its own Python process on the worker, so interpreter startup and repeated imports can dominate when tasks are small. The worker environment must match your function. Imports must exist on workers, and version skew can show up as ordinary Python exceptions.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">ndcctools.taskvine</span> <span class="k">as</span> <span class="n">vine</span>

<span class="n">m</span> <span class="o">=</span> <span class="n">vine</span><span class="p">.</span><span class="nc">Manager</span><span class="p">()</span>

<span class="k">def</span> <span class="nf">score</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">y</span><span class="p">,</span> <span class="n">scale</span><span class="o">=</span><span class="mf">1.0</span><span class="p">):</span>
    <span class="k">return</span> <span class="n">scale</span> <span class="o">*</span> <span class="p">(</span><span class="n">x</span> <span class="o">*</span> <span class="n">x</span> <span class="o">+</span> <span class="n">y</span> <span class="o">*</span> <span class="n">y</span><span class="p">)</span>

<span class="n">t</span> <span class="o">=</span> <span class="n">vine</span><span class="p">.</span><span class="nc">PythonTask</span><span class="p">(</span><span class="n">score</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="n">scale</span><span class="o">=</span><span class="mf">0.5</span><span class="p">)</span>
<span class="n">m</span><span class="p">.</span><span class="nf">submit</span><span class="p">(</span><span class="n">t</span><span class="p">)</span>

<span class="k">while</span> <span class="ow">not</span> <span class="n">m</span><span class="p">.</span><span class="nf">empty</span><span class="p">():</span>
    <span class="n">done</span> <span class="o">=</span> <span class="n">m</span><span class="p">.</span><span class="nf">wait</span><span class="p">(</span><span class="mi">5</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">done</span> <span class="ow">and</span> <span class="n">done</span><span class="p">.</span><span class="nf">successful</span><span class="p">():</span>
        <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">score =</span><span class="sh">"</span><span class="p">,</span> <span class="n">done</span><span class="p">.</span><span class="n">output</span><span class="p">)</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">PythonTask</code> exposes one additional handle that matters for dependencies. The <code class="language-plaintext highlighter-rouge">.output_file</code> property gives you the <code class="language-plaintext highlighter-rouge">File</code> object that carries the result. Downstream tasks can depend on that file edge. This is essential when temp output is enabled because <code class="language-plaintext highlighter-rouge">.output</code> is not available on the manager. The same temp output rule also applies to <code class="language-plaintext highlighter-rouge">FunctionCall</code>.</p> <p><code class="language-plaintext highlighter-rouge">FunctionCall</code> also builds on <code class="language-plaintext highlighter-rouge">Task</code>, but it targets a different execution model. It is designed for the case where Python cold start overhead dominates. A workflow can have millions of tiny calls where each call does little work. Starting a fresh Python process per call can cost more than the function itself.</p> <p>TaskVine solves this with a long lived library. The library runs as a persistent worker side process. You install it through the manager. The manager can generate the library from Python functions and it can hoist imports into the library preamble. The library also has an execution mode. It can run calls directly in process, or it can fork for isolation. A <code class="language-plaintext highlighter-rouge">FunctionCall</code> then sends only a function name plus arguments to the library. It still serializes the call arguments, but it does not need to ship the function body each time. Imports and global variables can live inside the library and stay warm across calls.</p> <p>The example below uses the manager helper that creates a library from local Python functions and installs it on workers. The <code class="language-plaintext highlighter-rouge">FunctionCall</code> tasks then reference the library by name.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">ndcctools.taskvine</span> <span class="k">as</span> <span class="n">vine</span>

<span class="n">m</span> <span class="o">=</span> <span class="n">vine</span><span class="p">.</span><span class="nc">Manager</span><span class="p">()</span>

<span class="k">def</span> <span class="nf">preprocess</span><span class="p">(</span><span class="n">inp</span><span class="p">,</span> <span class="n">out</span><span class="p">):</span>
    <span class="nf">return </span><span class="p">(</span><span class="n">inp</span><span class="p">,</span> <span class="n">out</span><span class="p">)</span>

<span class="k">def</span> <span class="nf">align</span><span class="p">(</span><span class="n">inp</span><span class="p">,</span> <span class="n">out</span><span class="p">,</span> <span class="n">threads</span><span class="o">=</span><span class="mi">1</span><span class="p">):</span>
    <span class="nf">return </span><span class="p">(</span><span class="n">inp</span><span class="p">,</span> <span class="n">out</span><span class="p">,</span> <span class="n">threads</span><span class="p">)</span>

<span class="n">lib</span> <span class="o">=</span> <span class="n">m</span><span class="p">.</span><span class="nf">create_library_from_functions</span><span class="p">(</span>
    <span class="sh">"</span><span class="s">mylib</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">preprocess</span><span class="p">,</span>
    <span class="n">align</span><span class="p">,</span>
    <span class="n">hoisting_modules</span><span class="o">=</span><span class="p">[</span><span class="sh">"</span><span class="s">os</span><span class="sh">"</span><span class="p">],</span>
    <span class="n">exec_mode</span><span class="o">=</span><span class="sh">"</span><span class="s">fork</span><span class="sh">"</span><span class="p">,</span>
<span class="p">)</span>
<span class="n">m</span><span class="p">.</span><span class="nf">install_library</span><span class="p">(</span><span class="n">lib</span><span class="p">)</span>

<span class="n">t1</span> <span class="o">=</span> <span class="n">vine</span><span class="p">.</span><span class="nc">FunctionCall</span><span class="p">(</span><span class="sh">"</span><span class="s">mylib</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">preprocess</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">a.fastq</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">a.clean.fastq</span><span class="sh">"</span><span class="p">)</span>
<span class="n">t2</span> <span class="o">=</span> <span class="n">vine</span><span class="p">.</span><span class="nc">FunctionCall</span><span class="p">(</span><span class="sh">"</span><span class="s">mylib</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">align</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">a.clean.fastq</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">a.bam</span><span class="sh">"</span><span class="p">,</span> <span class="n">threads</span><span class="o">=</span><span class="mi">8</span><span class="p">)</span>
<span class="n">m</span><span class="p">.</span><span class="nf">submit</span><span class="p">(</span><span class="n">t1</span><span class="p">)</span>
<span class="n">m</span><span class="p">.</span><span class="nf">submit</span><span class="p">(</span><span class="n">t2</span><span class="p">)</span>

<span class="k">while</span> <span class="ow">not</span> <span class="n">m</span><span class="p">.</span><span class="nf">empty</span><span class="p">():</span>
    <span class="n">done</span> <span class="o">=</span> <span class="n">m</span><span class="p">.</span><span class="nf">wait</span><span class="p">(</span><span class="mi">5</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">done</span> <span class="ow">and</span> <span class="n">done</span><span class="p">.</span><span class="nf">successful</span><span class="p">():</span>
        <span class="nf">print</span><span class="p">(</span><span class="n">done</span><span class="p">.</span><span class="n">output</span><span class="p">)</span>
</code></pre></div></div> <p>This surface has one extra dependency hook that matters. A <code class="language-plaintext highlighter-rouge">FunctionCall</code> requires a library name and the manager checks it at submission time. The call arguments go into an input file. The result goes into an output file. The <code class="language-plaintext highlighter-rouge">.output</code> property then decodes the result back into a Python value. Temp output works the same way as in <code class="language-plaintext highlighter-rouge">PythonTask</code>. The downstream dependency should use the <code class="language-plaintext highlighter-rouge">File</code> edge, not the Python return value.</p> <p>Mode choice usually follows the boundary you want. <code class="language-plaintext highlighter-rouge">Task</code> fits external commands and explicit file edges. <code class="language-plaintext highlighter-rouge">PythonTask</code> is the simplest to get running for Python glue code and structured returns, but <code class="language-plaintext highlighter-rouge">FunctionCall</code> often wins on runtime performance once Python cold start overhead matters. <code class="language-plaintext highlighter-rouge">FunctionCall</code> asks you to manage a library, but it keeps Python warm, it centralizes imports and global state, and it avoids paying interpreter startup costs on every call.</p>]]></content><author><name>Cooperative Computing Lab</name></author><category term="technical-articles"/><category term="taskvine"/><category term="python"/><category term="api"/><summary type="html"><![CDATA[In the TaskVine API, Task, PythonTask, and FunctionCall are three ways to describe work. This post explains how each one expresses dependencies with File objects, and why libraries help avoid Python cold starts and reuse imports and global state.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://ccl.cse.nd.edu/assets/blog/2026/taskvine-insights-picking-the-right-task-surface-in-python/TaskVine-Insights.png"/><media:content medium="image" url="https://ccl.cse.nd.edu/assets/blog/2026/taskvine-insights-picking-the-right-task-surface-in-python/TaskVine-Insights.png" xmlns:media="http://search.yahoo.com/mrss/"/></entry><entry><title type="html">Floability Winter Research Retreat 2026</title><link href="https://ccl.cse.nd.edu/blog/2026/floability-winter-research-retreat-2026/" rel="alternate" type="text/html" title="Floability Winter Research Retreat 2026"/><published>2026-02-03T17:00:00+00:00</published><updated>2026-02-03T17:00:00+00:00</updated><id>https://ccl.cse.nd.edu/blog/2026/floability-winter-research-retreat-2026</id><content type="html" xml:base="https://ccl.cse.nd.edu/blog/2026/floability-winter-research-retreat-2026/"><![CDATA[<p>In January, we held our annual research retreat for the Floability project on a snowy January day at Notre Dame. Thanks to our colleagues from University of Illinois, DePaul University, and University of Missouri who made the trek out!</p> <p>The Floability Project is an NSF funded research project to enable the rapid and portable deployment of notebooks expressing complex scientific workflows across a wide range of cyberinfrastructure. The key technical challenge is that workflows are incomplete: the code by itself cannot be moved between facilities without accurately capturing the software dependencies, required datasets, and capabilities of the underlying cluster hardware.  Floability builds on a variety of open source technologies, including Jupyter to provide interactive notebook environments, Sciunit to discover software and data dependencies, TaskVine to express and execute workflows, and Conda Forge to provision and deploy software environments.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/floability-winter-research-retreat-2026/group-photo-480.webp 480w,/assets/blog/2026/floability-winter-research-retreat-2026/group-photo-800.webp 800w,/assets/blog/2026/floability-winter-research-retreat-2026/group-photo-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/floability-winter-research-retreat-2026/group-photo.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div>]]></content><author><name>Cooperative Computing Lab</name></author><category term="news"/><category term="news, floability, taskvine"/><summary type="html"><![CDATA[Snow kept falling as we hosted our Floability winter retreat at Notre Dame, catching up with friends from UIUC, DePaul, and Missouri and hashing out some tough workflow problems together.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://ccl.cse.nd.edu/assets/blog/2026/floability-winter-research-retreat-2026/group-photo.png"/><media:content medium="image" url="https://ccl.cse.nd.edu/assets/blog/2026/floability-winter-research-retreat-2026/group-photo.png" xmlns:media="http://search.yahoo.com/mrss/"/></entry><entry><title type="html">TaskVine Insights - Intermediate Data Model</title><link href="https://ccl.cse.nd.edu/blog/2026/taskvine-insights-intermediate-data-model/" rel="alternate" type="text/html" title="TaskVine Insights - Intermediate Data Model"/><published>2026-01-27T17:00:00+00:00</published><updated>2026-01-27T17:00:00+00:00</updated><id>https://ccl.cse.nd.edu/blog/2026/taskvine-insights-intermediate-data-model</id><content type="html" xml:base="https://ccl.cse.nd.edu/blog/2026/taskvine-insights-intermediate-data-model/"><![CDATA[<div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/taskvine-insights-intermediate-data-model/TaskVine-Insights-480.webp 480w,/assets/blog/2026/taskvine-insights-intermediate-data-model/TaskVine-Insights-800.webp 800w,/assets/blog/2026/taskvine-insights-intermediate-data-model/TaskVine-Insights-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/taskvine-insights-intermediate-data-model/TaskVine-Insights.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <h2 id="why-intermediate-data-becomes-the-bottleneck">Why intermediate data becomes the bottleneck</h2> <p>If you’ve run large DAG workflows on a cluster, you’ve probably seen this pattern: CPU looks fine, workers are alive, but the makespan still crawls. A common reason is <strong>intermediate data</strong>—the “in between” outputs that feed the next stages of the graph.</p> <p>In a small workflow, it’s totally reasonable to ship every task’s output back to the manager (or to some central location), then redistribute it later. In a big DAG, that approach turns the manager into a traffic jam:</p> <ul> <li>every edge in the DAG starts to look like “worker -&gt; manager -&gt; worker”</li> <li>the manager’s network and disk become shared bottlenecks</li> <li>recovery/retries multiply the same transfers</li> </ul> <p>TaskVine’s intermediate data model exists mostly to avoid that outcome.</p> <h2 id="the-default-model-outputs-come-back-to-the-manager">The default model: outputs come back to the manager</h2> <p>At a high level, TaskVine’s manager schedules tasks, and workers execute tasks. Files are tracked as TaskVine <code class="language-plaintext highlighter-rouge">File</code> objects in the manager-side API, and tasks declare which files they consume/produce.</p> <p>In the default mode, when a task produces output files, those outputs are treated as something the manager can retrieve and store locally. That’s the safe, simple behavior: once the manager has the file, it can hand it to any future task without caring which worker originally produced it.</p> <p>The downside is obvious in large DAGs: if many tasks are producing large intermediates, you end up “pinballing” data through the manager.</p> <h2 id="the-intermediate-model-declare-outputs-as-temp-keep-them-on-workers">The intermediate model: declare outputs as temp, keep them on workers</h2> <p>TaskVine has a second, more DAG-friendly model for intermediate results: declare the intermediate as a <strong>temp file</strong>.</p> <p>The key idea is:</p> <ol> <li>The producing task writes the output as usual on its worker.</li> <li>Instead of immediately bringing that file back to the manager, the file is kept on the <strong>worker’s local disk / cache</strong>.</li> <li>The worker notifies the manager that the file exists (think: “this cached name is now ready on worker X”).</li> <li>When some downstream task needs that file, TaskVine can arrange a <strong>worker-to-worker transfer</strong> to wherever the consumer runs.</li> </ol> <p>So the DAG edge becomes “worker → worker” most of the time, and the manager stays mostly control-plane.</p> <p>This is not magic: you’re still moving bytes. The win is that you avoid forcing <em>all</em> intermediate bytes to traverse one central node.</p> <h2 id="using-temp-outputs-in-the-python-api">Using temp outputs in the Python API</h2> <p>There are two common ways you’ll use this from Python:</p> <h3 id="1-create-a-temp-file-explicitly-with-declare_temp">1) Create a temp file explicitly with <code class="language-plaintext highlighter-rouge">declare_temp()</code></h3> <p>This is the most direct style: you create a temp <code class="language-plaintext highlighter-rouge">File</code>, attach it as an output of one task, then attach it as an input of another task.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">ndcctools.taskvine</span> <span class="k">as</span> <span class="n">vine</span>

<span class="n">m</span> <span class="o">=</span> <span class="n">vine</span><span class="p">.</span><span class="nc">Manager</span><span class="p">()</span>

<span class="c1"># A temp file represents an intermediate result.
</span><span class="n">F</span> <span class="o">=</span> <span class="n">m</span><span class="p">.</span><span class="nf">declare_temp</span><span class="p">()</span>

<span class="c1"># Producer writes F on its worker.
</span><span class="n">tA</span> <span class="o">=</span> <span class="n">vine</span><span class="p">.</span><span class="nc">Task</span><span class="p">(</span><span class="sh">"</span><span class="s">python3 produce.py out.dat</span><span class="sh">"</span><span class="p">)</span>
<span class="n">tA</span><span class="p">.</span><span class="nf">add_output</span><span class="p">(</span><span class="n">F</span><span class="p">,</span> <span class="sh">"</span><span class="s">out.dat</span><span class="sh">"</span><span class="p">)</span>
<span class="n">m</span><span class="p">.</span><span class="nf">submit</span><span class="p">(</span><span class="n">tA</span><span class="p">)</span>

<span class="c1"># Consumer reads F (TaskVine will fetch it from a worker that has it).
</span><span class="n">tB</span> <span class="o">=</span> <span class="n">vine</span><span class="p">.</span><span class="nc">Task</span><span class="p">(</span><span class="sh">"</span><span class="s">python3 consume.py in.dat</span><span class="sh">"</span><span class="p">)</span>
<span class="n">tB</span><span class="p">.</span><span class="nf">add_input</span><span class="p">(</span><span class="n">F</span><span class="p">,</span> <span class="sh">"</span><span class="s">in.dat</span><span class="sh">"</span><span class="p">)</span>
<span class="n">m</span><span class="p">.</span><span class="nf">submit</span><span class="p">(</span><span class="n">tB</span><span class="p">)</span>

<span class="k">while</span> <span class="ow">not</span> <span class="n">m</span><span class="p">.</span><span class="nf">empty</span><span class="p">():</span>
    <span class="n">t</span> <span class="o">=</span> <span class="n">m</span><span class="p">.</span><span class="nf">wait</span><span class="p">(</span><span class="mi">5</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">t</span><span class="p">:</span>
        <span class="k">pass</span>
</code></pre></div></div> <p>This is the mental model you want for DAGs: temp files are named intermediates that can stay out on the workers.</p> <h3 id="2-for-pythontask-and-functioncall-tasks-enable_temp_output">2) For PythonTask and FunctionCall tasks: <code class="language-plaintext highlighter-rouge">enable_temp_output()</code></h3> <p>If you’re using <code class="language-plaintext highlighter-rouge">PythonTask</code> or <code class="language-plaintext highlighter-rouge">FunctionCall</code> and you want the result file to stay at workers, enable temp output on that task. When temp output is enabled, the task’s Python-level output is not available locally (because the output wasn’t brought back); instead, you pass around the <code class="language-plaintext highlighter-rouge">File</code> object and let downstream tasks load it.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">cloudpickle</span>
<span class="kn">import</span> <span class="n">ndcctools.taskvine</span> <span class="k">as</span> <span class="n">vine</span>

<span class="n">m</span> <span class="o">=</span> <span class="n">vine</span><span class="p">.</span><span class="nc">Manager</span><span class="p">()</span>

<span class="k">def</span> <span class="nf">make_big</span><span class="p">():</span>
    <span class="c1"># pretend this is a big intermediate
</span>    <span class="k">return</span> <span class="p">{</span><span class="sh">"</span><span class="s">x</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">...</span><span class="sh">"</span> <span class="o">*</span> <span class="mi">10_000_000</span><span class="p">}</span>

<span class="k">def</span> <span class="nf">use_big</span><span class="p">(</span><span class="n">filename</span><span class="p">):</span>
    <span class="k">with</span> <span class="nf">open</span><span class="p">(</span><span class="n">filename</span><span class="p">,</span> <span class="sh">"</span><span class="s">rb</span><span class="sh">"</span><span class="p">)</span> <span class="k">as</span> <span class="n">f</span><span class="p">:</span>
        <span class="n">data</span> <span class="o">=</span> <span class="n">cloudpickle</span><span class="p">.</span><span class="nf">load</span><span class="p">(</span><span class="n">f</span><span class="p">)</span>
    <span class="k">return</span> <span class="nf">len</span><span class="p">(</span><span class="n">data</span><span class="p">[</span><span class="sh">"</span><span class="s">x</span><span class="sh">"</span><span class="p">])</span>

<span class="n">tA</span> <span class="o">=</span> <span class="n">vine</span><span class="p">.</span><span class="nc">PythonTask</span><span class="p">(</span><span class="n">make_big</span><span class="p">)</span>
<span class="n">tA</span><span class="p">.</span><span class="nf">enable_temp_output</span><span class="p">()</span>          <span class="c1"># keep output on worker
</span><span class="n">m</span><span class="p">.</span><span class="nf">submit</span><span class="p">(</span><span class="n">tA</span><span class="p">)</span>

<span class="c1"># IMPORTANT: downstream tasks consume the output via the File object.
</span><span class="n">tB</span> <span class="o">=</span> <span class="n">vine</span><span class="p">.</span><span class="nc">PythonTask</span><span class="p">(</span><span class="n">use_big</span><span class="p">,</span> <span class="sh">"</span><span class="s">in.p</span><span class="sh">"</span><span class="p">)</span>
<span class="n">tB</span><span class="p">.</span><span class="nf">add_input</span><span class="p">(</span><span class="n">tA</span><span class="p">.</span><span class="n">output_file</span><span class="p">,</span> <span class="sh">"</span><span class="s">in.p</span><span class="sh">"</span><span class="p">)</span>
<span class="n">m</span><span class="p">.</span><span class="nf">submit</span><span class="p">(</span><span class="n">tB</span><span class="p">)</span>

<span class="k">while</span> <span class="ow">not</span> <span class="n">m</span><span class="p">.</span><span class="nf">empty</span><span class="p">():</span>
    <span class="n">t</span> <span class="o">=</span> <span class="n">m</span><span class="p">.</span><span class="nf">wait</span><span class="p">(</span><span class="mi">5</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">t</span> <span class="ow">and</span> <span class="n">t</span><span class="p">.</span><span class="nf">successful</span><span class="p">():</span>
        <span class="c1"># tB is not temp-output, so we can read it normally
</span>        <span class="k">pass</span>
</code></pre></div></div> <h3 id="3-using-temp-outputs-in-daskvine-graph-executor">3) Using temp outputs in DaskVine graph executor</h3> <p>DaskVine already exposes this idea with a simple switch: <code class="language-plaintext highlighter-rouge">worker_transfers</code>.</p> <p>When <code class="language-plaintext highlighter-rouge">worker_transfers=True</code> (the default), DaskVine tries to treat intermediate results as worker-resident and only brings results back to the manager when needed (targets, checkpoints, etc.). In the DaskVine implementation, tasks that are considered “lazy” for transfer enable temp output so that their results remain on workers.</p> <p>In practice, you use it like this:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">ndcctools.taskvine.compat.dask_executor</span> <span class="kn">import</span> <span class="n">DaskVine</span>
<span class="kn">import</span> <span class="n">dask</span>

<span class="n">m</span> <span class="o">=</span> <span class="nc">DaskVine</span><span class="p">(</span><span class="n">name</span><span class="o">=</span><span class="sh">"</span><span class="s">my-dask-run</span><span class="sh">"</span><span class="p">)</span>

<span class="c1"># Default is worker_transfers=True: intermediates stay on workers.
</span><span class="k">with</span> <span class="n">dask</span><span class="p">.</span><span class="n">config</span><span class="p">.</span><span class="nf">set</span><span class="p">(</span><span class="n">scheduler</span><span class="o">=</span><span class="n">m</span><span class="p">.</span><span class="n">get</span><span class="p">):</span>
    <span class="n">result</span> <span class="o">=</span> <span class="n">some_dask_collection</span><span class="p">.</span><span class="nf">compute</span><span class="p">()</span>
</code></pre></div></div> <p>If you want to force intermediates to be brought back to manager (more conservative, often slower at scale), you can set:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">with</span> <span class="n">dask</span><span class="p">.</span><span class="n">config</span><span class="p">.</span><span class="nf">set</span><span class="p">(</span><span class="n">scheduler</span><span class="o">=</span><span class="n">m</span><span class="p">.</span><span class="n">get</span><span class="p">):</span>
    <span class="n">result</span> <span class="o">=</span> <span class="n">some_dask_collection</span><span class="p">.</span><span class="nf">compute</span><span class="p">(</span><span class="n">scheduler</span><span class="o">=</span><span class="n">m</span><span class="p">.</span><span class="n">get</span><span class="p">,</span> <span class="n">worker_transfers</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>
</code></pre></div></div> <h2 id="practical-advice">Practical advice</h2> <p>Temp intermediates help most when your DAG has big intermediate edges and lots of reuse. They help less (or can hurt) when intermediates are tiny, when the cluster network is already saturated, or when worker local disks are very constrained.</p> <p>There’s an important implication to keep in mind: keeping data on workers trades manager-side throughput for durability. If you’re running on a cluster where worker failure, preemption, or churn is common, then leaving key intermediates only on worker-local disks is risky—losing a worker can mean losing the only copy and forcing recomputation. In those environments, you may prefer to bring important data back to the manager (or checkpoint it to a shared filesystem) so the workflow can survive worker loss without paying the full recompute cost.</p> <p>This is especially true for expensive intermediates: if a task ran for hours to produce a file, you often want that output to be persisted rather than treated as a temporary cache entry. On the other hand, for large numbers of fine-grained tasks (small outputs, lots of edges) or on clusters with good worker reliability, temp intermediates are usually the right default because they reduce manager-worker traffic and keep the system moving.</p>]]></content><author><name>Cooperative Computing Lab</name></author><category term="technical-articles"/><category term="taskvine"/><summary type="html"><![CDATA[Large DAGs often bottleneck on intermediate data. Here's how TaskVine's temp files keep intermediates on workers and cut manager-side transfers.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://ccl.cse.nd.edu/assets/blog/2026/taskvine-insights-intermediate-data-model/TaskVine-Insights.png"/><media:content medium="image" url="https://ccl.cse.nd.edu/assets/blog/2026/taskvine-insights-intermediate-data-model/TaskVine-Insights.png" xmlns:media="http://search.yahoo.com/mrss/"/></entry><entry><title type="html">TaskVine Insights - Example Workflow Logs</title><link href="https://ccl.cse.nd.edu/blog/2026/taskvine-insights-example-logs/" rel="alternate" type="text/html" title="TaskVine Insights - Example Workflow Logs"/><published>2026-01-20T17:00:00+00:00</published><updated>2026-01-20T17:00:00+00:00</updated><id>https://ccl.cse.nd.edu/blog/2026/taskvine-insights-example-logs</id><content type="html" xml:base="https://ccl.cse.nd.edu/blog/2026/taskvine-insights-example-logs/"><![CDATA[<div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/taskvine-insights-example-logs/TaskVine-Insights-480.webp 480w,/assets/blog/2026/taskvine-insights-example-logs/TaskVine-Insights-800.webp 800w,/assets/blog/2026/taskvine-insights-example-logs/TaskVine-Insights-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/taskvine-insights-example-logs/TaskVine-Insights.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Understanding a distributed runtime is much easier when you can inspect what actually happened during execution. To help users quickly learn and explore TaskVine, we introduce the TaskVine Example Logs repository:</p> <p><a href="https://github.com/cooperative-computing-lab/taskvine-example-logs">https://github.com/cooperative-computing-lab/taskvine-example-logs</a></p> <p>A quick screenshot:</p> <div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/taskvine-insights-example-logs/example-logs-repo-screenshot-480.webp 480w,/assets/blog/2026/taskvine-insights-example-logs/example-logs-repo-screenshot-800.webp 800w,/assets/blog/2026/taskvine-insights-example-logs/example-logs-repo-screenshot-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/taskvine-insights-example-logs/example-logs-repo-screenshot.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>This repository contains curated execution logs from real TaskVine workloads, all were from in-production scientific workflows. The logs capture runtime behavior such as task scheduling, worker activity, data movement, and performance statistics. Users can study realistic executions without setting up clusters or running large experiments themselves.</p> <p>The logs are designed to be used together with TaskVine’s analysis and visualization tools. In particular, the TaskVine Report Tool can directly consume these logs and generate timelines, throughput plots, and performance breakdowns, making it easy to understand where time is spent and how a workflow executes in practice, please check this repository for more instructions:</p> <p><a href="https://github.com/cooperative-computing-lab/taskvine-report-tool">https://github.com/cooperative-computing-lab/taskvine-report-tool</a></p> <p>If you are new to TaskVine, the official manual explains how logs are produced and how they reflect the runtime’s execution model:</p> <p><a href="https://cctools.readthedocs.io/en/latest/taskvine/">https://cctools.readthedocs.io/en/latest/taskvine/</a></p> <p>This repository is part of the broader CCTools ecosystem, where you can find the entire TaskVine codebase:</p> <p><a href="https://github.com/cooperative-computing-lab/cctools">https://github.com/cooperative-computing-lab/cctools</a></p> <p>If you have questions, run into issues, or want to discuss TaskVine behavior, please use GitHub Discussions, which is our official support channel. The team actively monitors the forum and typically responds quickly:</p> <p><a href="https://github.com/cooperative-computing-lab/cctools/discussions">https://github.com/cooperative-computing-lab/cctools/discussions</a></p> <p>We hope these example logs make it easier to understand TaskVine, explore its runtime behavior, and turn execution data into useful insights.</p>]]></content><author><name>Cooperative Computing Lab</name></author><category term="taskvine-insights"/><category term="tech"/><category term="taskvine"/><category term="insights"/><category term="logs"/><summary type="html"><![CDATA[TaskVine users can explore these example logs and gain insights into how workflows are executed, what information TaskVine provides for developers, and which visualization tools are available.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://ccl.cse.nd.edu/assets/blog/2026/taskvine-insights-example-logs/TaskVine-Insights.png"/><media:content medium="image" url="https://ccl.cse.nd.edu/assets/blog/2026/taskvine-insights-example-logs/TaskVine-Insights.png" xmlns:media="http://search.yahoo.com/mrss/"/></entry><entry><title type="html">Graduate Students Share Research and Systems Insights at SC25</title><link href="https://ccl.cse.nd.edu/blog/2026/graduate-students-share-research-and-systems-insights-at-sc25/" rel="alternate" type="text/html" title="Graduate Students Share Research and Systems Insights at SC25"/><published>2026-01-15T17:00:00+00:00</published><updated>2026-01-15T17:00:00+00:00</updated><id>https://ccl.cse.nd.edu/blog/2026/graduate-students-share-research-and-systems-insights-at-sc25</id><content type="html" xml:base="https://ccl.cse.nd.edu/blog/2026/graduate-students-share-research-and-systems-insights-at-sc25/"><![CDATA[<p>At SC25, graduate students from our lab participated in poster sessions, workshops, panel discussions, and research demonstrations, engaging with researchers from universities, industry, and national laboratories across the HPC community.</p> <div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/graduate-students-share-research-and-systems-insights-at-sc25/sc25-photo2-480.webp 480w,/assets/blog/2026/graduate-students-share-research-and-systems-insights-at-sc25/sc25-photo2-800.webp 800w,/assets/blog/2026/graduate-students-share-research-and-systems-insights-at-sc25/sc25-photo2-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/graduate-students-share-research-and-systems-insights-at-sc25/sc25-photo2.jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Graduate student Colin Thomas presented a poster covering his work completed over the summer at NERSC, Lawrence Berkeley National Laboratory. The poster, titled “Inference-as-a-Service Prototype at NERSC,” was previously referenced in an earlier blog post. During the poster session, Colin received a number of visitors and had the opportunity to explain the work and answer questions. Attendees were largely interested in the policy decisions made in the inference service pipeline, such as the scaling of compute resources in response to the client demand and the support of multiple users making inferences to the same server.</p> <p>Graduate student Ryan Hartung attended SC25 in St. Louis, where he participated in workshops, technical sessions, and discussions with researchers and practitioners from universities, corporations, and national laboratories around the world. He joined the XLOOP workshop on Sunday, where a member of the RADICAL-collaboration team presented the group’s paper on xGFabric. Later in the workshop, Ryan was selected to represent the team on the panel discussion “Bridging the Edge-to-Exascale Divide: Real-Time Scientific Workflows in the 5G Era,” alongside panelists from Nvidia, Lawrence Berkeley National Laboratory, and the University of Louisiana at Lafayette.</p> <p>At the Department of Energy booth, Ryan demoed his research, showcasing a real-time interaction between a 5G network and an HPC simulation, supported by a presentation outlining the research problem, implementation, and end-to-end system design. A discussion with a national lab researcher highlighted potential for xGFabric as a reusable, domain-agnostic software stack, particularly for scenarios combining low-capacity sensors with large-scale HPC resources for adaptive, real-time science. These conversations led to new ideas, including scaling private 5G deployments to multi-site sensor federations and orchestrating large drone fleets for experiment-in-the-loop workflows.</p> <p>Beyond formal sessions, Ryan engaged in conversations throughout the conference on topics such as AI–HPC integration, power-aware scheduling, quantum–HPC hybrid algorithms, in-memory processing fabrics, and cooling approaches for exascale systems. These interactions provided fresh perspectives on current challenges and reinforced emerging directions in storage, networking, AI integration, resilience, and sustainable computing.</p> <p>Below are some nice photos they took during the conference:</p> <div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/graduate-students-share-research-and-systems-insights-at-sc25/sc25-photo1-480.webp 480w,/assets/blog/2026/graduate-students-share-research-and-systems-insights-at-sc25/sc25-photo1-800.webp 800w,/assets/blog/2026/graduate-students-share-research-and-systems-insights-at-sc25/sc25-photo1-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/graduate-students-share-research-and-systems-insights-at-sc25/sc25-photo1.jpeg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/graduate-students-share-research-and-systems-insights-at-sc25/sc25-photo3-480.webp 480w,/assets/blog/2026/graduate-students-share-research-and-systems-insights-at-sc25/sc25-photo3-800.webp 800w,/assets/blog/2026/graduate-students-share-research-and-systems-insights-at-sc25/sc25-photo3-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/graduate-students-share-research-and-systems-insights-at-sc25/sc25-photo3.jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/graduate-students-share-research-and-systems-insights-at-sc25/sc25-photo4-480.webp 480w,/assets/blog/2026/graduate-students-share-research-and-systems-insights-at-sc25/sc25-photo4-800.webp 800w,/assets/blog/2026/graduate-students-share-research-and-systems-insights-at-sc25/sc25-photo4-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/graduate-students-share-research-and-systems-insights-at-sc25/sc25-photo4.jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="row justify-content-sm-center"> <div class="col-sm-12"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/blog/2026/graduate-students-share-research-and-systems-insights-at-sc25/sc25-photo5-480.webp 480w,/assets/blog/2026/graduate-students-share-research-and-systems-insights-at-sc25/sc25-photo5-800.webp 800w,/assets/blog/2026/graduate-students-share-research-and-systems-insights-at-sc25/sc25-photo5-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/blog/2026/graduate-students-share-research-and-systems-insights-at-sc25/sc25-photo5.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>It was an invigorating dive into the evolving world of high-performance computing at SC25. The conference provided valuable opportunities for our students to share their research, learn from peers, and explore new directions in HPC. We look forward to building on these connections and insights as we continue to advance the field of cooperative computing.</p>]]></content><author><name>Cooperative Computing Lab</name></author><category term="news"/><category term="news"/><summary type="html"><![CDATA[Two of our graduate students participated in SC25, presenting work on inference services at NERSC and edge-to-HPC workflows.]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://ccl.cse.nd.edu/assets/blog/2026/graduate-students-share-research-and-systems-insights-at-sc25/sc25-photo2.jpg"/><media:content medium="image" url="https://ccl.cse.nd.edu/assets/blog/2026/graduate-students-share-research-and-systems-insights-at-sc25/sc25-photo2.jpg" xmlns:media="http://search.yahoo.com/mrss/"/></entry></feed>